# 安卓高级诊断与本机容量边界

- 日期：2026-09-24；状态：`confirmed`；来源：[自动化和真实验收](../../docs/qa/android-management/2026-09-24-advanced-logs-and-capacity.md)。

高级日志是单次明确同意的诊断请求。后端只采当前工作区已归属、正在运行的 ReDroid 设备，最多五台；每台从最近 200 行、最多 64 KiB 的 logcat 中按时间保留最近 300 秒。导出仅含时间与级别；应用可控的 tag 和正文均不保存。受限下载 ID 五分钟过期。真实 HTTP 采集 196 条元数据，并验证合成敏感文本未进入结果。

本机 Lima `free -m` 总量 7921 MiB；设备最低 768 MiB、运行环境保留 512 MiB。十台最低总需求 8192 MiB，故本机十台规模验收 `blocked`。容量变化后需实测，不把数学准入判断冒充实际十台运行。
