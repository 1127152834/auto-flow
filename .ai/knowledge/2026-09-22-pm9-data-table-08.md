# PM9 DATA-TABLE-08 补证

- 日期：2026-09-22
- 状态：confirmed
- 2026-09-23 校正：下文将一般 mapped 入口/权限拒绝描述为系统目标拒绝的范围已 superseded；精确反例与强化断言见 `2026-09-23-pm9-input-validation.md`。
- 来源：`apps/backend/tests/integration/test_project_table_schema_capability.py` 定向回归与 `node scripts/verify-pm9-coverage.mjs`

新增 `test_system_status_is_read_only_while_business_status_field_and_status_write_coexist`。真实 SQLite/能力服务场景验证：结构查询返回固定 `statusId` 且不可写；普通 `status` 字段可并存；伪造系统字段改型、删除和来源映射分别拒绝，拒绝操作不生成操作事实或推进表版本；正式状态命令仍能写入记录状态。

定向文件回归为 15 passed；Ruff 与 PM9 覆盖引用校验通过。覆盖台账当前为 251 条中 236 条有范围断言、15 条未定位，198 partially_verified、53 planned、0 verified。生产 worker、打包应用、三平台原生交互和 Google 实网证据仍未由此补证。
