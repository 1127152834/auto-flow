# 安卓管理全分支复审结论

- 日期：2026-09-24；状态：`confirmed`（代码与定向 RED→GREEN；全阶段验收仍 `partial`）。来源：[最终分支审查 QA](../../docs/qa/android-management/2026-09-24-final-branch-review.md)及隔离工作区全分支只读复核。
- 共享运行时独占锁必须覆盖镜像内容的实际变动与其目录/Operation 发布，以及备份归档到引用目录的发布窗口；仅包住 Docker 调用不足以阻止数据丢失。
- 批次公开修订号为 `generation+1`。容量等待后的复读只能提前发现冲突；最终栅栏必须在生命周期控制器持锁后复核。显式失败重试可冻结新的内部 `retryExpectedRevision`，原批次创建输入仍不可变。
- 跨重启核实必须使用持久身份与只读客体证据：APK 安装查归属容器内包版本，删除查归属容器/卷是否存在；未知结果不能由缺失的 ADB tunnel 或旧设备快照推断。
- 真实 Mac/ReDroid 于 2026-09-24 用 Shizuku `versionCode=1086` 验证：安装后重建控制台，原回执核实成功并清除持久命令标记，设备可恢复控制；本轮自建实例与卷最后删除并确认消失。命令及输出见上述 QA，不将此视为真实 HTTP 进程级中断或 ADB 实际断线验收。
- 清理父操作只有冻结的所有候选均有持久子项，且所有子操作成功时才能成功；查询原请求需汇合异步子操作。恢复目标创建前的预检失败应留下确定的失败终态。
- 最终只读复审未见未报告的 Critical/Important；[最终完整软件门槛](../../docs/qa/android-management/2026-09-24-final-full-gates.md)后端 `4031 passed`、Node 22 前端 `5625 passed`，其他检查均 exit 0。复审与自动化均不代替真实 Mac 完整故障矩阵。GApps 专用镜像/账号、十实例本机容量、真实断网和若干磁盘/归档中断仍见验收矩阵。
