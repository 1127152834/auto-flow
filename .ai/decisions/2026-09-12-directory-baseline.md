# 目录预设基准

- 日期：2026-09-12
- 状态：confirmed（本次用户授权预设骨架；不代表业务实施计划已批准）
- 来源：本次用户请求、已批准 `docs/architecture/README.md`、现有源码布局。
- 决定：保留 `apps/backend` 和 `apps/desktop/src` 运行路径，补齐后端分层、前端 domains、UI 包骨架和测试目录。
- 取舍：只创建本阶段已明确领域的目录及占位，不改业务代码、依赖、导入或构建配置；不创建第二套 sidecar/services 路径。
- 限制：`packages/ui` 尚未注册为工作区；现有 UI 草稿保留原位。
- 待协调：迁移规格第 3 节使用了另一套路径，主任务在实施前应统一；本次不修改主任务规格。
- 验证：目录职责清单逐项存在性核验；现有仓库结构测试。
- 规范入口：`docs/PROJECT_STRUCTURE.md`。
