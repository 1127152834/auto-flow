# M3 拾取会话与框架定位

日期：2026-09-13；状态：confirmed；来源：用户逐项确认并批准实施计划。

拾取使用独立临时可见会话，用户手动准备网页；与运行互斥，确认关闭并清理后运行。新增 framePath 到原五类元素节点，同时覆盖拾取、测试和执行，不新增 switch_iframe 节点或运行时全局框架状态。会话只在内存，不迁移历史数据库。继续 workflows 分层，复用启动、资源锁、进程清理及离开协议。录制/Debug另行设计。

验证补充：浏览器worker通道使用 asyncio.timeout 对当前任务限定读写预算，避免 Python 3.11 wait_for 在读写完成与取消竞争时吞掉停止。依据为真实HTTP任务栈、4个先失败后通过的竞态测试、源码/冻结各连续5次启动即关闭。进程未确认清理时有界失败，保留资源锁与重试责任。
