# 安卓模拟器管理完整实施授权与基线校准

- 日期：2026-09-22
- 状态：confirmed
- 来源：用户明确要求完成 AM1–AM4 全部开发与验收，并要求先依据当前 HEAD、迁移图和未提交改动重新校准计划。
- 隔离分支：`codex/android-management-complete`；起点 `a92f0688f206d4339ff4468c1871f3ccdd6816dc`。

## 执行边界

- 主工作区现有未提交 Studio 迁移修改和证据目录属于其他任务，不覆盖、不清理、不回写。
- 允许在隔离 worktree 修改安卓后端、前端、OpenAPI 生成类型、增量迁移、测试、验收证据和受影响 `.ai` 记录。
- 继续使用现有 Mac/Lima/ReDroid provider、设备归属校验、独占控制、generation/sequence 栅栏、幂等和结果未知保护。
- 不恢复退役工作流执行链；工作流接口仅保留兼容拒绝边界，安卓管理首页不依赖工作流分配。
- 真实设备、网络、账号和数据变更只针对本轮新建且带标签的隔离资源；条件不足时记录 `blocked`。

## 当前校准事实

- 当前唯一 migration head 为 `0019_recording_commands`；新迁移必须从该 head 增量演进。
- 现有 Android 基础生命周期、运行时归属校验、控制台输入护栏和 APK 基础能力已存在；AM1 的主要工作是拆分管理契约、持久操作和控制会话，并移除管理 UI 对工作流查询的依赖。
- 原 2026-09-19 文档中的 `5f07e2ad`/`pm07_environments` 作为历史编写基线保留，不能作为本次实现父节点。

## 交付方法

每个任务按 RED → GREEN → 回归验证执行；测试必须先证明缺失行为。出现失败先完成根因调查、数据流追踪和最小复现，再做单一修复。每阶段同步更新 OpenAPI、迁移、测试、验收证据和本记录/阶段索引。
