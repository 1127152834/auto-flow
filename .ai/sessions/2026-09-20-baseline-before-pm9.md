# PM9 开工前的基线归并与修复

- 日期：2026-09-20；状态：confirmed。
- 来源：用户要求合并全部有效代码到 baseline，再建立 PM9 分支；Git 祖先核查、独立只读审查及本轮验证。
- 基线原 tip：719451b7；Studio checkpoint 真合并 45c114ff；远端 Android AM1（实际只有已批准规划文档）真合并 29554d93。两条退役 Studio/M6 快照依既有 ADR 保持归档，不恢复旧执行器。
- 独立审查三项均复现并修复：模块原命令重试不再重新选择名称或合并当前版本后算摘要；敏感参数按目标参数名传递，输出继续传播敏感性且不入事件明文；模块→工作流→模块交替引用纳入同一 worker 冻结闭包及浏览器需求判定。
- 反例先失败：原请求在源修改/删除后恢复；两种敏感来源（父参数、模块内凭据）；模块内已保存浏览器工作流引用。修复后模块/HTTP/worker/coordinator 36 项通过，ruff 与 mypy（392 文件）通过。前端 404 文件 / 5445 项、合并快照全量后端 3155 passed / 15 skipped 通过；后端全量运行开始于修复之前，三处改动另外有定向回归。
- 请求表不重写。新 update/import/duplicate 摘要按原逻辑命令确定；旧 checkpoint 已保存的这三类 receipt 无原始请求负载，不能无歧义转成新摘要，旧请求若返回 IDEMPOTENCY_CONFLICT 应核对已保存模块，不能自动更换请求 ID 盲重发。
- 本记录不代表 PM9 完成。项目生产运行 catalog 仍只有四个浏览器节点，PM4–PM8 管理验收中的 fake executor 不能替代完整发行链。
