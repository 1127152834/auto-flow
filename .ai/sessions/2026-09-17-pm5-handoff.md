# PM5 Handoff · 2026-09-17 02:21 +08

状态：`proposed`（现场已核对；真实 persist 未通过；未提交）
来源：本 worktree 代码、`docs/superpowers/plans/2026-09-17-project-management-pm5.md`、失败测试 `terminals/535482.txt`、成功桌面构建 `terminals/535483.txt`
给接手 agent：直接按第 3 节顺序做，不要重开 PM6 / 不要重设计 / 不要提交除非用户要求。

---

## 0. 你是谁、任务是什么

在**唯一**隔离工作区完成 PM5，不重开、不重设计、不进 PM6。

优先演示链（用户明确）：

1. 真实 UI 建项目 + 账号表/记录
2. 启动任务
3. 在本地测试站登录
4. End 保留并关联账号
5. 关闭原浏览器
6. 后续任务按关联环境恢复登录态

允许：工作流节点推进用隔离/模拟执行器。
必须真实：登录保存/恢复走 CloakBrowser `launch_persistent_context_async` + 本地测试站。
报告必须拆开：「管理侧及实际浏览器环境存取已验证」vs「真实执行核心接入待验收」。
停在 PM5 验收。不 commit，除非用户明确要求。

---

## 1. 现场（只改这里）

| 项 | 值 |
|---|---|
| 工作区 | `/Users/zhangtiancheng/Documents/projects/autoflow-project-management-pm5` |
| 分支 | `codex/project-management-pm5` |
| 基线 | PM4 `fbda6f1736b53ebf8144513c893e1bf6bc5a459b` |
| 主/旧树 | **只读**。不要改其它 worktree。 |
| 唯一执行卡 | `docs/superpowers/plans/2026-09-17-project-management-pm5.md` |
| 规格 | 总里程碑 PM5 节、C10–C18、HTTP §3.6。迁移实际文件是 `pm07_environments.py`（`pm05_environments` 名已被占用）。 |
| QA Electron | PID `94298`，`--remote-debugging-port=9333`，`--user-data-dir=/var/folders/8g/sq3srr71063c083rpkd32k380000gn/T/autoflow-pm5-ui-qa-qst8mgd6/workspace` |
| 内核可执行文件 | `~/Library/Application Support/@autoflow/desktop/data/kernels/chromium-145.0.7632.109.2/Chromium.app/Contents/MacOS/Chromium` |
| QA 工作区内核副本 | `$QA_WORKSPACE/data/kernels/chromium-145.0.7632.109.2`（已拷过） |
| 已构建桌面 | `electron-vite build` 成功，`main-Z4X45WJi.js`。**当前 9333 上的进程仍是重建前的旧 Electron，必须只杀 QA 进程后重拉。不要动 Cursor / Chrome / 9222。** |

Git：大量 PM5 未提交改动（已跟踪文件 + 大量 `??` 新文件）。不要 `git add -A` 扫进无关物。不要 commit。

---

## 2. 已完成 vs 未完成

### 管理侧（代码+合同测试大体完成，UI 旧构建已走过一遍）

- 环境身份 / 三种来源 / 占用 / 目录 / 详情 / 重命名
- End 保存 / 另存 / 关联 / `saved_unlinked` 修复 / 维护打开放弃 / 删除影响查询（删除命令属 PM8）
- 人工继续/结束/超时后端与环境页目录
- TaskEndPanel：`recordTargets`、替换勾选、修复按钮
- 额度：`LIVE_INSTANCE_STATES`、`check_live_capacity()`、`max_live_instances=32`、`CAPACITY_EXHAUSTED` 429
- `POST /environment-instances/{instanceId}/open` 202；UI「进入当前浏览器」
- `resource_query` 已按 `fixedEnvironment` 解析 profile/kernel
- 启动规则已去掉 PM4「只允许 newFromProfile」硬拦截（源码已改，**旧 Electron 未验证**）
- 保存成功后实例应收口 `closed`；运行目录只列活动实例
- 前端 OpenAPI 已生成过

QA 证据（管理侧，**不含**真实登录存取）：

- `docs/project-management/implementation/pm5/qa-runs/result.json`
- `docs/project-management/implementation/pm5/ui-verification.md`

QA 项目：`0b4af061-0693-42c8-8097-f74850b3e965`  
自动化 `登录环境演示`：`f76dd007-0d20-4b5f-bf6a-05291a6345db`，策略 `fixedEnvironment`  
保存环境：`f8c567f1-3631-4e15-b7e8-231e889ff175`（当时是占位文件，不是真实登录 cookie）

### 未完成（按阻塞顺序）

1. **真实 persist/restore 失败，且修复未复跑**
2. **生产 `acquire()` / worker 半接线**：`persistent` 需要 `userDataDir`，但 freeze/attach **没有写入** `userDataDir`
3. **`openInstance` 的 `opener=None`**：HTTP/UI 记账，不启动 CloakBrowser
4. **重建后的 Electron 未重拉**；启动对话框、额度、进入浏览器、End 面板未用新构建验收
5. **QA 工作区可能还没有公开 profile**（`browserVersion=145.0.7632.109.2`），启动会被 KERNEL/PROFILE 拦住
6. **11 项真实验收 / 同视口截图 / 覆盖账本未关**
7. 未提交；Windows / 打包 / 用户手测保持未验收

---

## 3. 接手后立刻做（不要先重写架构）

### 3.1 先让 persist 测试通过

上次失败（**修复前**的代码）：

```
tests/integration/test_environment_persist_restore.py:91
assert body == "已登录"
得到：未登录
```

之后已改、**尚未复跑**：

- Cookie 加 `Max-Age=86400; HttpOnly; SameSite=Lax`：`apps/backend/tests/fixtures/login_site.py`
- 拷贝/恢复时丢掉 Chromium 锁：`environment_store.py` 的 `_RUNTIME_LOCK_NAMES` / `_ignore_runtime_locks` / `_clear_runtime_locks`
- `persistent_launch_kwargs` 接受 `executablePath`
- persist 测试登录后断言 cookie、`storage_state()`、等 `SingletonLock` 消失

复跑：

```bash
KERNEL="$HOME/Library/Application Support/@autoflow/desktop/data/kernels/chromium-145.0.7632.109.2/Chromium.app/Contents/MacOS/Chromium"
export AUTOFLOW_TEST_CLOAKBROWSER="$KERNEL"
export CLOAKBROWSER_BINARY_PATH="$KERNEL"
uv run --directory apps/backend pytest \
  tests/integration/test_environment_persist_restore.py \
  tests/unit/test_environment_store.py \
  tests/unit/test_persistent_context.py -q --tb=short
```

若仍 `未登录`，先查恢复目录里有没有 `Default/Cookies` 或 `Default/Network/Cookies`，以及第二次启动是否因锁文件另开空 profile。不要把失败写成“管理侧已验证登录恢复”。

CloakBrowser 官方 API：`apps/backend/.venv/.../cloakbrowser/browser.py` 的 `launch_persistent_context_async(user_data_dir=...)`。`ensure_binary(browser_version=...)` 会自己找二进制；测试必须传 `executablePath`，否则可能和 QA 内核不是同一个。

### 3.2 补完半截的生产接线（persist 通过后再动）

当前半成品，**不能原样上线演示任务**：

`WorkflowBrowserResources.acquire()`（`apps/backend/src/autoflow/application/workflows/browser_resources.py`）

- 已接受 `browser in {newFromProfile, persistent}`
- `persistent` **强制**要求 `request["userDataDir"]`
- `ProjectRunResourceResolver` 只写了 `"browser": "persistent"` + `environmentRef`，**没有** `userDataDir`
- 因此现在启动 `fixedEnvironment` 任务，acquire 会 422「持久环境缺少工作副本目录」

`workflow_worker.py` 已能在 `browser.userDataDir` 存在时走 `launch_persistent_context_async`。

`list_instances` 已加 `run_id` 过滤，但 **没有** `find_instance_by_run()`。  
`attach_task_instance` 用的是 **core `run_id`**，dispatcher.acquire 拿到的是 **`run_request_id`**，二者不是同一个字段。不要拿 `run_request_id` 去查 `active_run_id`。

建议（选更小的）：

- attach 成功后把 `instanceId` / 实例目录写进该 Run 的 `resource_request`；或
- acquire 用 `environmentRef` + 该任务已 attach 的实例解析目录。

不要为了 QA 替换生产核心。隔离执行器只模拟节点推进，登录目录必须是真实 `user_data_dir`。

### 3.3 接上 `openInstance` opener

`EnvironmentService(..., opener=None)` 在 `apps/backend/src/autoflow/bootstrap/app.py` ~270。

`open_instance()` 在 `opener is None` 时只记 operation / 把实例标 active，**不启动浏览器**。  
「进入当前浏览器」必须headed 打开实例目录上的 persistent context。可复用 `launch_persistent_instance` + profile 的 fingerprint / `browserVersion` / kernel executable。`close_instance` 要能收到口打开的进程。

### 3.4 只重启 QA Electron，再验启动框

1. 只杀 PID `94298` 及其 QA 子进程（`user-data-dir` 含 `autoflow-pm5-ui-qa-qst8mgd6`）。不要杀 Cursor/Chrome。
2. 同一 workspace + `9333` 拉已构建的 `apps/desktop`。
3. 若无公开 profile：POST `browserVersion=145.0.7632.109.2`（与内核目录名一致）。
4. 打开自动化 `登录环境演示`，确认启动框不再报 `ENVIRONMENT_SOURCE_NOT_SUPPORTED` / 「当前仅支持从浏览器配置创建临时环境」。
5. 再走：启动 →（隔离执行器可推进节点）→ 真实浏览器登录夹具 → End 关联 → 关原浏览器 → 下一任务恢复。

登录夹具：

- `apps/backend/tests/fixtures/login-site.html`
- `apps/backend/tests/fixtures/login_site.py`
- 账号 `demo` / `pass`；`GET /me` 有 cookie 则 `已登录`，否则 `未登录`

### 3.5 额度 / End 面板 / 11 项

额度合同：`tests/contract/test_project_environments.py`（live capacity + open）。现场满应 429，UI 说明阻断。

End 面板：`apps/desktop/src/renderer/domains/project-runs/components/TaskEndPanel.tsx`，管理侧 End 是 `POST /tasks/{taskId}/end`。

11 项真实验收：执行卡勾的是「11 项真实验收、视觉同视口、手测、覆盖账本」。总里程碑 PM5 固定场景 + 真实应用段（`2026-09-13-project-management-milestones.md` §PM5）是验收口径。没有单独的 11 行清单文件；按该节「固定场景」+「真实应用」逐条留证据，缺平台标未验收。

自动验收命令（执行卡）：

```bash
uv run --directory apps/backend pytest \
  tests/unit/test_project_environment_rules.py \
  tests/contract/test_project_environments.py \
  tests/integration/test_project_environment_retention.py \
  tests/integration/test_project_manual_actions.py -q
npm --workspace @autoflow/desktop test -- src/renderer/domains/environments
npm run openapi:generate && npm run openapi:check
```

Cookie / token / 密码禁止进日志、截图报告、提交。

---

## 4. 关键文件

| 职责 | 路径 |
|---|---|
| 执行卡 | `docs/superpowers/plans/2026-09-17-project-management-pm5.md` |
| 规格 | `docs/project-management/implementation/contracts.md` C10–C18；`api-contracts.md` §3.6 |
| 域 | `apps/backend/src/autoflow/domain/environments/{models,rules}.py` |
| 服务 | `application/environments/{service,retention,manual}.py` |
| 存储 | `infrastructure/filesystem/environment_store.py` |
| HTTP | `adapters/http/project_environments.py` |
| 迁移 | `infrastructure/database/migrations/versions/pm07_environments.py` |
| persist helper | `providers/browser/persistent_context.py` |
| 工作流租约 | `application/workflows/browser_resources.py` |
| worker | `providers/browser/workflow_worker.py`（仍默认 `launch_context_async`） |
| 启动 freeze | `application/project_runs/resources.py` |
| 启动规则 | `domain/project_runs/rules.py`（三种来源已放行） |
| attach 时机 | coordinator ~401；scheduler `_attach_claimed_environment` |
| 前端环境 | `apps/desktop/src/renderer/domains/environments/` |
| End UI | `domains/project-runs/components/TaskEndPanel.tsx` |
| persist 测试 | `apps/backend/tests/integration/test_environment_persist_restore.py` |

生产 `WorkflowBrowserResources.acquire()` 历史上只接受 `newFromProfile`。现已放行 `persistent`，但目录注入未完成。

---

## 5. 不要做

- 不要新开 PM6，不要改 Studio 画布 / transport / bridge
- 不要第二套生产执行器
- 不要把模拟执行写成“生产工作流可用”
- 不要占用 Chrome `9222`；QA 只用 `9333`
- 不要杀非 QA Electron
- 不要把 cookie/token 写入 `.ai`、截图说明或 commit
- 不要在 persist 仍失败时宣称演示链完成
- 不要 commit，除非用户要求

---

## 6. 报告口径

分两栏写，不要混：

1. **管理侧及实际浏览器环境存取已验证**：Electron + FastAPI + SQLite + 目录副本 +（仅当 persist 测试或真实 UI 链通过时）CloakBrowser 登录恢复
2. **真实执行核心接入待验收**：生产 worker 用 persistent `user_data_dir` 跑完整工作流节点

Windows / 其它 arch / 打包 / 用户手测：未执行则保持未验收。

---

## 7. 旧会话里过时的句子

`.ai/sessions/2026-09-17-pm5-resume.md` 已过时：

- 「本机未设置 AUTOFLOW_TEST_CLOAKBROWSER」——已设过，测试**跑了但失败**
- 「UI 打开的是改前构建，尚未重新打包」——桌面已 rebuild，**进程未换**
- 「额度与现场进入未完」——后端/UI 合同在，**opener 未接、新构建未验**
