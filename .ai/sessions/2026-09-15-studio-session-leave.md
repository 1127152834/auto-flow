# Studio 会话离开与执行策略核销

2026-09-15，confirmed。用户要求保留成果，改为唯一缺口清单、源码迁入、完整功能块关闭。

- 在原实施计划第3节维护唯一9块剩余清单；capabilities/verified-cases核销已有证据。旧专项“尚未”文字只作历史，不叠加为新任务。
- 保留本轮 documentLeave/useDraftProtection 共享离开保护，拾取/录制参与；取消与失败保留，会话身份变化阻止误清理。运行/宿主仍未接入，不声称完成。
- 全量292文件3492测试，typecheck/lint/build通过，浏览器Mock录制取消/保存后新建/重新打开验证完成。证据在既有 evidence/f5-session-leave。
- 下一完整功能块 F4.2：只补已批准的审查编辑、变量化、预览、分页和恢复，保留冻结原转换算法。非阻塞问题先登记。

## 同日继续：正式窗口与共享会话协调

- 接入同一离开资源协调：运行按原runId停止并查询终态；拾取/录制切换先确认，保留浏览器时不关闭共享浏览器；浏览器直接关闭也先处理会话。审查409保留尾部，重试保存后离开；删除到空审查仍持久化。
- 正式主进程/preload主frame握手；关闭/退出/切工作区先保存清理。显式重启有活跃会话时拒绝。工作区成功重建按realpath隔离的renderer分区；失败解锁原窗。
- CUA正式Electron实测取消/保存关闭、重开、取消/保存退出和A-B-A隔离恢复。修复实测发现的后台确认、取消误报权限和macOS路径别名。未操纵Store、未运行CDP smoke冒充UI。
- 当前回归298文件3547测试通过；后续录制审查局部7测试通过；typecheck/lint/build通过快照分别留档。当前操作窗口出现用户切换，主动让出，不继续覆盖用户设置。
- 对应F5-LEAVE-021–028。仍9个未关闭块；F5剩启动/状态双响应丢失占用保护及活跃会话原生交叉验收，不声明整块完成。下一步先收口本块，再F3.3结果诊断。

- 最后收口不确定启动/重连：启动与状态同时丢失仍占用；启动pending时拒绝提前清理；恢复后确认实际sessionId才关闭；关闭后的旧status/pages不能复活占用。F5-LEAVE-029–030，最后局部6文件60用例通过。此前全量298/3547是共享协调快照，不谎称覆盖之后的新分支。
- 并行用户任务正在追加删除数据库/DP和收窄浏览器配置。已通知保留AutoBrowserDialog的统一离开调用；本提交不包含节点目录/GlobalConfig/排除文档改动，最新节点数量待该任务提供后更新。

## 同日继续：结果与诊断

按既有F3.3缺口完成独立运行分页、大值、固定截止导出、历史选择及旧日志隔离。原版表格/变量面板保留，扩展服务适配，不建设执行器。内存与实际HTTP共用协议测试，正式Electron UI经主入口创建保存/Mock运行/详情/原生导出/重启恢复；未操纵Store。F3.3证据回填原台账，剩余8块。并行用户排除数据库/DP后节点数227，旧浏览器配置消费者迁移仍在F4.1/F5待办。分发包和未测平台保留F6，不混为通过。
