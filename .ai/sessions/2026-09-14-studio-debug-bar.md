# 调试控制条确认与停止优先

继续F3：错误可见，HTTP接受等待SSE，停止可打断前端等待，旧回执不能作用下一次暂停。增加仅供UI使用的pauseRevision，明确不冒充服务端pauseId。过滤其他workflowId的paused/resumed事件。实际UI发现停止写失败日志，已修复。

组件8+HTTP2，相关50项通过，类型/lint通过。实际UI已完成断点→单步→输入→下一暂停，变量值正确，停止退出；修复后界面回放显示INFO执行已停止，HTTP新停止日志断言通过。完整134文件1620项、构建与21脚本通过。独立runId与服务端调试命令幂等继续未完成。
