# 图像资源面板读取保护

2026-09-14，confirmed（F2/F5有界修复）。

资源及文件夹统一刷新，先校验两份响应再显示；图像使用后端共享样例读取器补齐path默认值。失败显示错误及重试，保留已有缓存，加载或失败不冒充空目录。请求序号、连接修订和卸载保护阻止旧回复写入；已有图像缓存期间变化时不覆盖它，同时仍显示合法文件夹列表。重命名、删除目录和移动后读取统一走这一入口。写操作自身的确认、并发及回执校验还未在本批解决。

初始7测试失败并出现2个未处理错误；补齐11个用例后连同上传、预览和共享合同共40项通过。测试中资源列表按文件名断言；图像字节预览由独立预览测试验证，避免以未提供图像字节的夹具要求img立即出现。TypeScript、ESLint、49脚本及构建通过，证据evidence/f2-image-panel-loading。

实际UI（2026-09-14约13:23，Codex内置浏览器、独立localhost:5175来源、Mock）：进入图像面板显示两张既有验收图；场景切离线，切日志再切回图像，显示“加载图像资源失败：Mock network offline”和重试按钮，两张缓存图片仍在。恢复连接并点击重试，错误消失，两张图正常。最后恢复在线并收起场景面板，未上传、删除或重命名资源。此证据不代表正式Electron或真实后端。

全量最近记录为上一批264文件3036项；本批未重新跑全量。全局工作区隔离、原始截图入口及所有资源写操作仍在后续验收范围。
