# 安卓自定义候选镜像真实链

- 日期：2026-09-24；状态：confirmed（实测范围），完整目标active/partial。
- 来源：[候选镜像报告](../../docs/qa/android-management/2026-09-24-custom-image.md)和结果JSON。基线471b816f，隔离worktree；本增量只改QA脚本与文档。
- 新候选由本机固定ReDroid基础派生，关闭build网络，只加测试文件和归属标签。基础/A均ready；tag及模板改指B后原实例保持A并读回v1。自定义备份恢复新实例读回v1和数据，模板/实例/备份各自阻止内容删除；引用解除后HTTP删除成功，基础保持不变。
- 两个QA错误（终态枚举、备份清理ID）按真实契约修正并重跑，未改生产逻辑。第三轮外部中断后核实进程已死/后端存活并清理自身资源；第四轮完整exit0。没有把部分成功或中断轮记为通过。
- 镜像/模板定向40 passed/1 warning；QA Ruff、授权开关、迁移唯一head和diff检查通过。完整门槛代码未变，沿用之前记录而非声称新运行。
- T12.3关闭；当前102步骤83 passed/16 not_run/3 blocked。T12回退演练、桌面控制/断线/缩放、性能指标、APK实际断连，以及GApps/十台条件仍未完成。
- 三个Studio文件哈希与保护基线一致，不纳入提交。

- 最终跨轮核实发现旧枚举漏掉第二轮一个无tag候选；按该轮固定ID与标签清除，并将QA枚举改为-a。最终9台实例/卷missing、8个候选ID不存在、备份记录为0；此前第二轮完整清理判断由此记录纠正。
