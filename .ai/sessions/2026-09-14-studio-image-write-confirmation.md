# 图像资源写操作确认

2026-09-14，confirmed，有界F2/F1修复。

七类操作（上传、删除、重命名、创建/重命名/删除目录、移动）经统一回执校验。上传沿用冻结源码asset信封，不要求原本不存在的success字段；其他操作必须success=true及符合各自类型的结果字段。矛盾错误、空响应、字符串success、非法资产元数据拒绝，保留HTTP/业务错误。资产path默认值与共享读取器一致。

删除失败保留图片并显示原因；资源和目录重命名失败保留编辑状态，资源重命名有实际组件回归。初始写回执36例中24失败，面板2例先失败；最终新增43合同例、2面板例，并含读取/上传/内存与HTTP回归共77项通过。TypeScript、ESLint、49脚本与构建通过。证据evidence/f2-image-write-confirmation。测试定位器曾移除Testing Library不支持的exact选项，name匹配保持准确，未放宽业务断言。

本批没有新增实际浏览器写操作或真实后端验收，没有重复全量。未知写入结果提示刷新核对，不能自动重发。并发写操作、稳定命令标识及迟到写回执的工作区隔离仍待处理，原始系统截图入口也未由此自动纳入受保护写入。
