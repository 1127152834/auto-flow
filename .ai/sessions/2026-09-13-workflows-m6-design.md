# M6 设计会话

> 2026-09-13 路线替代（superseded）：用户要求完整迁入 WebRPA UI、前端交互及后端业务逻辑，并允许弃用现有 Studio 实现。后续以[源码迁入规格](../../docs/superpowers/specs/2026-09-13-studio-webrpa-source-migration-design.md)为准。本文保留为历史记录；旧自定义视觉、节点/执行模型及 M6 计划不再约束新 Studio。M6 未完成试验已归档，不能标为交付。

日期：2026-09-13；状态：proposed，等待方案审查，未开始实施。

用户请求：继续设计下一阶段，先输出文档。前一任务是M5复测，最近相关提交76eb855。

完成：读取工程基准、当前工作流/拾取/调试实现与WebRPA冻结录制源码；建立M6正式设计、六批实施计划和全未执行验收矩阵；更新当前总里程碑和能力映射，历史编号不作为完成证据。

发现：旧录制缓冲读取即清空，iframe有name/index回退，导航采用10秒推断，密码遮罩不阻止原值进入事件；这些是静态代码发现，不冒充旧项目实测。新录制值包含模板语法时还需要明确原文语义，故提出schemaVersion3而非静默改变v2解释。

本轮取舍均为建议：独立会话、停止后保留现场、持久化录制草稿、常见动作优先、顶层尾部追加、页面别名。drag/upload及其他未完成核心能力仍单列后续，不声称完整对齐。

验证：本轮仅检查文档路径、内部链接、git diff空白与范围、来源及状态一致性；未运行业务测试。所有M6验收项为未执行。保留当前工作树中模型/UI/其他文档的无关改动，不修改.ai/memory/project-context.md或旧automation-studio/PLAN.md。

下一步：用户确认M6专项后，从批次A的真实采集通道与文档契约开始，不从页面挂假录制按钮开始。
