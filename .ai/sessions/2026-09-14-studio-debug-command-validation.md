# F3 调试命令拒绝路径

2026-09-14。修复 Mock 将任意 /debug/* 当作继续命令的问题。只接受 POST resume、step、breakpoints；未知动作 404，错误方法 405，无活跃运行/未暂停 409。断点必须为冻结文档节点标识数组，启动及更新都校验，错误返回 422。允许空数组以及轨迹未经过的合法分支节点；不根据实际选中轨迹删掉断点。

新增 34 个内存/真实 HTTP 用例。初始 24 个用例中 22 失败、2 通过；修复并补充启动及未选分支用例后，相关 60 项通过。前端全量 126 文件/1487 项通过，类型与 lint 通过。本批不包含随后拾取面板修改的最终全量验证。

这些是服务消费协议夹具测试，不证明真实浏览器调试。独立 runId、pauseId、命令修订、完整幂等单步合同和历史恢复尚未全部接入，F3 未完成。证据 evidence/f3-debug-validation。
