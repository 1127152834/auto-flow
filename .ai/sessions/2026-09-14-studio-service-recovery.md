# 服务反馈与连接恢复

2026-09-14，confirmed：有界修复；整体F0–F6仍实施中。

全局快捷键：注册失败不再静默，保留用户配置并写错误日志；重连成功记录恢复；同一错误去重；配置变化、较新的重连请求或卸载后，旧响应不能更新日志。复用现有快捷键接口，不新增系统全局热键实现。本次防护针对前端响应，服务端并发配置修订仍需正式合同闭合。

节点拾取：单元素与相似元素业务失败、网络异常均反馈，完整成功轮询后记录恢复并重置去重状态。失败保留原定位值与停止入口，不继续消费同轮的其它选择结果。

连接横幅：共享传输层仅在网络异常时报告离线，后续成功HTTP响应确认恢复。故障之前发出的旧响应不能消除较新的故障；替换传输后旧请求的错误不能污染新连接；HTTP业务错误和主动取消不被标为网络中断。横幅订阅恢复事件；手动检查抛错时仍可重试。恢复事件只说明服务通信恢复，不推断运行、浏览器或调试状态。

新增13项测试：快捷键6，拾取轮询3，连接恢复4。首次快捷键3失败，拾取3失败，连接3失败及1个未处理异常；修复后相关7文件76项通过。类型、lint、构建和21工程脚本通过。逐项证据见evidence/f5-service-recovery，真实点击发现并回归了恢复后横幅残留问题。

剩余：稳定run/session/command标识、跨入口所有权、全部节点配置分支、宿主异步离开、正式Electron与平台验收仍未完成。未将Mock当作真实自动化能力。

完整回归：146个前端测试文件、1749项全部通过（07:04:00启动，200.92秒）；未包含未执行的平台验收。
