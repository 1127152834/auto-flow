# F1 命令响应身份与形状校验

2026-09-14，confirmed。命令 POST 的成功 HTTP 响应必须是对象，包含与请求相同的 commandId 和布尔 success；错误身份或无效形状使用既有只读查询恢复，不重发命令。查询结果必须包含相同身份、布尔结果及 200–599 的整数原状态码，否则保持 unconfirmed。明确 HTTP 拒绝仍直接报告错误，不将冲突变成重试。稳定 ID 接受字符串，默认仍生成 UUID。

13 个新用例覆盖错误/缺失身份、缺失/非布尔结果、数组/null、查询错误状态码与明确 409。原订阅者异常隔离夹具补充合同要求的 commandId，保持原断言：消费者抛错不触发查询或重复提交。修复前10失败/3通过；相关37通过。全量121文件/1405用例、类型/lint/构建通过，证据 evidence/f1-command-identity。

此次并不意味着 HTTP 接受等于浏览器动作应用；独立 runId、所有命令业务处理、持久化状态查询、完整 schema 与正式 Electron 仍需后续完成。
