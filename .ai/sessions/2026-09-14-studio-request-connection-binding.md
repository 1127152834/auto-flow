# 请求发起时绑定连接

2026-09-14，confirmed，有界F1/F5修复。

apiRequest原本await preloadConfig()后才读取服务地址；preloadConfig只是检查同步配置，不执行异步加载。因此调用API后立即切换连接，会把准备给旧工作区的请求发送到新服务。移除多余等待，同步读取地址并进入当前传输函数；后续请求仍使用新连接。没有新增连接缓存或第二套请求器。

录制开始、停止、配置写入和读取4项先全部失败，修复后通过；相关6文件64项通过。全量264文件3,036项通过（484.00秒），包括图像合同、WebDAV、录制游标。类型检查、lint、49脚本检查、renderer/main/preload构建通过。证据 evidence/f1-request-binding。

这只保证已发起请求不会错发新服务，不解决后续定时轮询的会话所有权、旧响应消费和完整工作区切换清理。相关消费者仍需按会话及连接身份保护。正式Electron、实际后端及未实测平台没有新增验收结论。
