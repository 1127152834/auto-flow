# 图像写入连接与重命名并发

2026-09-14，confirmed，有界F2/F5。

七类图像资源写回执绑定调用时连接修订。连接变化后回执不可用于当前缓存，返回明确提示要求在原工作区核对，不重发、不声称后端操作已撤回。图片及文件夹重命名共享在途锁；Enter与blur只能发送一次，等待/错误确认期间输入禁用，失败确认后保留名称并恢复编辑。其他类别写操作的全局互斥、宿主退出与完整工作区协调仍未完成。

初始7个旧连接回执用例失败、Enter/blur重复提交1例失败；修复并增加文件夹分支后，9个新用例及现有资源回归共66项通过。TypeScript、ESLint、49脚本及构建通过。证据evidence/f2-image-write-races。未新增真实浏览器/正式Electron测试，也未重复全量。
