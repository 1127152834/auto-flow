# JavaScript脚本服务消费与诊断交付

2026-09-14，confirmed：本有界切片已实现，整体F0–F6尚未完成。合同见js-script-contract.md。

新增脚本请求/领取/结果/状态OpenAPI模型及生成类型；保留现有事件与命令通道。Mock在js_script节点等待前端领取和回传，确认结果后才继续，错误或停止不调度后续。仅同步原先已存在的变量，显式结果变量可以创建；变量追踪保存输入与脚本的实际变化。未初始化的输出占位不伪造为运行初值。

脚本和编辑器测试共用专用Worker。30秒上限与取消均终止Worker；同步main、严格JSON返回值、变量副本；Promise、DOM、非法结果明确失败。关闭或改代码使编辑器在途测试失效。同请求领取解决重复事件/其它窗口/页面重开抢执行；领取或回传响应丢失查询原commandId，读取状态断线则查询原requestId，不重跑已执行脚本。脚本完成/失败记录及claim必须由未来真实后端按合同维护，Mock内存不提供跨服务崩溃持久化。

测试新增54项：脚本值19、Worker生命周期5、代码编辑器4、内存/HTTP命令12、内存/HTTP消费14。后端8项合同校验通过。协议最初10失败；消费测试第一次1通过9失败，定位为测试清理全局localStorage后未重建，修复隔离夹具后保留全部断言通过。修复了测试替身this别名lint及Mock类型声明问题。

全量前端151文件1799项通过；此后追加畸形事件2项、大值2项，最终专项54项另行通过。类型、lint、构建、21脚本、OpenAPI、Ruff、mypy通过。构建与浏览器实测见证据；未将测试数量等同于完整里程碑完成。

遗留：全局独立runId/调试修订、其它交互命令、全部配置分支和工具、变量追踪面板本身的错误/竞态、宿主离开与正式包平台验收继续推进。同步脚本契约没有扩展成异步脚本语言；Worker不宣称是权限安全沙箱。
