# 零值滑块配置验收

2026-09-14，confirmed（F2有界配置修复）。284入口保持，不增加国际化。

范围：已保留 text_to_speech 音量与 sound_trigger 音量阈值。两处原 `||` 将合法0映射为默认1/50；改为仅空缺采用默认值，标签和输入一致。语速/音调同步采用空缺默认值并去除错误显示的引号。共享 Slider 转发 id/aria-label/aria-labelledby；四个配置滑块对应可访问标签，保留原视觉和原生键盘操作。

10个新增测试覆盖音量0/0.5/1、阈值0/50/100、缺省值、真实回调与零值回显、撤销重做、序列化重开。最初8失败2通过；第一次修复脚本未覆盖声音触发组件正文，仍4失败；修正后全部通过，与数字输入回归合计36项通过。284节点面板及输入弹窗相关327项回归、21脚本、TypeScript、ESLint、renderer/main/preload构建通过。最新全量基线为前一切片153文件1829项，本批增加10项专项，不冒称新全量结果。

浏览器通过真实动作库拖拽、输入、Home键、Cmd+S和打开列表验证，见browser-ui.md。仅编辑/Mock保存回读，没有朗读、监听系统音频或调用真实执行器。声音阈值是否由未来后端准确检测仍需真实后端验收；现有前端提示不能充当能力证据。
