# F1/F2 图像资源命令合同对齐

2026-09-14，confirmed。按冻结 reference/WebRPA/backend/app/api/image_assets.py 对照消费者：重命名成功返回 success+asset，创建目录返回 path，目录重命名返回 newPath，移动返回 newFolder，删除目录返回 deletedCount。否则现有面板会出现保存成功但列表不更新。

Mock 补齐无效名称、根目录删除、资源不存在、目录不存在和目标冲突的拒绝；拒绝不改变资源元数据。目录重命名/删除按完整路径段前缀处理，a 的子目录操作不会影响 ab。移动到新目录登记目录路径，沿用源码创建目标目录的行为。未修改已有用户资源或真实文件系统。

30 个新增内存/真实 HTTP 用例，最初22失败；相关65通过，类型/lint/构建通过。上一批全量123文件/1417通过，本批随后新增的30用例计入定向结果，未将旧全量数字当作本批全量验收。证据 evidence/f1-image-commands。实际浏览器验证重命名即时展示和刷新恢复。

完整API生成schema、资源分页、导入引用失效、目录路径跨平台以及正式Electron仍未全量验收；本批不声称图像资源所有能力或F1已完成。
