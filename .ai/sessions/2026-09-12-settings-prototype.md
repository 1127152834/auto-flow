# 设置页原型设计

- 日期：2026-09-12
- 状态：原型与新增提案 proposed；源码与运行取证 confirmed。
- 用户请求：开始设置页原型，核对旧功能，并提出合适的新增设置。

## 已完成

- 在独立临时 user-data 下启动旧 Electron 应用，实际查看常规、Workspace、关于三个页签；未读取原用户数据或凭据，未操作工作区切换、服务重启或业务数据。
- 核对旧 `SettingsPage.tsx`、workspace API、主进程协调器和 workspace-settings；新仓库核对 sidecar、平台路径、窗口关闭、动效与脱敏基础。
- 内置 imagegen 生成三张同一视觉方案的设置原型，保存 `docs/prototype/settings/`。保留旧模块能力，采用顶部全局导航和横向页签，无设置侧栏。
- 记录事实基线、交互状态、动效与生成提示词；交叉评审后修正重复导航、旧数据目录示例、遗漏的只读技术信息，以及不可写导出位置的恢复动作。
- 本轮未修改业务代码，没有开始正式规格或实施计划，也未操作其他任务的分支或提交。

## 待确认事项

- 新增建议：90/100/110/125% 界面缩放，跟随系统/减少/完整动效，本地诊断包预览和导出。
- 工作区选择目标后二次确认、覆盖当前执行器的占用预检属于新交互/实现目标，尚未实现。
- 产品“工作区”指业务数据根，与后端 `AppPaths.workspace` 子目录不同；不搬迁目录，不兼容旧数据。
- 保留新应用当前平台退出语义：macOS 关闭窗口保留进程，Windows 关闭最后一个窗口退出。

## 验证与边界

- 三张 PNG 的文件签名、像素尺寸、原始生成文件一致性和文档链接已检查；人工核对了视觉系统、模块范围和关键状态文案。
- 旧应用的隔离取证进程组已结束，没有遗留本轮启动的桌面应用或子进程。
- 源码核对时 HEAD 为 `b45ca843594a5fa2d3879dfdf83905fae3e8245c`，工作树存在其他任务的修改；本次新增资产独立，不回退这些修改。
- 图片是设计参考，不代表交互可运行或 Windows 测试通过。动态状态、键盘行为、路径适配与诊断脱敏需要在实现阶段验证。
- 下一步：用户确认原型范围与布局后，再准备正式设计规格、桌面 IPC/后端契约和实施计划；业务开发仍遵循独立分支及组件先行。

来源：[设置原型索引](../../docs/prototype/settings/README.md)、[能力基线](../../docs/prototype/settings/settings-capability-baseline.md)、[交互规则](../../docs/prototype/settings/settings-interactions.md)。
