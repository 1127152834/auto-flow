# F1/F2 图像资源读取与上传响应

2026-09-14，confirmed。图像资源列表、图片路径选择器、大图预览复用 ImageAssetPreview。服务图片通过 studioFetch 读取 Blob，不将受鉴权服务地址直接作为 img.src；组件负责加载、错误重试、来源切换取消、迟到响应保护及 object URL 释放。现有 data:image 资源仍直接展示。Mock thumbnail/file 路由返回实际图片字节，缺失404、内容格式不支持422。

真实 UI 验收发现并修复上传响应包装不一致：冻结 WebRPA image_assets.py:157 和三个消费者均要求 {asset: ...}，Mock 原先返回裸资源，导致已写入却报失败。协议测试改为断言原合同包装，保留字节完整性断言；新增实际上传组件测试。

新增12用例：9图片读取/取消/失败/重试组件，2内存及HTTP二进制端点，1文件选择后列表接入。相关30通过；最终全量123文件/1417用例，类型/lint/构建、17脚本检查与OpenAPI一致性通过。证据 evidence/f1-image-resources。全量结果不包含随后新建的资源命令校验测试。

浏览器上传→缩略图→大图预览通过，具体步骤与范围见 browser-ui.md。资源重命名/目录错误分支仍有差异，下一批继续；完整 F1 schema、资源分页与正式 Electron 尚未验收。
