import assert from 'node:assert/strict'
import { mkdtemp, mkdir, rm, writeFile, realpath } from 'node:fs/promises'
import { tmpdir, release } from 'node:os'
import { execFileSync } from 'node:child_process'
import { join, resolve } from 'node:path'
import { createServer } from 'vite'
import react from '@vitejs/plugin-react'
import tailwind from '@tailwindcss/vite'
import { _electron, expect } from '@playwright/test'

const root = resolve(import.meta.dirname, '..')
const evidence = join(root, 'docs/design-system/verification/final-controls')
await mkdir(evidence, { recursive: true })
const userData = await realpath(await mkdtemp(join(tmpdir(), 'autoflow-ui-g0-')))
let electron
let server
const checks = []
const errors = []
const zoomSamples = []
try {
  server = await createServer({
    root: join(root, 'apps/desktop/src/renderer'), configFile: false,
    cacheDir: join(userData, 'node_modules/.vite'),
    optimizeDeps: { include: ['@radix-ui/react-radio-group'] },
    plugins: [react(), tailwind()], server: { host: '127.0.0.1', port: 0 },
  })
  await server.listen()
  const address = server.httpServer.address()
  assert.ok(address && typeof address !== 'string')
  electron = await _electron.launch({
    args: [join(root, 'apps/desktop'), `--user-data-dir=${userData}`], cwd: root,
    env: { ...process.env, ELECTRON_RENDERER_URL: `http://127.0.0.1:${address.port}/#/__ui` },
  })
  const actualDirectory = await electron.evaluate(({ app }) => app.getPath('userData'))
  assert.equal(await realpath(actualDirectory), userData)
  const runtime = await electron.evaluate(() => ({ electron: process.versions.electron, chromium: process.versions.chrome }))
  const page = await electron.firstWindow()
  const settle = async () => page.evaluate(async () => {
    await new Promise(requestAnimationFrame)
    await Promise.all(document.getAnimations().filter(animation => animation.effect?.getTiming().iterations !== Infinity).map(animation => animation.finished.catch(() => {})))
    await new Promise(requestAnimationFrame)
  })
  const capture = async name => {
    await settle()
    const png = await electron.evaluate(async ({ BrowserWindow }) => (await BrowserWindow.getAllWindows()[0].webContents.capturePage()).toPNG().toString('base64'))
    await writeFile(join(evidence, name), Buffer.from(png, 'base64'))
  }
  page.setDefaultTimeout(8000)
  page.on('pageerror', error => errors.push(error.stack ?? error.message))
  await page.evaluate(() => {
    globalThis.__overlayTrace = []
    const record = type => {
      globalThis.__overlayTrace.push({ type, time: performance.now(), scroll: scrollY, layers: [...document.querySelectorAll('[data-overlay-depth]')].map(el => [el.getAttribute('data-overlay-depth'), el.getAttribute('data-state')]), popup: [...document.querySelectorAll('[data-af-popup]')].map(el => [el.closest('[data-overlay-depth]')?.getAttribute('data-overlay-depth'), el.hasAttribute('data-exiting')]) })
      globalThis.__overlayTrace = globalThis.__overlayTrace.slice(-24)
    }
    document.addEventListener('keydown', event => record(event.key), true)
    document.addEventListener('scroll', event => record('scroll:' + event.target.nodeName), true)
  })
  await expect(page.getByRole('heading', { name: 'AutoFlow 控件实验室' })).toBeVisible()
  const swatches = await page.locator('[data-token-swatch]').evaluateAll(elements => elements.map(el => ({ name: el.getAttribute('data-token-swatch'), token: getComputedStyle(el).getPropertyValue(`--color-${el.getAttribute('data-token-swatch')}`).trim(), background: getComputedStyle(el).backgroundColor })))
  for (const swatch of swatches) {
    assert.ok(swatch.token, `Missing runtime token: ${swatch.name}`)
    assert.notEqual(swatch.background, 'rgba(0, 0, 0, 0)', `Transparent swatch: ${swatch.name}`)
  }
  const readOnlyBackground = await page.getByLabel('只读输入').evaluate(el => getComputedStyle(el).backgroundColor)
  const disabledBackground = await page.getByLabel('禁用输入').evaluate(el => getComputedStyle(el).backgroundColor)
  assert.notEqual(disabledBackground, readOnlyBackground, 'Disabled and read-only surfaces must remain distinct')
  const densityInput = page.getByLabel('常规输入')
  assert.equal((await densityInput.boundingBox()).height, 40)
  await page.getByRole('button', { name: '默认 40px' }).click()
  assert.equal((await densityInput.boundingBox()).height, 32)
  await page.getByRole('button', { name: '紧凑 32px' }).click()
  assert.equal((await densityInput.boundingBox()).height, 40)
  await capture('01-lab.png')
  checks.push('isolated userData, development lab entry, runtime colors, read-only/disabled surfaces and 32/40 density')

  await page.getByRole('tab', { name: '验证基础' }).click()
  await page.getByRole('button', { name: '验证保存' }).click()
  const formChoice = page.getByRole('combobox', { name: '验证内核', exact: true })
  await expect(formChoice).toBeFocused()
  await expect(formChoice).toHaveAttribute('aria-invalid', 'true')
  await formChoice.fill('#0249')
  await page.getByRole('option', { name: 'CloakBrowser 146 · #0249' }).click()
  await expect(page.getByText('表单有修改', { exact: true })).toBeVisible()
  await page.getByRole('button', { name: '定位验证内核' }).click()
  await expect(formChoice).toBeFocused()
  await page.getByRole('button', { name: '验证保存' }).click()
  await expect(page.getByText('已保存验证值：kernel-249')).toBeVisible()
  await page.getByRole('button', { name: '重置表单' }).click()
  await expect(formChoice).toHaveValue('')
  await expect(page.getByText('表单未修改', { exact: true })).toBeVisible()
  checks.push('RHF error focus/setFocus, 500-item search/select, dirty, submit and reset')

  await page.getByRole('button', { name: '打开嵌套验证' }).click()
  const nestedSelect = page.getByRole('combobox', { name: '弹窗内通道' })
  await nestedSelect.click()
  assert.equal(await page.locator('[data-af-popup]').evaluate(el => el.closest('[data-overlay-depth]')?.getAttribute('data-overlay-depth')), '0')
  await page.keyboard.press('Escape')
  await expect(page.getByRole('listbox')).toHaveCount(0)
  await expect(nestedSelect).toBeFocused()
  await page.getByRole('button', { name: '管理内核 · 验证' }).click()
  const inner = page.getByRole('dialog', { name: '内核管理 · 验证' })
  const choice = page.getByRole('combobox', { name: '内层内核', exact: true })
  await page.getByRole('button', { name: '展开 内层内核', exact: true }).click()
  await expect(page.getByRole('listbox')).toBeVisible()
  assert.equal(await page.getByRole('listbox').evaluate(el => el.closest('[role="dialog"]')?.getAttribute('data-overlay-depth')), '1')
  const popupBox = await page.getByRole('listbox').boundingBox()
  assert.ok(popupBox && popupBox.height > 0)
  await expect(page.locator('[data-af-popup]')).toBeInViewport({ ratio: 1 })
  assert.ok(await page.getByRole('option').count() < 50, '500-item list should render a bounded visible subset')
  await page.keyboard.press('End')
  const longVirtualOption = page.getByRole('option', { name: /^长名称验收/ })
  await expect(longVirtualOption).toBeVisible()
  await expect(longVirtualOption).toHaveAttribute('aria-posinset', '501')
  await expect(longVirtualOption).toHaveAttribute('aria-setsize', '501')
  await page.keyboard.press('Home')
  await expect(page.getByRole('option', { name: 'CloakBrowser 146 · #0001' })).toBeVisible()
  await capture('02-nested-choice.png')
  await page.keyboard.press('Escape')
  await expect(page.getByRole('listbox')).toHaveCount(0)
  await expect(inner).toHaveAttribute('data-state', 'open')
  await expect(choice).toBeFocused()
  for (const key of [...Array(9).fill('Tab'), ...Array(9).fill('Shift+Tab')]) {
    await page.keyboard.press(key)
    assert.equal(await page.evaluate(() => document.activeElement?.closest('[data-overlay-depth]')?.getAttribute('data-overlay-depth')), '1')
  }
  checks.push('nested popup and Tab/Shift+Tab stay inside focus scope; first Escape only closes popup')

  await page.getByRole('button', { name: '模拟忙状态' }).click()
  await page.keyboard.press('Escape')
  await expect(inner).toBeVisible()
  await page.getByRole('button', { name: '结束忙状态' }).click()
  await page.getByRole('button', { name: '打开删除确认' }).click()
  await expect(page.getByRole('button', { name: '取消删除' })).toBeFocused()
  assert.equal(await page.getByRole('alertdialog').getAttribute('data-overlay-depth'), '2')
  await capture('03-third-layer.png')
  await page.keyboard.press('Escape')
  await expect(page.getByRole('button', { name: '打开删除确认' })).toBeFocused()
  await page.keyboard.press('Escape')
  await expect(page.getByRole('button', { name: '管理内核 · 验证' })).toBeFocused()
  await page.keyboard.press('Escape')
  await expect(page.getByRole('button', { name: '打开嵌套验证' })).toBeFocused()
  checks.push('busy guard, third-layer cancel focus and sequential return focus')

  for (const zoom of [1, 1.25, 1.5, 2, 1, 1.25, 1.5, 2]) {
    console.log(`Checking zoom ${zoom}`)
    await electron.evaluate(({ BrowserWindow }, factor) => {
      const window = BrowserWindow.getAllWindows()[0]
      window.setSize(1280, 800)
      window.webContents.setZoomFactor(factor)
    }, zoom)
    await page.getByRole('button', { name: '打开嵌套验证' }).click()
    await page.getByRole('button', { name: '管理内核 · 验证' }).click()
    await expect(page.getByRole('button', { name: '关闭内核验证' })).toBeInViewport({ ratio: 1 })
    // Wait for the dialog's real entrance animation before the first pointer hit.
    await expect.poll(() => inner.evaluate(el => el.getAnimations().filter(animation => animation.playState === 'running').length)).toBe(0)
    const toggle = page.getByRole('button', { name: '展开 内层内核', exact: true })
    await toggle.scrollIntoViewIfNeeded()
    await settle()
    await toggle.click()
    await expect(page.getByRole('listbox')).toBeVisible()
    assert.equal(await page.locator('[data-af-popup]').evaluate(el => el.closest('[data-overlay-depth]')?.getAttribute('data-overlay-depth')), '1')
    await page.keyboard.press('Escape')
    await expect(inner).toHaveAttribute('data-state', 'open')
    await expect(page.getByRole('listbox')).toHaveCount(0)
    await expect(choice).toBeFocused()
    await page.keyboard.press('ArrowDown')
    await expect(page.locator('[data-af-popup]')).toBeInViewport({ ratio: 1 })
    await page.keyboard.press('Escape')
    await expect(inner).toHaveAttribute('data-state', 'open')
    zoomSamples.push(await page.evaluate(factor => ({ factor, viewport: [innerWidth, innerHeight], devicePixelRatio }), zoom))
    if (zoom === 2) await capture('04-zoom-200.png')
    const sample = page.getByLabel('滚动样本', { exact: true })
    await sample.focus()
    await page.keyboard.press('End')
    await expect.poll(() => sample.evaluate(el => el.scrollTop)).toBeGreaterThan(0)
    await page.keyboard.press('Escape')
    await expect(page.getByRole('button', { name: '管理内核 · 验证' })).toBeFocused()
    await page.keyboard.press('Escape')
    await expect(page.getByRole('button', { name: '打开嵌套验证' })).toBeFocused()
  }
  checks.push('1280x800 at 100/125/150/200%; first mouse and keyboard popup opening, visible actions and keyboard scrolling')
  await electron.evaluate(({ BrowserWindow }) => BrowserWindow.getAllWindows()[0].webContents.setZoomFactor(1))
  await page.emulateMedia({ reducedMotion: 'reduce' })
  await page.getByRole('button', { name: '打开嵌套验证' }).click()
  const animation = await page.getByRole('dialog', { name: '浏览器配置 · 验证' }).evaluate(el => getComputedStyle(el).animationDuration)
  assert.ok(parseFloat(animation) < .001)
  assert.deepEqual(errors, [])
  checks.push('reduced-motion and no renderer exceptions')
  await page.keyboard.press('Escape')
  await expect(page.getByRole('button', { name: '打开嵌套验证' })).toBeFocused()
  await page.emulateMedia({ reducedMotion: 'no-preference' })
  const textSection = page.getByRole('region', { name: '按钮与文字控件' })
  const sampleSave = page.getByRole('button', { name: '保存样本', exact: true })
  await sampleSave.scrollIntoViewIfNeeded()
  const idleWidth = (await sampleSave.boundingBox()).width
  await sampleSave.click()
  await expect(sampleSave).toBeDisabled()
  assert.equal((await sampleSave.boundingBox()).width, idleWidth)
  await expect(sampleSave).toHaveText('保存样本正在保存…')
  await page.emulateMedia({ reducedMotion: 'reduce' })
  assert.equal(await sampleSave.locator('.af-spinner').evaluate(el => getComputedStyle(el).animationName), 'none')
  await page.getByRole('button', { name: '结束加载样本' }).click()
  const searchSample = page.getByRole('searchbox', { name: '搜索样本', exact: true })
  await page.getByRole('button', { name: '清除搜索', exact: true }).click()
  await expect(searchSample).toHaveValue(''); await expect(searchSample).toBeFocused()
  const passwordSample = page.getByLabel('可显隐密码', { exact: true })
  await passwordSample.focus()
  await passwordSample.evaluate(el => el.setSelectionRange(0, 6))
  await page.getByRole('button', { name: '显示密码', exact: true }).click()
  await expect(passwordSample).toHaveAttribute('type', 'text')
  await expect(passwordSample).toHaveValue('sample-only-key')
  await expect(passwordSample).toBeFocused()
  await page.getByRole('button', { name: '隐藏密码', exact: true }).click()
  await expect(passwordSample).toHaveAttribute('type', 'password')
  const normalText = page.getByRole('textbox', { name: '常规文字', exact: true })
  assert.equal(await normalText.evaluate(el => getComputedStyle(el).fontSize), '14px')
  await page.getByRole('button', { name: '默认 40px' }).click()
  assert.equal(await normalText.evaluate(el => getComputedStyle(el).fontSize), '12px')
  assert.equal((await normalText.boundingBox()).height, 32)
  await page.getByRole('button', { name: '紧凑 32px' }).click()
  await textSection.evaluate(el => el.scrollIntoView({ block: 'start' }))
  await capture('05-text-controls.png')
  await searchSample.evaluate(el => el.scrollIntoView({ block: 'start' }))
  await capture('08-search-password.png')
  checks.push('T3 loading width/name, static reduced spinner, search clear, reveal and 32/40 typography')

  await page.getByRole('button', { name: '提交文字样本' }).click()
  const boundName = page.getByRole('textbox', { name: '绑定名称', exact: true })
  await expect(boundName).toBeFocused()
  await expect(boundName).toHaveAttribute('aria-invalid', 'true')
  await boundName.fill('中文样本 / English')
  await expect(boundName).not.toHaveAttribute('aria-invalid', 'true')
  await page.getByRole('button', { name: '提交文字样本' }).click()
  await expect(page.getByText('文字样本已保存：中文样本 / English')).toBeVisible()
  await page.getByRole('button', { name: '重置文字样本' }).click()
  await expect(boundName).toHaveValue('')
  await electron.evaluate(({ BrowserWindow }) => BrowserWindow.getAllWindows()[0].webContents.setZoomFactor(2))
  await page.getByRole('button', { name: '聚焦绑定名称' }).click()
  await expect(boundName).toBeFocused()
  await expect(boundName).toBeInViewport({ ratio: 1 })
  await capture('06-text-form-200.png')
  checks.push('T3 RHF error/ref/reset, Unicode fill and 200% focused field visibility; IME not claimed')

  await electron.evaluate(({ BrowserWindow }) => BrowserWindow.getAllWindows()[0].webContents.setZoomFactor(1))
  const toggles = page.getByRole('region', { name: '勾选、单选与折叠' })
  const mixed = toggles.getByRole('checkbox', { name: '半选样本', exact: true })
  await mixed.scrollIntoViewIfNeeded()
  await expect(mixed).toHaveAttribute('aria-checked', 'mixed')
  await expect(mixed.locator('[data-af-choice-glyph="mixed"]')).toBeVisible()
  await expect(mixed.locator('[data-af-choice-glyph="checked"]')).not.toBeVisible()
  assert.equal((await mixed.boundingBox()).height, 32)
  assert.equal((await mixed.locator('[data-af-choice-mark]').boundingBox()).width, 18)
  await mixed.focus()
  await page.keyboard.press('Shift+Tab')
  await page.keyboard.press('Tab')
  await expect(mixed).toBeFocused()
  await expect.poll(() => mixed.evaluate(el => getComputedStyle(el).outlineWidth)).toBe('2px')
  await page.keyboard.press('Space')
  await expect(mixed).toBeChecked()
  await expect(mixed.locator('[data-af-choice-glyph="checked"]')).toBeVisible()
  await expect(mixed.locator('[data-af-choice-glyph="mixed"]')).not.toBeVisible()
  await page.keyboard.press('Space')
  await expect(mixed).not.toBeChecked()
  await expect(toggles.getByRole('checkbox', { name: '禁用已选样本', exact: true })).toBeDisabled()
  const shanghai = toggles.getByRole('radio', { name: '上海样本', exact: true })
  const hangzhou = toggles.getByRole('radio', { name: '杭州样本', exact: true })
  await shanghai.click()
  // Radix schedules focus after keydown; release after that task, as with a physical key.
  await page.keyboard.down('ArrowDown')
  await expect(hangzhou).toBeFocused()
  await expect(hangzhou).toBeChecked()
  await page.keyboard.up('ArrowDown')
  await page.keyboard.down('ArrowUp')
  await expect(shanghai).toBeChecked()
  await page.keyboard.up('ArrowUp')
  const openSwitch = toggles.getByRole('switch', { name: '关闭样本', exact: true })
  await openSwitch.focus()
  await page.keyboard.press('Space')
  await expect(openSwitch).toBeChecked()
  await expect(toggles.getByRole('switch', { name: '禁用开启样本', exact: true })).toBeDisabled()
  const summary = toggles.locator('summary').filter({ hasText: '填写说明样本' })
  await summary.focus()
  await page.keyboard.press('Enter')
  await expect(toggles.getByRole('button', { name: '说明内操作样本' })).toBeVisible()
  await page.keyboard.press('Tab')
  await expect(toggles.getByRole('button', { name: '说明内操作样本' })).toBeFocused()
  await page.keyboard.press('Shift+Tab')
  await page.keyboard.press('Space')
  await expect(toggles.getByRole('button', { name: '说明内操作样本' })).not.toBeVisible()
  await page.keyboard.press('Tab')
  await expect(toggles.locator('summary').filter({ hasText: '受控折叠样本' })).toBeFocused()
  await page.keyboard.press('Enter')
  await expect(toggles.getByText('展开状态与调用方同步，可由外部重置。')).toBeVisible()
  await toggles.getByRole('button', { name: '收起受控说明' }).click()
  await expect(toggles.getByText('展开状态与调用方同步，可由外部重置。')).not.toBeVisible()
  checks.push('T4 tri-state visible glyphs, 18px mark/32px target, focus ring, disabled controls, Radio arrows and native Disclosure Enter/Space/Tab')

  await toggles.getByRole('button', { name: '提交开关样本' }).click()
  const accepted = toggles.getByRole('checkbox', { name: '确认本地样本', exact: true })
  await expect(accepted).toBeFocused()
  await expect(accepted).toHaveAttribute('aria-invalid', 'true')
  await expect.poll(() => accepted.locator('[data-af-choice-mark]').evaluate(el => getComputedStyle(el).borderTopColor)).toBe('rgb(177, 60, 50)')
  await page.keyboard.press('Space')
  await toggles.getByRole('switch', { name: '启用样本', exact: true }).click()
  await toggles.getByRole('button', { name: '提交开关样本' }).click()
  await expect(toggles.getByText('开关样本已保存：已确认 / 已启用', { exact: true })).toBeVisible()
  await toggles.getByRole('button', { name: '重置开关样本' }).click()
  await expect(accepted).not.toBeChecked()
  await expect(toggles.getByRole('switch', { name: '启用样本', exact: true })).not.toBeChecked()
  const errorRadio = toggles.getByRole('radio', { name: '错误组候选一', exact: true })
  await errorRadio.click()
  await settle()
  await expect.poll(() => errorRadio.locator('[data-af-choice-mark]').evaluate(el => getComputedStyle(el).borderTopColor)).toBe('rgb(177, 60, 50)')
  await page.emulateMedia({ reducedMotion: 'no-preference' })
  await toggles.evaluate(el => el.scrollIntoView({ block: 'start' }))
  await capture('09-toggle-controls.png')
  await page.emulateMedia({ reducedMotion: 'reduce' })
  assert.ok(parseFloat(await openSwitch.locator('[data-af-choice-thumb]').evaluate(el => getComputedStyle(el).transitionDuration)) < .001)
  await page.emulateMedia({ forcedColors: 'active' })
  const markColors = await shanghai.locator('[data-af-choice-mark]').evaluate(el => ({ bg: getComputedStyle(el).backgroundColor, fg: getComputedStyle(el).color }))
  assert.notEqual(markColors.bg, markColors.fg)
  await expect.poll(() => openSwitch.locator('[data-af-choice-thumb]').evaluate(el => getComputedStyle(el).backgroundColor)).toBe(markColors.fg)
  await capture('10-toggle-forced-colors.png')
  await page.emulateMedia({ forcedColors: 'none', reducedMotion: 'no-preference' })
  await electron.evaluate(({ BrowserWindow }) => BrowserWindow.getAllWindows()[0].webContents.setZoomFactor(2))
  const longRadio = toggles.getByRole('radio', { name: /^长标签样本/ })
  await longRadio.focus()
  await expect(longRadio).toBeInViewport({ ratio: 1 })
  assert.ok(await page.evaluate(() => document.documentElement.scrollWidth <= innerWidth))
  await capture('11-toggle-200.png')
  await electron.evaluate(({ BrowserWindow }) => BrowserWindow.getAllWindows()[0].webContents.setZoomFactor(1))
  checks.push('T4 RHF error focus/submit/reset, selected error border, reduced motion, emulated forced colors and 200% long label without horizontal overflow')

  // T5: application-facing choices and own overflow rails, after G0 at normal scale.
  const choiceSection = page.getByRole('region', { name: '选择与自由输入' })
  await choiceSection.scrollIntoViewIfNeeded()
  await settle()
  const select = page.getByRole('combobox', { name: '通道选择', exact: true })
  await select.click()
  await expect(page.locator('[data-af-popup]')).toBeInViewport({ ratio: 1 })
  await page.getByRole('option', { name: '跟随系统' }).click()
  await expect(select).toContainText('跟随系统')
  await expect(choiceSection.getByText(/^选择值：/)).toContainText('选择值：""')
  await select.focus()
  await page.keyboard.press('ArrowDown')
  await expect(page.getByRole('listbox')).toBeVisible()
  await page.keyboard.press('Escape')
  await expect(select).toBeFocused()
  const readOnlySelect = page.getByRole('combobox', { name: '只读通道' })
  await readOnlySelect.focus(); await page.keyboard.press('s')
  await expect(choiceSection.getByText(/^选择值：/)).toContainText('选择值：""')
  await expect(readOnlySelect).toHaveAttribute('aria-expanded', 'false')
  const strict = page.getByRole('combobox', { name: '搜索通道', exact: true })
  await strict.fill('__custom__')
  await expect(page.getByRole('option')).toHaveCount(1)
  await page.getByRole('option', { name: /标识 __custom__/ }).click()
  await expect(choiceSection.getByText(/^选择值：/)).toContainText('选择值："__custom__"')
  await strict.fill('不存在的查询')
  await page.keyboard.press('Escape')
  await expect(strict).toHaveValue('同名资源')
  const free = page.getByRole('combobox', { name: '自由输入样本', exact: true })
  await free.fill('稳定')
  await page.getByRole('option', { name: /公开版稳定通道/ }).click()
  await expect(free).toHaveValue('stable')
  await free.fill('自定义 UA / 值')
  await page.keyboard.press('Escape')
  await expect(free).toHaveValue('自定义 UA / 值')
  await page.getByRole('button', { name: '重试加载', exact: true }).click()
  await expect(choiceSection.getByRole('alert')).toHaveCount(0)
  await select.click()
  await page.getByRole('option', { name: /^长名称/ }).click()
  assert.ok(await page.evaluate(() => document.documentElement.scrollWidth <= innerWidth))
  await capture('10-choices.png')
  await electron.evaluate(({ BrowserWindow }) => BrowserWindow.getAllWindows()[0].webContents.setZoomFactor(2))
  await select.scrollIntoViewIfNeeded(); await select.focus(); await settle()
  assert.ok(await page.evaluate(() => document.documentElement.scrollWidth <= innerWidth))
  await capture('12-choice-zoom-200.png')
  await electron.evaluate(({ BrowserWindow }) => BrowserWindow.getAllWindows()[0].webContents.setZoomFactor(1))
  checks.push('T5 Select custom listbox/null versus empty, key-safe selection, strict draft restore, free input/value callback, retry and long-label layout')

  const viewport = page.getByLabel('双轴滚动验收', { exact: true })
  await viewport.scrollIntoViewIfNeeded()
  await settle()
  const rootArea = viewport.locator('..')
  const vertical = rootArea.locator('[data-orientation="vertical"]')
  const horizontal = rootArea.locator('[data-orientation="horizontal"]')
  await expect(vertical).toBeVisible(); await expect(horizontal).toBeVisible()
  assert.equal((await vertical.boundingBox()).width, 12)
  assert.equal((await vertical.locator('.af-scroll-thumb').boundingBox()).width, 6)
  await viewport.hover()
  await page.mouse.wheel(0, 220)
  await expect.poll(() => viewport.evaluate(el => el.scrollTop)).toBeGreaterThan(0)
  const oldLeft = await viewport.evaluate(el => el.scrollLeft)
  const thumb = await horizontal.locator('.af-scroll-thumb').boundingBox()
  await page.mouse.move(thumb.x + thumb.width / 2, thumb.y + thumb.height / 2)
  await page.mouse.down(); await page.mouse.move(thumb.x + thumb.width / 2 + 110, thumb.y + thumb.height / 2, { steps: 8 }); await page.mouse.up()
  await expect.poll(() => viewport.evaluate(el => el.scrollLeft)).toBeGreaterThan(oldLeft)
  await viewport.focus(); await page.keyboard.press('End')
  await expect.poll(() => viewport.evaluate(el => el.scrollTop)).toBeGreaterThan(500)
  await expect(page.getByLabel('无溢出滚动验收').locator('..').locator('.af-scrollbar')).toHaveCount(0)
  await capture('11-scroll-area.png')
  checks.push('T5 ScrollArea 12/6px rails/thumbs, overflow-only visibility, wheel, horizontal thumb drag and keyboard End')

  // Measure event-to-next-paint latency; omit Playwright IPC and locator waiting time.
  await page.getByRole('button', { name: '重置表单' }).click()
  const profiler = process.env.UI_CHOICE_PROFILE ? await page.context().newCDPSession(page) : null
  if (profiler) { await profiler.send('Profiler.enable'); await profiler.send('Profiler.start') }
  const openTimes = [], filterTimes = []
  const armTiming = async (eventType, readyCount) => page.evaluate(({ eventType, readyCount }) => {
    globalThis.__choiceTiming = null
    document.addEventListener(eventType, () => {
      const start = performance.now()
      const observer = new MutationObserver(() => {
        const count = document.querySelectorAll('[role="listbox"] [role="option"]').length
        if (readyCount === 'visible' ? count === 0 : count !== readyCount) return
        observer.disconnect()
        requestAnimationFrame(() => { globalThis.__choiceTiming = performance.now() - start })
      })
      observer.observe(document.body, { childList: true, subtree: true })
    }, { once: true, capture: true })
  }, { eventType, readyCount })
  for (let i = 0; i < 20; i++) {
    const toggle = page.getByRole('button', { name: '展开 验证内核', exact: true })
    await toggle.scrollIntoViewIfNeeded(); await settle()
    await armTiming('pointerdown', 'visible')
    await toggle.click()
    await expect.poll(() => page.evaluate(() => globalThis.__choiceTiming)).not.toBeNull()
    openTimes.push(await page.evaluate(() => globalThis.__choiceTiming))
    await armTiming('input', 1)
    await formChoice.fill(`#${String(249 + i).padStart(4, '0')}`)
    await expect.poll(() => page.evaluate(() => globalThis.__choiceTiming)).not.toBeNull()
    filterTimes.push(await page.evaluate(() => globalThis.__choiceTiming))
    await page.keyboard.press('Escape')
  }
  const p95 = values => [...values].sort((a, b) => a - b)[Math.ceil(values.length * .95) - 1]
  const performanceSamples = { methodology: 'capturing pointerdown/input to requestAnimationFrame after first visible options (open) or one filtered option (filter); local DEV Electron, 20 samples (first open included), no IPC waiting included', openTimes, filterTimes, openP95: p95(openTimes), filterP95: p95(filterTimes), targets: { open: 200, filter: 100 } }
  await writeFile(join(evidence, 'choice-performance.json'), JSON.stringify(performanceSamples, null, 2) + '\n')
  if (profiler) {
    const { profile } = await profiler.send('Profiler.stop')
    const hits = new Map()
    for (const node of profile.nodes) { const key = `${node.callFrame.functionName} @ ${node.callFrame.url.split('?')[0].split('/').slice(-2).join('/')}`; hits.set(key, (hits.get(key) ?? 0) + (node.hitCount ?? 0)) }
    await writeFile(join(evidence, 'choice-profile-before.json'), JSON.stringify({ durationMs: (profile.endTime - profile.startTime) / 1000, sampleCount: profile.samples.length, topSelfSamples: [...hits].sort((a, b) => b[1] - a[1]).slice(0, 35), performanceSamples }, null, 2))
    await profiler.detach()
  }
  if (!profiler) assert.ok(performanceSamples.openP95 <= 200, `500-option open p95 ${performanceSamples.openP95}ms > 200ms`)
  if (!profiler) assert.ok(performanceSamples.filterP95 <= 100, `500-option filter p95 ${performanceSamples.filterP95}ms > 100ms`)
  checks.push(profiler ? 'T5 profiling only; performance thresholds not enforced' : 'T5 20-sample 500-option event-to-render performance within open 200ms / filter 100ms targets')

  await page.getByRole('button', { name: '打开抽屉验收' }).click()
  const drawer = page.getByRole('dialog', { name: '抽屉验收' })
  await expect(drawer).toHaveAttribute('data-placement', 'drawer')
  await page.getByRole('button', { name: '抽屉中的操作' }).click()
  assert.equal(await page.getByRole('menu').evaluate(el => el.closest('[data-overlay-depth]')?.getAttribute('data-overlay-depth')), '0')
  await page.getByRole('menuitem', { name: '编辑详情' }).click()
  await expect(page.getByRole('dialog', { name: '菜单打开的弹窗' })).toBeVisible()
  await page.getByRole('button', { name: '返回抽屉' }).click()
  await expect(drawer).toBeVisible()
  await page.keyboard.press('Escape')
  await expect(page.getByRole('button', { name: '打开抽屉验收' })).toBeFocused()
  await page.getByRole('button', { name: '字段帮助' }).focus()
  await expect(page.getByRole('tooltip')).toBeVisible()
  await page.keyboard.press('Escape')
  await expect(page.getByRole('tooltip')).toHaveCount(0)
  await page.getByRole('button', { name: '显示成功通知' }).click()
  await expect(page.getByText('组件验收操作成功')).toBeVisible()
  await page.getByRole('button', { name: '关闭通知' }).click()
  checks.push('G1 Drawer/menu-to-modal ownership, tooltip focus/Escape and feedback notification dismissal')
  await page.goto(`http://127.0.0.1:${address.port}/#/profiles`)
  // The DEV lab is chosen at module bootstrap, so a hash change alone cannot leave it.
  await page.reload()
  await expect(page.getByText('本地服务正常', { exact: true })).toBeVisible({ timeout: 15000 })
  await page.getByRole('button', { name: '新建配置', exact: true }).click()
  const realDialog = page.getByRole('dialog', { name: '新建浏览器配置', exact: true })
  const realName = realDialog.getByRole('textbox', { name: '名称', exact: true })
  await expect(realName).toBeVisible()
  assert.equal(await realName.evaluate(el => getComputedStyle(el).borderTopColor), 'rgb(139, 131, 119)')
  await realName.focus()
  await capture('07-real-browser-form.png')
  await realDialog.getByRole('tab', { name: '内核与代理', exact: true }).click()
  const realSwitch = realDialog.getByRole('switch', { name: '无头模式', exact: true })
  const originalSwitchState = await realSwitch.getAttribute('aria-checked')
  await realSwitch.focus()
  await page.keyboard.press('Space')
  await expect(realSwitch).toHaveAttribute('aria-checked', originalSwitchState === 'true' ? 'false' : 'true')
  assert.equal((await realSwitch.boundingBox()).height, 32)
  await capture('12-real-browser-switch.png')
  await page.keyboard.press('Space')
  await expect(realSwitch).toHaveAttribute('aria-checked', originalSwitchState)
  await realDialog.getByRole('button', { name: '取消', exact: true }).click()
  await expect(realDialog).toHaveCount(0)
  assert.deepEqual(errors, [])
  checks.push('real browser configuration form opens against isolated sidecar, shared input focus/style and cancel; no saved data')
  await page.goto(`http://127.0.0.1:${address.port}/#/settings`)
  await page.getByRole('tab', { name: '关于', exact: true }).click()
  await page.getByRole('button', { name: '导出诊断信息', exact: true }).click()
  const diagnostic = page.getByRole('dialog', { name: '导出诊断信息', exact: true })
  const realCheckbox = diagnostic.getByRole('checkbox', { name: '添加最近日志', exact: true })
  await expect(realCheckbox).toBeEnabled()
  await realCheckbox.click()
  await expect(realCheckbox).toBeChecked()
  await expect(realCheckbox).toBeEnabled()
  await expect(realCheckbox.locator('[data-af-choice-glyph="checked"]')).toBeVisible()
  await capture('13-real-settings-checkbox.png')
  await diagnostic.getByRole('button', { name: '取消', exact: true }).click()
  await expect(diagnostic).toHaveCount(0)
  assert.deepEqual(errors, [])
  checks.push('T4 real browser Switch toggles/restores; real settings diagnostic Checkbox updates preview and cancels without export')
  await writeFile(join(evidence, 'results.json'), JSON.stringify({ date: new Date().toISOString(), platform: process.platform, arch: process.arch, osVersion: process.platform === 'darwin' ? execFileSync('sw_vers', ['-productVersion'], { encoding: 'utf8' }).trim() : release(), runtime, codeBaseCommit: execFileSync('git', ['rev-parse', 'HEAD'], { cwd: root, encoding: 'utf8' }).trim(), sourceIncludesWorkingChanges: Boolean(execFileSync('git', ['status', '--porcelain', '--', 'apps/desktop/src', 'scripts/smoke-ui-controls.mjs', 'apps/desktop/package.json', 'package.json', 'package-lock.json'], { cwd: root, encoding: 'utf8' }).trim()), zoomSamples, checks, errors }, null, 2) + '\n')
  console.log(JSON.stringify({ passed: checks.length, platform: process.platform, checks }, null, 2))
} catch (error) {
  if (electron) {
    const page = await electron.firstWindow()
    console.log('OVERLAY_TRACE', JSON.stringify(await page.evaluate(() => globalThis.__overlayTrace)))
    await page.screenshot({ path: join(evidence, 'failure.png') }).catch(() => {})
    await writeFile(join(evidence, 'failure-aria.txt'), await page.locator('body').ariaSnapshot()).catch(() => {})
    console.log(JSON.stringify(await page.evaluate(() => ({ viewport: [innerWidth, innerHeight], visual: [visualViewport?.width, visualViewport?.height, visualViewport?.offsetTop], scroll: [scrollX, scrollY], bodyStyle: document.body.style.cssText, dialogs: [...document.querySelectorAll('[role=dialog]')].map(el => ({ rect: el.getBoundingClientRect().toJSON(), state: el.getAttribute('data-state') })), choices: [...document.querySelectorAll('[role=combobox]')].map(el => ({ expanded: el.getAttribute('aria-expanded'), rect: el.getBoundingClientRect().toJSON() })) })), null, 2))
  }
  throw error
} finally {
  await electron?.close()
  await server?.close()
  await rm(userData, { recursive: true, force: true })
}
