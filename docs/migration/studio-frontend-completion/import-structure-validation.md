# F2 导入结构保护

2026-09-14，confirmed。本批同时保护 importWorkflow 与 mergeWorkflow。重复节点/连线 ID、空 ID、悬空连线及错误 nodes/edges/variables 容器类型在修改文档之前被拒绝；保持原文档 ID、节点、变量、历史和脏状态。

合法但参数未完成、未知旧节点类型保持可导入。缺少连线 ID 继续自动生成；缺坐标沿用 sanitizer，合并路径也先处理坐标，避免与打开路径行为不一致。未批量改写已保存文档。该校验仅覆盖图身份与容器结构，不宣称完成全部变量/节点配置 schema 验证。

20 个新增用例，相关 320 项通过。全量 114 文件/1325 用例通过；类型、lint、构建、17 个脚本检查和 OpenAPI 一致性通过。证据 evidence/f2-import-structure。全量统计启动时尚未包含随后新增的网页字段分支测试；后者发现的问题另批修复。

边界：Store 与组件/协议回归，不是正式 Electron 文件导入端到端。本批拒绝时仍沿用现有 boolean/通用错误提示；细粒度字段错误合同仍需补全。
