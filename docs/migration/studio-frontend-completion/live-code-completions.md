# 代码编辑器实时变量补全

2026-09-14，F2 配套工具有界修复。

JavaScript 与页面注入编辑器曾在 onMount 捕获变量名和节点输出；打开期间文档发生修改后，提示仍停留在旧状态。现在每次 provideCompletionItems 从现有 Store 读取当前文档，保持原有字段范围和 Monaco 模型隔离/卸载清理，不重建编辑器、不新增状态库。Python 仅有固定代码片段，本项不适用，原生命周期回归保留。

新增两项测试先失败（找不到改名后的 vars.after），修复后通过；覆盖新增节点输出、变量改名、删除变量/输出，以及不重复注册提供器。67 项补全与浏览器脚本相关测试、19 项复制/脚本编辑回归通过，补齐测试变量 scope 后最终补全8项、TypeScript、ESLint和构建通过。

证据见 evidence/f2-live-completions；这些是组件/服务测试，没有把模拟 Monaco 视为实际浏览器端到端。先前257文件2922项全量发生在本修复之前，本批仅作有针对性的回归。284节点的全部变量字段识别及作用域可用性仍需继续核对，不因提示刷新修复而标为完成。
