# 配套组件依赖索引修正

日期：2026-09-13；状态：扫描器已验证，人工逐项功能验收未完成。

原扫描按全局同名声明搜索，不能正确处理import别名、barrel导出、命名空间属性、控件别名和lazy import。现按调用文件和实际导入/导出链解析；记录第三方控件，追踪lazy代码编辑器到实际实现与Monaco。循环解析使用路径集合终止，不能串入其他同名组件。

4条脚本测试验证284范围、Select别名、Oracle导出闭包与不串PostgreSQL、懒加载代码编辑器及未解析动态图标。完整脚本套件16条通过。最后一项未静态解析的是custom-dialogs.tsx#Icon，源码按提示类型选择lucide图标；保留人工复核项，不擅自消除。

所有节点状态仍待核对。1,988条是模板，不能据扫描器测试改为通过；真实字段分支和工具行为继续单独验收。
