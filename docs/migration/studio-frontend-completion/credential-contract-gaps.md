# 凭据合同剩余差异

2026-09-14 历史核对记录。2026-09-15 凭据改名、字段批量改名/删除、修订和幂等命令的前端/Mock缺口已经完成；以 `credential-protocol-contract.md` 最后一节和 `credential-settings-protection.md` 最新节为准。下表保留发现来源，不得再将已完成的前端字段能力列为当前缺口；真实秘密存储和原子持久化仍是后端交接。

来源：冻结reference/WebRPA/backend/app/api/credentials.py、services/credential_manager.py、executors/base.py；当前AutoFlow api/mock-settings.ts与CredentialSettings.tsx。

| 能力 | 已观察行为 | 后续必须完成 |
|---|---|---|
| 编辑已有名称 | 原UI将编辑后的名称传给upsert；旧服务按该名称查找/写入，因此不保证移动原记录，也不更新工作流引用 | 明确独立改名与内容修改的合同及失败事务边界；不能把两次请求伪装成原子修改 |
| 删除/改名已有字段 | 旧upsert从existing.fields复制再合并；省略字段不删除，空值保留旧值。原UI移除字段行不能保证服务删除该字段；Mock现已对齐部分合并 | 显式字段移除/改名语义；保留旧值不要求前端读取明文；Mock同步遵守 |
| 描述清空 | 旧服务和当前Mock均为空描述保留已有描述 | 统一描述清空规则与回显用例 |
| 写入失败 | 旧credential_manager._save捕获异常后只输出错误，调用者可能继续返回成功 | 真实后端必须原子持久化并传播失败；前端现有失败保护不能补救服务伪成功 |
| 可引用名称 | 执行器凭据引用按第一个点拆分名称/字段；存储API却只校验名称非空 | 定义可引用名称与字段的语法，保留旧数据并给出可操作提示；不自动重命名 |
| HTTP方法和特殊名称 | 本批已完成方法分派及无斜杠特殊名称；含斜杠路径仍未完整验收 | 用内存及实际HTTP测试冻结方法、路径编码及名称边界 |
| 类型与修订 | 本批读/写/删除/改名已使用生成DTO；仍无并发修订和幂等修改合同 | 冻结所有读/写/删除/改名DTO及错误包；稳定命令标识、并发冲突和迟到响应分别验收 |
| 配置离开 | 已补凭据手动切标签、关闭、遮罩、完成保存的保存/放弃/取消保护 | AI 关闭入口已接入并等待结果；宿主退出/换区与外部强制隐藏仍未完整协调 |

已完成部分见credential-settings-protection.md（提交时更新）。服务仍为Mock，仅保存名称和打码字段元数据，不保存真实凭据值，不代表加密存储或真实运行注入已实现。
