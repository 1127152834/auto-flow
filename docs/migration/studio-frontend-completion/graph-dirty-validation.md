# F2 图编辑未保存状态与离开保护

2026-09-14。添加节点、删除节点、添加连线统一标记未保存；重复连线、自连和删除不存在节点不创建编辑历史或脏状态。沿用既有连线规则，不增加图拓扑限制。

6 个规则用例覆盖三类有效编辑及三类无效操作，验证撤销、重做和历史。修复前 5 失败／1 通过；相关 43 用例通过。全量 119 文件／1383 用例通过，类型、lint、构建通过。证据位于 evidence/f2-graph-dirty。

实际浏览器验证删除最后一个节点后的新建保护、取消及撤销恢复。此次为浏览器与 Mock 服务验证，不计为正式 Electron 端到端或真实运行。F0–F6 尚未全部完成。
