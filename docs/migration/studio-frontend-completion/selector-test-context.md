# F4 定位测试的编辑上下文

2026-09-14；只读定位测试绑定发起文档、节点、选择器和提示信息，并使用前端请求序号隔离旧响应。换节点、换文档、删节点或卸载后不显示旧成功结果；字段变化提示重新测试。新请求的加载状态不能被旧响应清除。有效结果和错误日志绑定原节点，不改写选择器或未保存状态。

先复现 8 项失败；8 项修复用例加 284 个真实 ConfigPanel 注册回归共 292 项通过，再增加一项交错响应测试后 9 项定向通过。TypeScript、ESLint、renderer/main/preload 构建通过。证据：evidence/f4-selector-test-context。

本批用受控 API 响应验证前端，未查询真实网页，不宣称零/一/多匹配的真实浏览器执行通过。后端页面身份和导航失效、拾取会话启动/停止隔离、请求取消及资源清理仍需完善契约；F4 未关闭。
