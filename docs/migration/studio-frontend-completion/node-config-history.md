# F2 网页配置分支与共享参数历史

- 日期：2026-09-14；状态：本批 confirmed，F2 整体未完成。
- 原因：ConfigPanel/模块条/AI 调用的 updateNodeData 未记录历史和脏状态。修改参数后立即撤销会跳过参数编辑，甚至撤掉节点。

共享 updateNodeData 现在使用 updateNodesData：计算实际差异后保存前状态，一次提交并标记未保存；无效节点或相同值不产生编辑。批量 AI 配置与已确认选择器批量写回使用同一批量入口，整批只占一次撤销；重复节点补丁按请求顺序合并。

27 个实际 ConfigPanel 分支测试：打开方式 2、页面等待 3、已开页面匹配 2、点击类型 3、提取属性 6、等待元素状态 4、等待模式 3，以及跟进新标签页/输入前清空共 4 个开关分支。下拉通过键盘展开并点击真实选项；检查 Store 值、脏状态、条件说明/显隐、撤销重做、复制及文档导出重开。另 3 项批量/无变化/可选字段历史测试。相关 38 项通过，完整 116 文件/1355 项通过，类型/lint/构建通过。

实际浏览器完成导入、保存基线、修改打开方式、画布撤销重做、保存覆盖、刷新重开验证；详见 evidence/f2-node-config-history/browser-ui.md。

范围未完成项：这些不是七类节点的所有字段验收。URL/文本/数值合法性与变量插入、默认值一致性、全部通用高级字段、拾取会话上下文、其余节点分支和工具仍需逐项补齐。布尔分支已验撤销与重开，尚未在该测试覆盖复制；不将共用下拉测试覆盖冒称布尔复制。批量写回的异步文档/字段过期保护也尚待完成。F0–F6 均不因此关闭。
