# 拾取停止确认与重试

当前挂载的ConfigPanel统一显式停止、单选应用后停止、相似应用后停止。等待期间只发一次请求；收到业务失败/网络异常时保持停止入口，显示错误，不记录已停止。停止意图立即作废本轮结果和轮询，避免失败等待期间仍写入节点；已应用的选择器/索引保留。重试成功才清除本面板拾取状态。相似结果写入仍可整批撤销。

Mock增加一次性停止503场景，失败不释放picking；停止仅POST，错误方法405且不改变状态。开发按钮只在预览装配。协议保留原/system等错误包装，不把HTTP接受误当成功。

新增5组件和2内存/HTTP协议用例，连同已有拾取/定位上下文合计36通过。首次4组件失败并产生1未捕获Promise；实现后协议2项误期望裸错误文案，改为既有API包装的精确HTTP状态前缀，保留503和资源状态断言。类型/lint、构建及21脚本通过。实际UI失败→保留入口→重试成功见evidence/f4-picker-stop/browser.md。

本批不表示全局会话生命周期完成：ConfigPanel卸载/换节点清理、与AutoBrowser跨入口所有权、启动中取消、真实进程清理及正式Electron仍有缺口。没有因本批标F4整体通过。最近完整回归仍是上一批142文件1715项，本批报告定向36项。
