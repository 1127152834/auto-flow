# F2 数字参数与等待字段保真

2026-09-14。共享NumberInput过去用parseFloat读取前缀，1abc会成为1，Infinity会进入文档；失焦还会把空值/非法值写成默认值、把越界值夹到范围内。现在只转换完整、有限的十进制数字，保留其它原文并显示错误；不再静默替换用户配置。

保留原变量提示与引用字符串。中间输入1.不会因父组件接受数值1而擦掉小数点；外部撤销/赋值仍同步。min/max错误显示但不自动改值。原onBlur现在与内部处理共同执行，避免props展开覆盖内部处理。初始缺省仍用现有defaultValue；主动清空保留未完成草稿。

WaitConfig改为复用NumberInput，去掉第二处parseFloat截断逻辑。参数结构、节点入口和后端存储不变。

新增26项测试：非法原文6、空值1、越界2、小数编辑1、引用2、blur回调1、等待非法值3、合法数值6、外部更新1、等待字段撤销/重做/真实Mock保存重开3。首轮16项13失败3通过；修复后26项通过。类型首轮因测试缺少NodeData.label失败，补完整夹具后类型/lint通过。

全量前端136文件1648项通过；renderer/main/preload构建、21项脚本检查通过。实际浏览器拖入固定等待→输入1abc→失焦提示→保存→刷新→重开，原文和提示恢复，见evidence/f2-number-input/browser-ui.md。

## 边界

未新增表达式解释。含变量文本沿用源行为；完整变量语义及全图运行前校验仍待完善。这里只保证已编辑参数不被静默篡改，不宣称所有非法参数已禁止运行。每个节点的专属整数/步长/范围规则仍须逐项验收；不因共享组件通过就把284个节点所有分支标通过。
