# F2 不同高度节点下对齐

- 日期：2026-09-14；状态：本批 confirmed，F2 未完成。
- 来源：editor-store.alignNodes 及工具栏全部对齐入口。
- 原实现取最大 y，实际对齐顶部，节点高度不同时底边不一致。现在按 y + height/measured.height 计算最远底边，并减去各节点高度；复用原历史、脏状态及 React Flow 尺寸。
- 新测试 3 项：不同高度下对齐/未选节点不变/撤销重做；仅两节点水平均分、垂直均分不吞掉上一真实编辑的撤销。后两项是既有正确行为的回归，未声称原实现失败。
- 修复前 1 失败、2 通过；修复后与其他编辑命令共 11 通过。类型、lint、构建通过。证据 evidence/f2-alignment。
- 同期完整回归 111 文件/1295 用例通过（启动快照早于本批新用例，不将新用例计入该总数）。本批没有正式 Electron 端到端对齐验收。
