# F4 相似元素生成的原子编辑

2026-09-14。复用现有 updateNodesData，为确实需要同时变更变量的编辑提供可选变量快照。一次历史记录提交选择器和索引变量，保留当前节点选择；变量副本与调用方隔离，相同更新不标脏、不消耗历史。既有同名变量沿用源行为更新值，撤销恢复旧值；新建变量随撤销移除。

相似元素对话框是在选择索引变量名，不是重命名流程变量，关闭其通用输入框的重命名检测，避免误改其他节点已有引用。

3 个新组件用例修复前失败；修复后增加 2 个 Store 边界用例。拾取、编辑命令、变量及节点历史共 37 项通过，类型与 lint 通过。本批未另做真实页面相似元素采集或正式 Electron 验收。证据 evidence/f4-similar-atomic；前一份 picker-context-validation 中相似元素整批撤销缺口由此补齐，全局会话所有权和清理责任仍未补齐。
