# F4 拾取结果的编辑上下文保护

2026-09-14。ConfigPanel 将拾取请求绑定文档、节点、字段及原元素提示。面板切换/卸载使响应失效；每个 await 后重新校验。取消先撤销本地请求资格，迟到选择结果不能再改草稿或复制选择器。轮询不重叠，相似元素预览及确认也校验原字段。选择器与 selectorHints 合为一次撤销。

初始 11 个回归用例全部失败；修复后新增相似元素和轮询异常覆盖，共 16 个新增用例通过，加定位测试 9 项共 25 通过。类型、lint 和构建通过。实际浏览器 UI 完成拖拽→拾取写回→一次撤销/重做→测试→保存→刷新重开，详见 evidence/f4-picker-context/browser-ui.md。

本批只证明前端结果隔离；全局 picker 仍缺跨入口 session/request 所有权合同，启动中取消的服务器清理、清理失败责任及相似选择器与新增变量整批撤销仍待完成。未据此标记 F4 完成。
