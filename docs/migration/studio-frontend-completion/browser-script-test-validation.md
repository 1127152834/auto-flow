# 页面脚本测试验收

2026-09-14；已完成F1/F2有界配套工具切片，F0–F6整体仍未完成。

移除renderer主线程new Function和JSON伪DOM执行，保留编辑器布局并接入生成类型、服务上下文、稳定请求ID、原请求补查、取消确认、结果和错误展示。内存与HTTP Mock共用协议并明确不执行代码；增加后端DTO/OpenAPI，不伪造真实浏览器实现。

新增前端59项：协议16、编辑器11、实际API交付12、边界20；后端DTO16项。最终定向含相关编辑器回归6文件80项通过，类型与lint通过。完整回归163文件1972项通过，之后新增取消／完成竞争1项并修复文案；最终80项包含该修复，全量1972不包含该新增项，不宣称1973项全量已跑。脚本21、OpenAPI一致性、Ruff、mypy128文件及renderer/main/preload构建通过。

初次组件测试3项失败：大值文本定位受空白归一化影响（改为精确pre.textContent比较）；取消多等一轮查询（改为唤醒后直接取消）；上下文迟到后服务地址变化仍可能提交（提交前核对地址）。另补取消与完成竞争，保留完成终态且不应用过期结果。没有降低产品断言或删去失败场景。

实际UI验证缺浏览器、无返回值、取消、失败和保存重开，见evidence/f2-browser-script/browser-ui.md。大值、原ID补查、重复请求、取消失败、页面导航和字段变化由组件/本地HTTP测试提供证据。普通工作区恢复、宿主退出协调、同地址服务epoch和真实页面执行仍需后续实施，不能用Mock证据标通过。
