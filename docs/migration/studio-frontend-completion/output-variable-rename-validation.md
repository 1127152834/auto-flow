# 输出变量重命名与撤销

2026-09-14；状态：连续字段编辑的有界修复通过。

连续输入输出变量名后选择“全部更新”，此前字段逐字符历史和引用更新分离，撤销一次留下新名称配旧引用。现在保留该字段首次编辑前的历史点，确认时只有Store仍是本次输入产生的状态才合并；一次撤销、重做同步恢复名称与嵌套引用。外来编辑或其他Store变更使合并失效，保留原历史，不吞掉其他操作。这是保守的连续编辑合并，尚不承诺交错编辑也只有一次撤销。

新增6项组件测试：连续改名、确认期间另一次编辑、仅改此处、超过50次输入、输入之间另一次编辑、卸载时取消迟到确认。专项含变量历史和面板18项通过；全量164文件1979项通过（368.86秒），类型/lint、21脚本、OpenAPI及renderer/main/preload构建通过。

首轮2项失败：一次撤销仍为new是产品失败；另一项测试误用不存在的setName，修正为实际setWorkflowNameWithHistory后验证历史保护。未修改产品断言来放宽要求。

实际IAB界面以单独草稿“F2 输出变量撤销验收”验证：从动作库拖入JS脚本注入，输出old，备注{old} ${old} {older}；改new后确认全部更新；Cmd+Z后输出old且备注原样，Cmd+Shift+Z后输出new且备注{new} ${new} {older}。通过选择节点读取实际字段，未调用Store或页面内部函数。09:55:27保存成功。测试没有执行网页动作，不代表正式Electron宿主通过。
