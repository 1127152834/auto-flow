# 代码补全与运行事件上下文修复

2026-09-14，confirmed（有界切片），不代表 F2/F3 全部完成。

冻结源码的 JavaScript、页面注入和 Python 编辑器均注册 Monaco 全局补全而未释放。现三处共用 registerEditorCompletions：仅当前 editor.getModel() 接收建议，编辑器销毁时释放 provider。保持既有补全内容，不引入新依赖。

已确认活跃运行不会被重复或外来 started 事件重置；单/批量日志在浏览器错误检测和入缓冲前检查流程归属。缓冲携带接收时的文档标识，写入时再次校验；显式断开或替换连接时，在原有效上下文中冲刷并取消定时器。普通 SSE 断流续读保留缓冲，不误判结束。

新增补全6项，先全部失败，修复后通过；事件专项原6项中4失败，修复后全部通过。旧时间戳、重放、执行完成、节点高亮及脚本测试保持。联合专项4文件16项通过。编译检查发现测试替身/Monaco边界类型声明不足，按真实类型修复；未放宽断言或忽略lint。

本切片未做正式 Electron 或新浏览器交互验收；证据为真实组件+Monaco最小测试替身、内存及实际本地HTTP/SSE夹具。全量153文件1829项、TypeScript、ESLint、构建、21脚本、OpenAPI通过，见证据目录。独立runId、同文档不同轮次身份、日志持久化/服务端筛选、动态补全变量同步仍未关闭，不能以本切片宣称F1/F3完成。
