# Studio 图像缓存初始化与恢复

2026-09-14，F5 有界批次通过。复用已有 useStudioIntegration 和 imageAssetApi；不新增资源存储。

- 列表失败、异常和非法结构均记录明确错误，保留最后已确认列表，不显示成成功空库。
- 同工作区 SSE 重连、资源刷新事件及连接替换时重新读取。重复相同失败只记一次，恢复后记录恢复日志。
- 请求绑定组件生命周期、最新加载序号、连接修订和发起时缓存引用。卸载、旧请求或期间缓存已被上传/删除/其他刷新修改时，不覆盖新状态。
- 本项只保护初始化消费者。资源面板各写操作的服务端并发冲突、事务和完整跨工作区离开/隔离仍是后续任务。

## 验证

新增8项初始化/恢复场景。实现前6项失败并出现1个未捕获异步异常（另一个卸载保护已存在）；实现后与原初始化、图片选择、预览、上传及资源协议共5文件74项通过。TypeScript、ESLint、renderer/main/preload构建通过。

证据位于 evidence/f5-image-cache，8条逐项台账。未重跑整套前端，也未新增实际浏览器/正式Electron验收；本项并非完整资源管理并发方案。列表运行时校验与节点图像选择器共用 imageAssetContract，字段继续来自生成类型。
