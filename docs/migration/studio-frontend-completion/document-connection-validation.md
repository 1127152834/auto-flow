# 文档保存与打开的连接隔离

2026-09-14，F2/F5 局部交付，未关闭完整里程碑。

## 修复

- 保存捕获文档身份和服务连接修订号，默认目录、存在性检查、覆盖确认及保存回执后重新核对。旧请求不能继续向新连接写入或清除当前草稿标记。
- 默认目录缓存绑定返回它的连接；同一时刻只允许一次保存。显式 success=true、有效 filename 且无 error 才确认保存。
- 保存/放弃/取消的离开决定绑定原服务连接；确认期间换连接必须重新操作。保存期间新增编辑继续保留。
- 打开流程在读取和离开确认后检查原连接；文件列表读取有序，关闭/卸载/连接变更后的旧列表不应用。错误列表展示错误和刷新入口。
- 连接变化清空旧列表。删除确认与结果绑定原连接并防止重复提交；旧列表不能对新服务执行打开/删除。
- 整包读取、还原资源与应用结果绑定原连接；旧返回不覆盖新画布。该链路仍依赖服务端资源事务，未声称实现资源回滚。
- 文件操作图标添加可访问名称，便于窄窗口下使用菜单。

## 验证

本批首轮新增 10 项连接/回执用例均失败，修复后通过；补充列表格式、重试、旧条目失效和删除确认用例后，相关 2 文件 51 项通过（新增共 16）。未删除或弱化断言。

浏览器 localhost:5175 专用 Mock 数据：名称填写“F5 文档连接隔离验收”→Ctrl/Cmd+S，日志确认保存该 JSON；文件操作→打开→搜索测试文件→点击，日志确认已打开。不直接调用 Store，不操作真实文件或真实工作区。

全量回归与构建运行中，最终结果随后补记。真实宿主离开顺序、活跃运行/拾取/录制清理、工作区事务、文档接口的完整生成 DTO/稳定写入 ID/并发 revision 仍未关闭。本批连接修订号只是客户端失效保护，不能冒充服务端并发协议。

最终前端全量：280 文件、3361 用例全部通过，498.60 秒；包含留存和本批文档连接改动。renderer/main/preload 构建 32.83 秒通过，OpenAPI/目录检查通过。
