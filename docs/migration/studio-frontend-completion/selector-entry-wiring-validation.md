# F2.2 共享拾取工具全部入口核销

2026-09-15。仅扩展入口验收，没有修改共享 Store、协议或业务组件。

按现有组件和能力映射，当前 227 个保留节点中共有 24 个节点、26 个字段直接调用 `ConfigPanel.renderSelectorInput`。字段、组件、源码位置和显隐条件见 `evidence/f2-selector-entries/entries.json`。

本批已核销全部 26 个入口，包括普通网页元素字段、拖拽的源和目标字段、下载点击模式、条件与断言分支、等待和截图模式、AI 图片来源及验证码字段。

每个入口均通过实际 `ConfigPanel` 完成以下验证：

- 按字段条件进入正确配置分支；等待和截图在默认分支不显示拾取按钮。
- 点击字段旁的“可视化选择元素”，通过 URL 对话框启动选择器。
- 选择器使用流程内 `open_page` 的 URL，启动请求只发送一次。
- 轮询结果只写入目标字段；拖拽节点的另一选择器保持原值。
- 应用结果可撤销、重做，并能经工作流导出、清空、导入恢复。

专项结果：`selector-entry-wiring.test.tsx` 共 26 项，26 项通过，耗时 4.18 秒。TypeScript 与该测试文件 ESLint 均通过。

共享的迟到响应、取消、请求身份和恢复规则继续引用既有 `picker-result-context`、`picker-session-protocol` 与 `selector-test-context` 证据，不在 26 个字段重复复制同一套协议用例。这里关闭的是选择器入口接线层，不代表这些节点的其他配置字段已经全部验收。

测试使用 typed API mock 验证前端与服务消费协议，不代表真实浏览器拾取，也不替代正式 Electron 端到端验收。
