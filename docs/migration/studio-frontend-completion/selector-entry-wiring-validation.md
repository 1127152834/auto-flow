# F2.2 共享拾取工具核心入口核销

2026-09-15。仅测试新增；没有修改共享 Store、协议或业务组件。

按现有组件/能力映射定位到当前227节点中24节点、26个字段直接调用 ConfigPanel.renderSelectorInput，具体字段和源条件见 `evidence/f2-selector-entries/entries.json`。本批优先核销10个网页核心节点的11个字段：点击、悬停、输入、等待元素、等待的元素模式、提取、元素截图、下拉框、复选框，以及拖拽源/目标两字段。

每个入口通过实际 ConfigPanel 按钮和 URL 弹窗启动选择器，确认使用流程中打开网页的 URL 且只发一次启动请求；确认结果只写正确字段，拖拽另一字段不受影响；撤销/重做/序列化重开保留。等待和截图必须先用实际下拉切入元素模式，并验证非元素模式无拾取按钮。没有直接调用页面内部函数启动拾取，没有直接修改 Store 来替代该UI操作。

继承既有 picker-result-context / picker-session-protocol / selector-test-context 的共享迟到、取消、请求身份和恢复证据，不在11个字段各复制整套协议错误用例。本文只关闭入口接线层，不宣称这些节点所有表单字段已验收。

新增11项全部通过，3.19秒；TypeScript、该测试文件ESLint通过，输出位于证据目录。使用typed API mock响应，不是真实浏览器拾取，也不是正式Electron端到端。余下15字段在同一entries清单标缺验收，未自动标缺实现，没有扩展不存在的隐藏工具。
