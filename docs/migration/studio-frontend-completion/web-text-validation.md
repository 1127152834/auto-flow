# F2 网页文本与截图分支

2026-09-14，confirmed。新增 14 个实际 ConfigPanel 用例：URL 模板/空值、页面标识、CSS/XPath/变量选择器、多行及空输入、列名模板、中文输出变量名，以及三种截图范围的选择器显隐与保留。

初次 1 失败、13 通过：输入文本使用单行 input，换行被浏览器控件移除。复用 VariableInput 的 multiline 支持，避免另造输入组件。随后相关 44 项通过；类型/lint/renderer/main/preload 构建通过，证据 evidence/f2-web-text。

文字字段用真实 onChange 和 blur 操作，验证原文字面量、未保存、撤销重做、复制及保存格式重开。截图三种范围验证显隐及保存重开，未额外声称本批覆盖截图工具调用和执行。

真实浏览器完成多行编辑、保存、刷新和打开；两行中文与两种引用文本保持原样。该证据不覆盖运行时变量解析。

F2 未关闭：数值字段的严格解析、默认值/显隐统一、变量插入/重命名竞态和共享工具服务合同仍待补齐。前次 116/1355 全量是本批之前的基线，本批没有重复全量，按单属性修复运行相关测试及工程检查。
