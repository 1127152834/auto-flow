# SSE 序号连续性与连接释放

日期：2026-09-13；状态：局部implemented/verified。

事件ID为正安全整数。重复或较旧ID忽略；新ID须等于最后确认序号+1。发现缺口不应用后续事件，结束本连接并从最后确认位置重新读取。每次连接单独AbortController，重连前关闭旧响应，窗口生命周期结束取消全部请求。

GET /api/events/stream?afterSeq=N：非负安全整数；非法400，超出本服务日志尾部409。不能把失效游标解释为一个永不返回数据的正常连接。

真实HTTP和内存验证同一缺口1/3，补读2/3后仅交付1/2/3，并验证旧连接释放。完整97测试文件/1,137用例，类型/lint/构建通过。日志见f1-sse-*。

边界：服务重启事件epoch、日志截断游标重建、持久状态重载仍需独立协议；目前超范围409明确告知连接错误，不擅自制造运行终态。此合同不是F1或日志全量验收完成。
