# 静态数字运行预检

2026-09-14；状态：本批静态数字预检通过。F2/F3有界校验切片，不声称代替所有284节点的完整运行校验。

已确认差异：NumberInput保留非法原文且标红，Toolbar并未检查；冻结WebRPA workflow_executor.py在非法timeout/retryCount时静默回退默认。AutoFlow保留未完成草稿的保存能力，运行时拒绝已知静态错误，不能把用户错误默默变成其他参数。

本批范围：全草稿中可执行节点的共同超时、重试次数、重试间隔，以及wait节点的时间等待时长；只检查当前生效分支，禁用节点/画布工具不参与。按源码忽略使用内部超时的控制、交互和子流程节点的外部timeout。retryCount使用现有面板0–10整数约束，时间字段使用非负有限数字。缺省字段沿用默认，显式非法文字不覆盖成默认值。

普通十进制、正负号、小数、科学计数法共享NumberInput现有解析规则。含变量模板的动态值不在前端求值，保留到运行服务验证；本批不宣称已检查其类型/作用域或表达式合法性。保存、导出、撤销和编辑草稿不受运行预检阻止。

普通、有头/无头、从节点启动、快捷键共用同一入口；直接起跑只检查起点及沿连线可达节点，不用跳过的上游参数阻止调试；在任何创建/更新/执行请求前检查，用日志给出nodeId+参数路径+原因，选中首个错误节点。后端接入后仍必须独立验证所有参数。新增长循环、结构控制图和284专用表单的校验不属于本切片完成声明。

验收：未选节点的非法字段也拒绝；禁用/非生效字段不误报；默认节点不受影响；合法边界和变量原文保留；所有启动入口不发请求；修正后可重新启动；离开和保存仍工作。

## 验收结果

新增27项测试，包含保留目录284入口的共同数字默认值遍历（此项不代表各节点全部字段分支）。首次21通过3失败，三种启动入口均实际发送了非法配置；接入共同预检后，结合启动、数字输入及草稿保护共105项通过，类型、lint、21脚本、OpenAPI及构建通过。本批未再次运行全量；最近完整基线是上一批164文件1979项。

实际IAB：在独立测试草稿把超时改为1abc，10:11:14按F5出现nodeId+data.timeout+有限数字错误，没有出现运行中状态；改回60后10:11:36收到启动，10:11:37明确Mock调度后终态成功。10:12:29把已恢复合法参数保存。此证据仅验证前端与Mock服务，不是网页动作。

边界：含左花括号的动态值交由执行服务验证，本批不会解析或执行表达式；未补全所有节点专用字段、子流程调用展开及结构图预检。普通运行执行草稿级静态检查；直接起跑仅遍历显式连线可达部分。无效原文仍能保存。不能据此标F2/F3整体完成。
