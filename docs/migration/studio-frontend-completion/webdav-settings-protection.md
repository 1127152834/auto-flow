# WebDAV 配置前端确认与恢复

2026-09-14，F5 有界批次通过。继续保留WebDAV配套，使用现有三条local-workflows接口。

- 读取失败/非法配置明确显示并可重试；未读取确认前不允许用空默认值覆盖服务配置。
- 保存和测试共享互斥状态；HTTP失败、业务失败和非法确认不能显示成功；等待期间表单锁定，保留失败时输入。
- Mock保存只说明保存了模拟配置；Mock连接测试保持明确未执行，不冒充网盘可用。
- 卸载或连接替换后的旧响应不生效；连接替换后禁止把旧配置发送到新服务，要求重新读取。
- 本项不实现WebDAV客户端、密码持久化、远程文件操作或真实连接验收。

## 验证

首批8个新组件用例全部复现失败；修复并补充URL/迟到保存测试后10项通过。内存与本地HTTP各一条保存→重读→模拟连接失败链路通过（Mock不存测试密码），合计12项。TypeScript、ESLint、renderer/main/preload构建通过。IAB实操另1项，见 evidence/f5-webdav/browser-ui.md；13条逐项台账。

本批未重跑全量；上一个260文件2988项快照不包含这两个新测试文件。HTTP请求通过共享apiRequest处理状态和错误。完整WebDAV DTO、写入修订和真实凭据/文件服务仍待冻结，不能标F1/F5整批完成。
