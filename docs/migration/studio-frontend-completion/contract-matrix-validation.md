# F1 服务消费矩阵登记

2026-09-14。inventory-studio-services.mjs 使用既有 TypeScript AST 扫描扩充请求方法、端点表达式、请求体和显式响应类型，并生成 contract-matrix.md。转发 options 无法确定方法时标记 dynamic；不猜 GET，不将响应类型缺省解释成已验收。所有服务、事件和直接请求逐项展示位置。

当前登记 142 个服务方法、83 个事件、47 个直接请求，105 个 AI 画布操作另列。声明、静态消费者和实际验证分开；零静态消费者不授权删除。class、动态别名、IPC 等扫描盲区明确记录，完整 F1 合同仍未冻结。

新增脚本测试 4 项覆盖真实上传 multipart/生成类型、动态转发、矩阵完整登记和确定性生成；全部脚本 21 项通过，OpenAPI 一致性通过。逐服务必填字段、错误示例、所有权/修订、分页及清理仍需继续销账。
