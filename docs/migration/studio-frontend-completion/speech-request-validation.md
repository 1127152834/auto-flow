# 语音请求确认与重连验收

2026-09-14，confirmed（F1/F3有界切片）；整体F0–F6仍未完成。

语音通知由领取、执行、结果确认三段组成，复用既有稳定命令查询和HTTP/SSE通道。新增后端Pydantic/OpenAPI请求、状态和结果模型，生成前端类型；Mock与内存/真实本地HTTP均执行所有权、幂等、参数及过期检查。本批没有新增真实自动化后端业务路由。

原生语音成功只以onend为据；错误、环境缺少能力、60秒超时明确失败。停止取消本次朗读，迟到回调不提交。音量0有效。语音与脚本在普通断流期间保留原Socket及在途任务，手动重连不再替换对象而使结果失效。

## 修复与证据

- 首轮消费测试10项失败：终态中的全局停止与请求取消重复，正常成功也取消引擎。改为请求拥有清理职责，未放宽断言。
- 手动重连初测2项失败：朗读结束后运行停在running。保留同地址连接对象后，语音及脚本的内存/HTTP重连均通过。
- 最终专项5文件63项通过；全量157文件1884项通过。新增复制按钮测试在全量收集之后另行运行，不计入此数。
- 后端语音及兼容脚本合同19项通过；Ruff、mypy（128文件）、TypeScript、ESLint、21项脚本及OpenAPI一致性通过。构建结果见evidence/f3-speech-request/build.log。
- 原生浏览器实际运行见evidence/f3-speech-request/browser-ui.md；音量0，只证明原生结束回调及真实UI命令闭环，不证明音质、真实网页自动化或正式Electron。

逐项新增语音用例43项、脚本手动重连2项、浏览器1项已登记verified-cases.json。完整源码具名测试作为具体输入及断言依据。

真实后端仍须实现相同请求和命令持久化；同URL服务崩溃后新事件日志的epoch恢复、独立runId、正式宿主和跨平台仍在未完成范围。当前连接修复只验证普通SSE断流，不冒称解决进程重启。
