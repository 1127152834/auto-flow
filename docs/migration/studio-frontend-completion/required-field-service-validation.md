# 必填字段服务验收记录

2026-09-14。范围：F1/F2 必填字段元数据的契约、加载、缓存、连接隔离与配置提示。

## 结果

专项 45 项通过（服务/组件 34、内存与真实 HTTP 协议 4、既有规则 7）；后端 DTO 14 项通过。全量前端 251 文件、2,829 项通过。脚本 48 项、OpenAPI 一致性、TypeScript、ESLint、Ruff、mypy（202 文件）、renderer/main/preload 构建通过。日志见 [本批证据](evidence/f1-required-fields/full.log)。全量数量包含并行任务已经合入的其他功能，不能把全部新增测试归于本批。

生成器从冻结 WebRPA AST 字面量读取元数据，当前批准的 284 个节点中，原规则只覆盖 69 个，其余 215 个逐项记录在 required-field-source-coverage.json。未覆盖节点显示明确提示，不能被解释为无必填项。条件规则的分支使用合同夹具验证；当前保留源数据没有条件规则，不虚构实际覆盖。

## 真实 UI 操作

在独立 IAB 页面 localhost:5175/studio.html，通过界面新建“F2 必填规则验收”，拖入“打开网页”，展开配置面板。空 URL 显示缺少“要打开的网页 URL”。开发场景触发一次规则服务 503，显示失败及重新读取入口；点击重试恢复必填提示。填写 https://fixture.invalid/required-fields 后提示消失，10:27:35 通过 Cmd+S 保存成功。本链使用 Mock 元数据服务，没有执行网页动作；正式 Electron 与其他平台未实测。

## 修复与边界

补齐原先缺失的 Mock 路由及严格 DTO；失败不缓存为空规则，允许重试；相同连接合并请求，替换连接拒绝旧响应。连接地址先更新再通知订阅者。接口扫描补入 Api 对象外的 apiRequest 及普通 Event，生成台账仍是静态候选，不代表所有动态调用已冻结。

初轮两个 ConfigPanel 用例因未选择节点/窄屏折叠而失败，修正测试前置后通过；另一个连接用例错误地假设测试 setup 没有配置服务，改用隔离模块验证初次同步订阅。其间一次全量运行主动中断，不作为通过证据；最终完整重跑通过。未放宽产品断言。

仍需逐节点补齐 215 个未覆盖规则及全部显隐分支的来源核对。本批不构成 F1/F2 总门槛完成，不代表真实后端自动化能力交付。
