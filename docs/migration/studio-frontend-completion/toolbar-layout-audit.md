# 窄窗口工具栏审查

2026-09-14，浏览器 CUA 实际截图与只读 DOM 几何检查。

当前可视区域约 357px，header 固定高度 h-12 且不换行。文件菜单 x≈309、宽 38；节点对齐 x≈359、宽 60；右侧服务组 x≈431、宽 186。后两项位于右边界外，需滚动才能操作。该问题不属于可接受的功能隐藏，不能用组件测试节点存在代替可点击验收。

下一批：保持宽窗口原布局，窄窗口让工具栏自然换行或完整收纳；运行、文件、对齐、浏览器、录制、配置全部可访问，图标有名称。用实际浏览器验证 357/768/1440 宽度的边界和点击，不改变 Store 绕过菜单。暂不承诺高 DPI 或正式 Electron 通过。

补充实测：768px 时“更多操作”右边界约 811.6px，越界约 43.6px；1440px 时各按钮右边界在 1428px 内，未越界。视口测试完成后已恢复原 357px 默认大小。
