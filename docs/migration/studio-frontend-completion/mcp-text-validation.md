# MCP 配置文本解析与旧传输配置兼容

2026-09-14；本批通过，延续 F5 保存保护。

冻结表单把没有分隔符的环境变量或请求头行静默跳过，重复键后值覆盖前值，容易保存出与用户输入不同的配置。本批在提交前指出具体行并保留原文，不发送不完整配置。空行忽略；空值允许；值中的后续等号/冒号保留；沿用原先首尾空白整理。环境变量键区分大小写，请求头名称不区分大小写；重复键明确拒绝。只验证当前 transport 对应的可见字段，切换模式不能被隐藏字段错误阻断。

同时核对冻结 mcp_manager：兼容缺省 transport 的自动推断，以及 streamable_http / streamable-http 别名。表单展示为 http，未编辑读取不自动写回。此前新增加载校验不能阻止已有兼容配置读取。

用例覆盖合法及非法行、空值、重复键、分隔符保留、各传输模式、模板/保存保护回归。真实 MCP 启动、权限批准和命令执行仍不在本批。

## 验收结果

文本规则 18 项，表单新增 9 项的阶段专项共 45 项通过；之后与生成契约整合，新增扩展字段及部分失败显示 2 项。最新 MCP 三个文件共 64 项，含既有保存保护；加传输回归共 84 项通过。后端合同 29 项通过。TypeScript、ESLint、Ruff、mypy（202 文件）、OpenAPI、49 项脚本和 renderer/main/preload 构建通过。证据见 evidence/f5-mcp-contract。

中间全量 254 文件、2,880 项通过，覆盖文本解析阶段的文件集；不包含随后新增 mcp-service-contract.test.ts 的 17 项及表单新增 2 项，不将 19 项算成已完成全量回归。当前最新代码另有 84 项专项和构建证据。

实际 IAB：编辑已有禁用 Mock 配置，环境变量输入 OK=1 与第二行 BAD LINE，点击保存出现“环境变量第 2 行必须使用 KEY=VALUE 格式”，原文保留。取消重开，环境变量仍为空，命令 node / --version 与禁用状态保持原值。没有保存错误文本或执行命令。

首次表单测试使用多行 placeholder 字符串，因测试查询的空白归一化未匹配，改为真实可访问的“环境变量/请求头”标签查询后通过；未改动失败断言。源配置传输大小写/别名及 prototype-like 键已补回归。跨工作区在途配置请求和真实 MCP 执行仍不在本批。
