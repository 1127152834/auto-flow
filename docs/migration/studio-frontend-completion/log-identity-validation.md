# 日志身份验收

2026-09-14。服务端日志 ID、timestamp、details 经单条和批量缓冲保留；Store 按 ID 对当前保留行及本批输入去重。不同 ID 的同文日志仍各自展示；无 ID 的旧日志仍独立生成本地标识。

新增 6 项测试，相关专项 24 项通过。修改前 10 项中 5 失败（重复行/源 ID 丢失），修复后通过。全量 252 文件、2,835 项通过，TypeScript、ESLint 通过。证据在 evidence/f3-log-identity。全量测试文件集不包含随后新建的 MCP 测试，MCP 另行验收。

内存与真实本地 HTTP/SSE 两种路径均验证单条与批量交叉重复、保留行后的重连、源 details 及不同日志 ID。既有清空补读、跨文档缓冲隔离和时间戳回归通过。Store 验证相同 ID 内容冲突保留首次值、无 ID 不按内容去重、容量裁剪与清空恢复。

本批没有新增实际浏览器/Electron UI 操作证据；网络协议通过不等于正式端到端完成。去重仅覆盖保留窗口，未解决独立 runId、服务生命周期身份、持久化历史分页或全部日志导出，F3 总门槛仍未完成。
