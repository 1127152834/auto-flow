# F1 服务合同关闭证据

日期：2026-09-15

- 当前源码扫描：171 个服务方法、83 个事件、50 个直接请求、105 个 AI 画布操作。
- 服务族登记：23/23，通过 `studio-service-inventory.test.mjs` 的固定集合断言防止新增服务族静默漏出合同矩阵。
- 前端协议：54 个测试文件、1565/1565 通过；同一协议同时经过内存 fixture 和本地 HTTP/SSE。
- 后端 DTO 合同：503/503 通过。
- 服务清单脚本：8/8 通过。
- OpenAPI 一致性：通过。

详细服务族、共享规则和真实后端边界统一记录在 `../../contract-matrix-validation.md`。生成矩阵中的“已登记”只代表前端消费合同冻结；真实执行、采集、清理、模型、MCP、WebDAV、计划任务和秘密存储进入 F6 后端交接，不能用 fixture 通过替代。
