# 实际页面操作记录

2026-09-14，macOS当前浏览器，http://localhost:5175/studio.html。通过CUA点击/输入操作，不直接修改Store或调用页面内部函数。

1. 文件菜单→打开→F3 输入回传验收，05:18:44显示已打开，画布2节点。
2. 运行菜单→运行，05:18:55显示启动接受并等待“输入回传验收”弹窗，原始值“原始值”。
3. 输入“确认回执与恢复验收”→确定。05:19:20弹窗关闭，日志显示“[Mock] 已接收输入结果”。
4. 后续提取节点显示Mock调度日志，完成2节点/失败0；数据表格1行。
5. 展开接口场景→断开 SSE 并补读；页面保持工作台，未重新显示已回答的输入弹窗。

刷新/HMR会重新初始化浏览器内Mock服务，不能代替同服务历史回放。新事件客户端回放完整旧事件的断言由真实HTTP集成用例执行；这里仅记录界面实际断流补读。未验收正式Electron、打包入口、macOS Intel或Windows。后续提取是模拟事件，不是真实浏览器提取。

控制台进一步确认：05:19:31.983断流，05:19:32.988重新连接（本地时区）；没有再次输入或提交。
