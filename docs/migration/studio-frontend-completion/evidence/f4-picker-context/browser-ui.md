# 浏览器 UI 验收：拾取写回

2026-09-14，Codex 内置浏览器，http://localhost:5175/studio.html，独立验收标签。服务为明确标注的 Mock，未实际操作第三方网页，不属于 Electron 实机验收。

1. 在动作库搜索点击元素，实际拖拽到空画布。输入选择器 #before。
2. 属性按钮打开 URL 对话框，留空并启动选择器。界面显示停止选择，日志显示启动成功。
3. 通过接口场景面板的“拾取：提交元素”按钮提交受控结果。04:21:39 画布和属性均变为 #submit，日志显示拾取及复制完成。
4. 聚焦画布后 Ctrl+Z 一次恢复 #before；Ctrl+Y 恢复 #submit。工具栏按钮保持焦点时第一次 Ctrl+Z 没有生效，切到画布焦点后验证通过，未把第一次算通过。
5. 点击测试定位，04:22:33 日志显示命中 1 个元素，并明确文本为 Mock 定位结果（未查询网页）。
6. 名称改为 F4 拾取写回验收，Cmd+S 保存。日志确认 F4 拾取写回验收.json。
7. 重新加载标签，从文件菜单打开该文件；04:23:12 成功恢复 1 个点击节点，画布选择器 #submit，状态空闲。

核心动作均通过点击、拖拽、输入和快捷键完成，没有写 Store 或调用页面内部函数。异步迟到和相似元素分支由组件测试覆盖，本 UI 用例不替代这些测试或真实页面拾取。
