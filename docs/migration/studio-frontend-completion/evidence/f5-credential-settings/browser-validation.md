# 凭据面板实际界面验收

2026-09-14，Codex内置浏览器，独立http://localhost:5175/studio.html来源，Mock服务。

从更多操作进入全局配置→凭据库，显示仅保存名称和打码字段信息的Mock说明；起始列表为空。新增“F5 凭据确认验收”，说明“仅用于 Mock 元数据验收”，测试值使用虚构字符串，非真实口令。添加第二字段并填入带空格的重复value，点击保存后明确提示字段名重复。确定后表单名称和说明保留，移除重复行再保存，列表显示该名称、说明及value: •••••• (Mock)。刷新后仍有同一记录。进入编辑再取消，关闭全局配置。

所有主要操作通过CUA点击/输入完成，没有调用Store或页面内部方法。列表打码回显已观察；针对密码输入空值的一次只读定位超时，因此本条UI证据不单独断言底层密码input值为空。未执行删除、真实加密或后端变量注入。保留上述Mock元数据样例，配置窗口已关闭。
