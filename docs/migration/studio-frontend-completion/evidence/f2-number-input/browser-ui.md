# 实际画布数字参数保存往返

2026-09-14，macOS当前浏览器，http://localhost:5175/studio.html。CUA真实点击、拖拽和输入，没有直接操作Store。

1. 展开284入口动作库，搜索固定等待，从库拖拽节点到画布，节点数1。
2. 打开节点参数，等待时长填1abc；点击面板标题失焦，输入仍为1abc，显示“请输入有效的有限数字或变量引用”。
3. 命名F2 数字原文验收，Cmd+S，05:47:44日志确认保存为F2 数字原文验收.json。
4. 刷新页面，通过文件→打开选择该文件。05:48:13确认打开，节点数1。
5. 选中固定等待并展开面板，等待时长仍为1abc，错误提示恢复；未运行该无效草稿。

保存使用当前浏览器Mock数据服务；没有计作正式Electron/真实后端执行。测试文件留在独立localhost测试数据中，未删除用户文件。
