# 浏览器调试与输入联合验收

2026-09-14，macOS当前浏览器，http://localhost:5175/studio.html。全部操作通过CUA界面点击和输入完成，没有调用Store或页面内部函数。

1. 打开已保存“F3 输入回传验收”（2节点），点击“用户输入验收”的设置断点按钮，按钮变为“移除断点”。
2. 05:34:13运行，界面显示“断点暂停 @ 用户输入验收”；点击单步。
3. 出现输入弹窗，填入“调试单步输入值”，确定；05:34:37显示已接收输入结果。
4. 页面进入“单步暂停 @ 输入后提取验收”；展开变量，answer值为“调试单步输入值”。没有执行第二节点提取。
5. 05:34:48点击调试条停止，暂停条消失、工具栏空闲；当时日志错误显示“执行失败，共执行1个节点，失败0个”，据此修复停止状态日志。
6. 修复后热更新回放同一个停止事件，页面显示INFO“执行已停止，共执行1个节点，失败0个”。新运行的停止日志另由HTTP集成用例断言，热更新回放不冒充重新执行。

此为实际UI操作+Mock协议链路。没有真实网页动作或浏览器进程清理证据，正式Electron/打包/Windows/macOS Intel仍未实测。
