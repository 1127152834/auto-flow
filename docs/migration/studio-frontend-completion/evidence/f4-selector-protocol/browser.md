# 浏览器 UI 验收（2026-09-14，Asia/Shanghai）

地点：独立验收页 http://localhost:5175/studio.html，Codex IAB tab 7；内存 Mock 服务。通过 CUA 点击，不直接调用 Store。

1. 06:08:50 从文件菜单打开已有「F4 拾取写回验收」，选择器恢复为 #submit。
2. 06:09:01 点击测试定位：ERROR「浏览器未打开，请先启动浏览器或元素拾取后再测试」。
3. 06:09:20 通过可视化选择元素→启动选择器打开 Mock 会话；06:09:25 点击停止选择。
4. 展开接口场景，选择定位零匹配→点击测试：06:09:31 WARNING「未命中任何元素」。
5. 选择多匹配→点击测试：06:09:37 显示「命中 4 个元素」，文本明确「Mock 定位结果（未查询网页）」。
6. 选择服务失败→点击测试：06:09:43 ERROR「Mock 定位失败（显式服务失败场景，未查询网页）」。
7. 恢复单匹配→点击测试：06:10:12 显示「命中 1 个元素」。以上各步字段均保留 #submit，没有自动写回或保存。

页面沿用源成功日志的「已在页面高亮」文案，但本次接口没有操作真实网页；不计真实定位/高亮/iframe 或正式 Electron 验收通过。
