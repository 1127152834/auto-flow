# F3.3 正式窗口结果与诊断验收

2026-09-15，macOS arm64，Electron 41.10.3；本轮为构建 HTML 正式窗口 + 浏览器本地持久 Mock 合同，不是实际网页提取。

测试工作区沿用 F5 独立临时环境，user-data-dir 为 `/var/folders/8g/sq3srr71063c083rpkd32k380000gn/T/autoflow-studio-native-9g9x073c`。从主窗口“总览→工作流工作台”打开 `out/renderer/studio.html?view=automation-studio`；未操作 Store、未调用页面内部函数、未注入 CDP。

实际操作与可核对结果：

1. 通过画布右键“快速选择模块→提取数据”添加节点，名称设置为“结果诊断正式窗口验收”；选择器 `#result`、结果列“验收值”，点击保存。可见保存成功日志。
2. F5 启动 Mock，运行 `51249e1b-91ba-424a-99b8-25b97a747c31` 完成；日志明确写“未执行网页动作”。结果表显示 1/1，点击 value 单元格打开只读详情，节点 `JhCBQwf-zqVqYVgk-p-Lv`、执行 `51249e1b-91ba-424a-99b8-25b97a747c31:1`，值 `"Mock result"`。
3. 点击“导出本次运行结果”，原生 macOS 保存框显示按 runId 命名的 JSONL；保存后从磁盘读取，255 字节，运行/节点/执行标识及完整类型值正确。原始文件复制在同目录。
4. “更多操作→变量追踪”首次显示该运行 0 条，未拿其他运行充数。在全局变量 UI 新增 `diagnostic_input="正式窗口诊断初值"`，保存确认覆盖，再运行。第二个 runId 为 `e1c5d37d-d875-4501-8977-356c40b2729f`。
5. 变量追踪绑定第二个运行，显示 1 条 create/string、来源 `[Mock] Initial values`、可核对中文初值。点击导出 JSONL，经原生保存框保存；磁盘文件356字节，序号1及完整中文值正确，同目录保留原始导出。
6. Cmd+Q 正常退出，进程退出码0。使用相同隔离目录重启，从主窗口再次进入 Studio：历史日志可读；数据表格显示第二轮 1/1 和原 nodeId。变量追踪点击“读取运行历史”，显式选择第二个运行，再次显示原初值。未重放执行。

界面测试中发现：历史结果已读而标签仍用当前草稿数据行数显示0。已移除历史模式的错误本地计数，保留结果区服务总数；该末尾小修复由组件回归验证，新构建需在 F6 最终入口复核。原表格 readonly 模式保留空“操作”列属于非阻塞布局差异，登记于当前台账，不在此扩展改造。

1000条诊断/大值/分页/故障通过组件和实际HTTP测试，不声称在这个原生窗口手动点满1000次。Windows、macOS Intel、分发包及真实自动化后端未在本轮验收，继续归 F6/后端账。
