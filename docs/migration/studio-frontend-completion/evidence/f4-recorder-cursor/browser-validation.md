# 录制界面验收

2026-09-14 13:03–13:06，macOS上的Codex内置浏览器，独立 http://localhost:5175/studio.html 来源；通过CUA实际点击、输入及快捷键操作。服务为Mock，不是真实浏览器采集或正式Electron。

1. 空流程中打开Mock自动化浏览器，通过录制菜单开始，Mock测试页导航至 https://example.test/，输入“录制确认测试”，点击提交。录制列表显示打开网页、输入、点击，共3步。
2. 接口场景选择服务离线，再停止。显示“停止录制异常：Error: Mock network offline”，保留3步及停止录制按钮。恢复连接后再停止，仍3步，按钮改为开始录制。
3. 关闭自动等待后生成：画布3节点2连线；焦点在场景按钮时Cmd+Z无动作，点击画布节点后Cmd+Z一次回0节点，Cmd+Shift+Z恢复3节点2连线。快捷键作用域保护符合当前实现。
4. 命名“F4 录制游标与重连验收”，Cmd+S；13:05:17界面确认保存.json。通过浏览器面板关闭Mock浏览器，13:05:32日志确认关闭。
5. 刷新Studio，从文件菜单打开上述流程：恢复3节点2连线。输入节点属性为选择器#name、输入文本“录制确认测试”、清空原内容开启。

没有调用Store或页面内部函数来完成上述操作；没有注入真实账号数据。录制已停止，Mock浏览器已关闭，保存的验收流程保留。未执行真实网页回放、正式宿主退出或工作区切换。
