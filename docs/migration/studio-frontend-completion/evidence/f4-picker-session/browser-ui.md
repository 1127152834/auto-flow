# 浏览器界面证据

日期：2026-09-15，Asia/Shanghai。入口 `http://127.0.0.1:5175/studio.html`，内置浏览器；页面明确显示 Mock 接口提示。

1. 01:43，展开网页元素交互，将「点击元素」拖入画布；模块数量由 0 变为 1，属性面板显示选择器必填提示。
2. 点击「可视化选择元素」和「启动选择器」；显示「停止选择」，日志确认已启动。
3. 展开 Mock 测试页并点击「提交测试按钮」；属性选择器变为 `#submit`，画布显示同值，拾取按钮恢复，日志显示已选择与已复制。
4. 点击「测试定位」；日志显示命中 1 个元素，摘要明确为「Mock 定位结果（未查询网页）」。
5. 名称改为「F4 拾取会话验收」，点击保存；01:43:49 日志确认保存为同名 JSON，工具栏恢复空闲状态。
6. 刷新页面，点击打开，从文件列表选择刚保存的流程；01:44:56 显示名称恢复、模块数量 1、点击元素及 `#submit`，日志确认打开。

核心操作通过可见按钮、输入、拖放完成，没有直接写 Store 或调用页面内部函数。这是前端 UI 与 Mock 保存协议验收，非真实浏览器自动化、非原生 Electron，未覆盖 Windows。
