# IAB 前端实操证据

2026-09-14，http://localhost:5175/studio.html，专用测试标签 7。
通过动作库搜索“等待”，拖入两个固定等待节点，点击首节点断点，流程名称输入“F3 暂停身份与单步确认”。从运行菜单启动，11:54:26 显示“断点暂停 @ 固定等待”。
实际点击一次单步：11:54:33 日志出现“[Mock] 第 1 次调度：已模拟 固定等待 的事件；未执行网页动作”，随后显示“单步暂停 @ 固定等待”。
点击停止：11:54:50 显示“执行已停止，共执行 1 个节点，失败 0 个”，调试条消失。点击保存：11:55:10 显示“工作流已保存: F3 暂停身份与单步确认.json”。
全程使用拖拽、按钮和输入，没有修改 Store 或调用页面内部函数。未连线的两节点使用 Mock 当前顺序语义；不证明真实控制图编译。命令身份、重复提交及丢响应由另列 HTTP/SSE 自动化用例覆盖。本记录不算正式 Electron 或真实浏览器自动化通过。
