# 凭据协议浏览器回归

2026-09-14，CUA 浏览器 2 / tab 7，独立 http://localhost:5175/studio.html；全部通过可见界面操作，没有直接修改 Store。

1. 更多操作 → 全局配置 → 凭据库，显示 Mock 说明及本任务创建的「F5 凭据确认验收」。
2. 编辑，将说明从「仅用于 Mock 元数据验收」改为「Mock 协议更新回显验收」。没有输入、读取或发送真实凭据。
3. 点击保存，编辑表单关闭，列表出现新说明，value 仍显示 •••••• (Mock)。
4. 点击刷新，说明仍为新值；点击完成保存关闭全局配置，回到空闲画布。

这是内存 Mock 的浏览器交互回归；不是加密持久化、真实凭据注入或正式 Electron 端到端证据。特殊名称、错误回执和跨连接竞态由协议/组件测试覆盖，没有将这些自动测试计作浏览器实点。
