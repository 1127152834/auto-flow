# 录制浏览器验收记录

2026-09-15，Asia/Shanghai，开发入口 `http://127.0.0.1:5175/studio.html`，明确 Mock 模式。

1. 打开自动化浏览器面板，点击打开浏览器，观察已打开状态。
2. 从录制菜单进入网页智能录制，02:08:02 开始录制成功。
3. 打开 Mock 测试页，输入「录制会话验收」，点击提交按钮。录制面板出现输入 `#name` 和点击 `#submit`，共两步。
4. 02:09:12 停止后仍保留两步，生成按钮可用。生成后画布节点数量为 2。
5. 在非输入控件焦点下使用 Cmd+Z，节点数量恢复 0；Cmd+Shift+Z 后恢复 2，并显示原选择器。
6. 命名「F4 录制来源验收」并使用 Cmd+S；02:09:58 日志确认保存同名 JSON。
7. 刷新页面，经文件菜单重新打开该流程，02:10:53 恢复节点数量 2、`#name` 和 `#submit`。
8. 按 F5，02:11:00 新运行出现 running，随后变为 completed，工作台恢复空闲。

普通指针点击被宿主的 `codex-browser-sidebar-comments-root` 空透明层拦截（DOM 只读检查显示全视口、pointer-events:auto、z-index:2147483647）。后续使用可访问按钮操作和键盘完成；没有修改遮挡层、Store 或调用业务内部函数。该运行只验证 Mock 协议，不执行真实网页操作，也不是原生 Electron 验收。
