# JavaScript 浏览器交互证据

2026-09-14，Codex IAB tab7，http://localhost:5175/studio.html。实际点击/拖拽/键盘输入，未直接修改Store或调用页面内部函数。

1. 从模块库拖入JS脚本，文档命名F3 脚本回传验收；07:22:35首次保存。编辑 main(vars) 返回 {total:[1,2,3].reduce((a,b)=>a+b,0),text:"前端脚本验收"}；07:23:58.807覆盖本次自建文件。已有其它15份文件保留。
2. 首次编辑器测试发现未初始化输出变量导致非JSON错误，修复输入收集后重新打开文档07:25:22，编辑器测试真实显示total=6及中文文本。
3. 在代码编辑器中临时输入访问document.title，测试明确显示document is not defined；输入无限循环，测试按钮显示正在测试，点击关闭后编辑器退出、主UI可继续操作。临时代码没有保存。
4. 07:27:39运行保存的计算脚本；07:27:40日志显示已确认前端脚本结果与执行1节点失败0。诊断初次仅显示初值，作为配套缺口补齐。
5. 07:31:42再次打开保存文档，07:31:58运行完成；更多操作→变量追踪，实际显示js_result创建、类型object、[Mock]前端脚本返回值、新值{total:6,text:"前端脚本验收"}。普通草稿变量初值保持未赋值。

计算由真实Web Worker执行；运行协调、领取、回传与变量追踪使用Mock服务。未执行网页动作，不代表CloakBrowser、正式Electron、macOS Intel或Windows实机验收。构建产物包含独立js-script.worker文件，开发页HMR重装不计作普通服务恢复。
