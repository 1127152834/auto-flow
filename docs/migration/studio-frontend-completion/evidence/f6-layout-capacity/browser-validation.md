# 实际浏览器操作证据

2026-09-14，CUA tab7；独立 localhost Mock。默认357px，临时768/1440px，最后恢复默认并保留测试页。

工具栏：按只读 DOM 几何核对边界。修复后357px所有按钮右界≤345px；768px未发现越界；1440px仍48px高。真实点击窄窗口录制菜单可显示“网页智能录制”，Esc关闭。

容量：文件操作→导入整包→系统文件选择器载入 capacity-500.bundle.json。等待“整包已导入（还原模块0个、图片0张）”。画布计数500。切模块条显示500步。实际行属性0499/0500经DOM核实；先前通过同名文本定位会误命中配置标题，因此最终按实际行属性限定文字目标。

标准窗口选择0500，属性值容量标记0500；编辑为“容量标记 0500：界面编辑已确认”，点击保存，日志确认同名JSON。刷新应用→打开→搜索“F6 500 节点容量验收”，列表25个测试文件中的1个；点击打开，模块条行计数500，0500属性保持编辑值。

窄模块条初次复测曾误开出错处理弹窗，已修复内部排列和底部遮挡；之后按实际0499/0500行依次点击，DOM选中行分别为capacity-node-0499、capacity-node-0500，出错处理弹窗数量0。未把初次错误计为通过。

恢复视口时两次只读header evaluate超时；截图仍显示应用，文件菜单打开/关闭正常。性能指标尚未完整测量。导出选择弹窗曾打开后取消，未声称JSON下载验证通过。
