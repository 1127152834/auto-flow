# F5.3 配套配置与 AI 核销

日期：2026-09-15

## 结论

- AI 助手权限链已闭合：智能审批、完全控制、自动批准、拒绝、关闭取消、并发请求保护及工具总开关均通过真实组件与 Store 验证。
- AI 模型配置已闭合：配置新增、修改、默认项、删除、能力标签、场景路由、失败切换、连接成功与失败回执均通过设置界面验证。
- 凭据字段、MCP、WebDAV、留存策略、连接绑定与传输错误恢复沿用已有专项证据，并在本批统一回归中再次通过。
- 修复模型温度为 `0` 时被错误回退到 `0.7` 的问题；显式零值现在可以保存和恢复。

## 证据边界

本批使用真实 React 组件、Store、API 适配层和本地 HTTP/Mock 合同验证前端业务。Mock 结果只证明服务消费、错误处理与恢复链路，不代表真实模型调用、MCP 进程、WebDAV 存储或秘密存储后端已完成。

凭据字段专项见 `../f5-credential-fields/`，MCP 专项见 `../f5-mcp-contract/`、`../f5-mcp-save/`、`../f5-mcp-leave/`，WebDAV 见 `../f5-webdav/`，留存策略见 `../f5-retention/`。

## 检查结果

- F5.3 关联回归：27 个测试文件，388/388 通过。
- 新增 AI 权限及模型设置：2 个测试文件，9/9 通过。
- TypeScript：通过。
- ESLint（本批文件）：通过。
- OpenAPI 一致性：通过。
- Electron main/preload/renderer 构建：通过。

原生秘密存储、真实模型/MCP/WebDAV 后端和平台实机结果继续列入后端及 F6 交接，不以本批 Mock 验收替代。
