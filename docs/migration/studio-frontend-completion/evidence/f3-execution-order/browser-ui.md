# 确定轨迹的浏览器交互证据

2026-09-14，独立 IAB 标签页，localhost:5175/studio.html，Mock 服务。通过文件菜单/文件选择器导入 browser-fixture.json；画布显示五节点与真假两路连线。点击「重复提取验收」的设置断点按钮；接口场景输入 open,condition,true,extract,extract 并应用下次轨迹。所有动作经 UI 完成，没有写 Store 或调用内部函数。

03:46:51 从运行菜单启动。日志出现打开页面、条件入口、选中分支三次调度；提取前显示「断点暂停 @ 重复提取验收」。点击单步，03:47:03 产生第一次提取；界面显示「单步暂停 @ 重复提取验收」，数据页仅1条，index=4。点击继续，03:47:16 完成第二次提取；数据页保留两条 extract 结果，index=4/5。日志区确认五次调度，没有未选分支执行消息。最终状态退出运行中，草稿仍未保存，未自动保存文件。

这是已配置轨迹的前端/Mock验收，没有计算条件表达式或真实循环，没有执行网页动作，也不是正式Electron。没有验证刷新后运行历史持久化。
