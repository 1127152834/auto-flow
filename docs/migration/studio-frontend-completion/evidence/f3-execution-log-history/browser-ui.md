# 浏览器 UI 证据

日期：2026-09-14；页面：`http://localhost:5175/studio.html`；传输：页面明确标识的 Mock 接口。

1. 展开“接口场景”，点击“日志容量：1万条”。日志栏选择“一万条日志验收”，显示 `100 / 10000`，列表从序号 9901 展示至 10000。
2. 点击“更早日志”，显示 `200 / 10000`。
3. 在搜索框输入 `容量故障-09901`，服务筛选后显示 `1 / 1` 及唯一匹配；清空后恢复 `100 / 10000`。
4. 重新导航到同一 Studio URL，未重新生成数据；页面自动恢复“一万条日志验收”运行选择、`100 / 10000` 和最新日志。
5. 点击“下载”并等待浏览器下载事件两次，均在 10 秒后超时。协议和组件测试已验证完整 JSONL Blob；本项不标为浏览器下载成功，也不标为正式 Electron 验收。

操作全部通过可见控件完成。此证据证明前端 UI、Store 恢复及 Mock HTTP 消费闭环，不证明真实自动化执行。
