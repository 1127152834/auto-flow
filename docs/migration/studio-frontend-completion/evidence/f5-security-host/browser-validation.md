# 安全页宿主归属：浏览器实点

2026-09-14；CUA tab 7；独立 localhost:5175 Studio。更多操作 → 全局配置 → 安全。

页面显示「访问鉴权由 AutoFlow 宿主统一管理」及复用应用服务连接的说明；旧独立令牌开关、重置、复制、输入/保存控件不再出现。关闭全局配置，标题消失。

只读验收，没有读取或更改系统令牌，没有修改宿主鉴权。正式宿主 IPC 及所有平台鉴权实测不由该页面验收替代。
