# 输入节点 UI 到服务回传验收

2026-09-14，http://localhost:5175/studio.html，内置浏览器独立测试标签，明确标注 Mock 服务。

1. 文件菜单导入整包。第一次选用普通工作流 JSON，被正确拒绝为 HTTP400 Invalid workflow bundle；修正测试夹具为 webrpa-workflow-bundle 后，04:55:44 成功导入两个节点。未放宽导入校验。
2. 从工具栏运行菜单启动。04:55:57 请求被接受；随后出现“输入回传验收”弹窗，默认原始值；此时后续提取尚无结果。
3. 在实际输入框填“真实界面回传值”并点击确定。04:56:21 日志确认 [Mock] 已接收输入结果。
4. 04:56:22 才出现第二次调度的提取日志，然后运行完成（2节点、0失败），数据表格计数1。
5. 打开全局变量页，编辑草稿的 answer 仍为 original。该面板是草稿变量，不是运行变量查看器；本次运行变量值由 HTTP 协议测试查询确认，不冒称本UI显示了运行值。

导入、运行、输入、提交和日志检查均通过真实UI。没有写Store或调用页面内部方法。后续提取是受控模拟结果，未操作真实网页。05:00:05 通过 Cmd+S 保存 F3 输入回传验收.json；验收标签保留用于后续工作。
