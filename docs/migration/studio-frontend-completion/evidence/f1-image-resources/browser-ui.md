# 图像上传与预览界面证据

2026-09-14，独立 IAB 标签页 7，localhost:5175/studio.html，Mock。通过图像页签→上传图像→真实文件选择器提交本目录 preview-fixture.png 的 32×32 蓝色 PNG。首次提示成功0/失败1；追踪冻结源码 image_assets.py:157 和现有三个上传消费者，确认 Mock 缺少 asset 包装。

修复后重新上传：列表数量由1变2，没有失败弹窗，出现第二张缩略图。点击后浮层展示蓝色 PNG。截图观察确认像素实际解码，不仅检查 img 标签存在。未修改 Store 或调用内部页面函数。首张是首次尝试已写入 Mock 的同一测试图，保留用于说明误报失败。

该 UI 验证使用内联 Mock 图片；服务图片的鉴权传输、字节读取与释放由组件/HTTP 测试覆盖。未声称这条 UI 用例验证真实后端或正式 Electron。
