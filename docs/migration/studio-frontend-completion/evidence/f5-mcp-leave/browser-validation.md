# MCP 编辑离开：浏览器实点

2026-09-14；CUA tab 7；独立 localhost:5175 Studio；内存 Mock。

1. 全局配置 → MCP，原测试项 mcp-ui-check 为已禁用，已连接 0 / 工具 0。
2. 编辑，工作目录从空白改为 /tmp/studio-mcp-leave-fixture；点击关闭 MCP 编辑，显示保存 MCP 编辑三选确认，原字段禁用。
3. 取消，字段恢复可编辑。再次关闭 → 保存后继续，编辑层关闭，列表提示已保存，服务器仍禁用。
4. 再次编辑，目录回显测试值；清空目录并普通保存，编辑层关闭。关闭全局配置，无多余草稿提示。

没有启动命令或远程服务，没有更改自动批准工具、服务器启用状态或真实凭据。仅 Mock 配置编辑回归，不计作真实 MCP 连接或正式 Electron 验收。
