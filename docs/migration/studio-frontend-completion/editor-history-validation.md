# 编辑历史回归

日期：2026-09-13；状态：拖动/尺寸局部验收通过，变量历史待补。

先添加连续拖动与无标记位置修改测试：修复前 2 失败/4 通过，失败分别是拖动没有标脏、键盘移动没有标脏。修复改为首个手势事件前记录，手势进行中不重复记录。增加显式尺寸连续事件测试。完整前端 91 文件/1,110 用例通过。

真实浏览器 UI 验收：打开此前保存的“F1 录制恢复验收”；通过鼠标拖拽移动“输入文本”。标签可见坐标从(519,238)到(563,273)，Cmd+Z恢复(519,238)，Cmd+Shift+Z恢复(563,273)。这是 DOM 可见位置测量，不读取 Store，不伪造用户操作。当前为浏览器 UI+Mock，非原生 Electron 验收。

未覆盖：分组及多选拖动全部组合、所有尺寸控件、变量原子编辑、全部快捷键。具体三条规则用例登记于 verified-cases.json；日志随本次提交。
