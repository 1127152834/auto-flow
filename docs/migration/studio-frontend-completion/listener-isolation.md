# F1 事件订阅者异常隔离

2026-09-14；同一个事件逐个调用已登记订阅者，订阅者抛错单独记入诊断日志，不伪装成传输断线，不阻止后续订阅者，不导致同一事件反复重放。命令结果回调抛错也不能被误判为命令 HTTP 响应丢失。原始 payload 不附加写入异常日志。

先复现 3 项失败，修复后连同序号补读、命令恢复共 9 项通过；内存和实际本地 HTTP/SSE 使用相同断言。类型、lint、构建通过。证据：evidence/f1-listener-isolation。

异常订阅者自身的 UI 状态仍需要通过错误修复和状态重读恢复；不能说忽略错误就保证该消费者已成功应用事件。服务重启 epoch、全量运行状态恢复仍属后续工作，F1/F3 未关闭。
