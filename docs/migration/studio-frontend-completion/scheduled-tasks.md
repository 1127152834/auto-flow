# 保留计划任务配套：实现与验收

日期：2026-09-13。状态：局部已实现、持续验收；不代表 F0/F1/F5 关闭。

## 调用链与边界

Toolbar/AI open_scheduled_tasks → ScheduledTasksDialog → 源任务/创建/编辑/日志/统计组件 → scheduledTaskStore → 既有 scheduledTaskApi → StudioTransport。

八个源文件的路径及 checksum 已登记 source-manifest.json。沿用原布局与时间/热键/启动/Webhook 配置，不新建调度体系。内存协议夹具通过已有运行协调器模拟手动执行，保留配置和日志；不会安装真实定时器、系统热键、启动任务、Webhook 服务，也不发送通知。通知凭据值不持久化。任务引用的排除节点仍阻止启动。

修复源 Store 将 success:false 当成功、列表不展示操作错误、自愈开关先显示成功而忽略失败的问题。自愈仅维护工作流 selfHeal 元数据；执行自愈属于后端，当前没有真实修复动作。普通保存保留该元数据。

## 逐项记录

| ID | 前置数据、用户操作 | 预期界面 / 请求 / 状态 | 层级及证据 |
|---|---|---|---|
| TASK-001 | 保存 scheduled.json；创建每日任务，编辑名称、禁用、重读、统计、删除 | API CRUD 成功；Store 与持久数据一致；删除后 GET404 | scheduled-tasks.test.ts 第一个用例；f0-scheduled-tests.txt |
| TASK-002 | 已有任务；离线删除，再提交空名称 | 删除失败保留任务；Store.error；空名称422 | 同文件第二个用例 |
| TASK-003 | 任务关联一节点流程；执行两次、删除、停止、查询日志 | 第二次及活跃删除409；停止后非运行态；日志 stopped | 同文件第三个用例；模拟执行不等于网页动作 |
| TASK-004 | 关联不存在文件；执行 |404；不留活跃任务或虚构日志 | 同文件第四个用例 |
| TASK-005 | scheduled.json；设置 selfHeal，普通保存，再读取；改不存在文件 | 元数据保留；缺文件404 | 同文件第五个用例 |
| TASK-006 | 打开空创建表单；点击创建 | 提示请输入任务名称；表单不关闭、任务为空 | scheduled-tasks-ui.test.ts 第一个用例 |
| TASK-007 | 关联不存在文件；列表点击执行 | role=alert 显示工作流不存在；原任务保留；无停止按钮 | 同文件第二个用例 |
| TASK-008.memory/http | 创建启动任务、列表、执行缺失文件、日志、删除 |200/404真实状态；无错误运行记录 | transport-parity.test.ts；两个传输相同断言 |
| TASK-UI-001 | localhost 隔离数据已有 F0 离线节点草稿；真实点击计划任务→创建→填写 F0 计划任务往返→关联草稿→创建 | 弹窗关闭；列表1个、每天08:00；未改画布 | CUA 实际浏览器 UI，2026-09-13 本任务工具记录 |
| TASK-UI-002 | 上述任务包含排除节点；点击执行 | 422 明确显示；无成功计数/运行态 | CUA；首次发现源列表漏错误，修复后实测显示 HTTP422 工作流包含已排除节点 |
| TASK-UI-003 | 重载页面→计划任务→编辑→改名 F0 计划任务已编辑→启动触发→5秒→保存 | 重载任务存在；保存后名称和“启动后5秒”正确 | CUA；原生Electron未据此通过 |

## 尚待验证

五种时间表单全部分支、输入/快捷键录入取消清理、通知配置、监控页面宿主行为、刷新和并发编辑竞态、服务重启中的活跃任务恢复、长日志容量、全部离开矩阵仍未验收。任务级服务契约继续并入 F1，不把当前局部 Mock 当生产调度器。
