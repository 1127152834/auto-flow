# 自动化浏览器面板命令串行化

AutoBrowserDialog内打开/关闭/导航/启动拾取/停止拾取共用一个在途命令标记；处理期间禁用其它变更按钮和刷新。命令开始即作废更早的状态读取，避免旧状态覆盖正在变化的资源。拒绝/异常后恢复按钮，保留最后确认状态及已有错误提示。

新增3组件场景，分别让启动/停止/导航等待，重复点击并尝试关闭，断言只发一次原命令且不插入关闭；拒绝后控件恢复。首次3失败9已有通过，修复后与拾取停止共17通过。新增HTTP交互1项，从React按钮通过实际本地服务保持请求在途，不直接调用内部UI函数；验证启动和关闭不交错，启动确认后可停止。测试Promise因动态method推断unknown引发类型失败，补显式结果类型，未削弱断言。

完整回归144文件1725项通过；另新增HTTP1项通过。类型/lint、构建及21脚本通过，证据evidence/f4-browser-commands。没有宣称正式ElectronE2E或真实自动化浏览器执行通过。

剩余：跨入口会话所有权、稳定sessionId/commandId和重启后的命令恢复仍待合同批次；本面板串行化不等同后台资源全局互斥。原生状态/登录持久语义以将来真实服务验收为准，Mock不提供这些效果。
