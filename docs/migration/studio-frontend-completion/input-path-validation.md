# F2/F3 输入路径选择配套工具

2026-09-14。保留input_prompt已有文件/目录选择模式，不恢复已排除的文件处理节点。

组件统一复用systemApi.selectFile/selectFolder。结果绑定原请求和本次选择序号，期间切换/关闭弹窗、提交输入、手工修改路径时，迟到结果不再覆盖。浏览按钮显示选择中并禁用重复打开，输入框仍可编辑；服务失败显示错误，保留草稿；路径必须是字符串，空路径保持原值。

对照reference/WebRPA/backend/app/api/system_dialog.py：源取消包为success:false,path:null,message:"用户取消选择"。API适配只对两个路径选择接口、无error且精确匹配该包时识别为取消；真正error优先报错。没有把所有success:false泛化为成功。

新增18项组件/传输用例：文件和目录各验证换请求、手工编辑、HTTP失败、成功与忙碌、空选择、无效路径、提交后迟到、源取消包、携带error的取消文案。首批6项修复前全部失败。改用异步公共API后测试显式等待传输请求建立，避免使用前一次用例的resolver；原断言保留。

相关4文件68项通过，TypeScript与ESLint、renderer/main/preload构建、21项脚本检查通过。完整前端1592项是上一批输入回执代码的全量基线，本批只报告针对性回归，不伪称新增18项已经跑过全量。

原生系统选择器/正式Electron入口尚未实测；本批验证组件和受控传输，F2/F3继续进行。
