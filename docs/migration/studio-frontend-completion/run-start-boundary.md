# F3 启动请求边界

- 日期：2026-09-14；状态：本批 confirmed，F3 未完成。
- 所有工具栏运行、快捷键、AI 运行入口与 run-from-node 共用 executeWorkflow。

准备请求在途时拒绝重复进入；已有 running 状态也不再次启动。断点数组与单步模式在第一次异步等待前捕获。无论成功或失败都释放本次准备标记。HTTP 接受只显示“启动请求已接受”，不将已完成/停止/失败等状态改回 running；执行状态由原 SSE 事件消费更新。

先复现 5 失败；扩展 7 项新组件用例，与草稿保护共 36 项通过。类型检查、lint、构建通过；见 evidence/f3-start-boundary。测试通过 F5 和 run-from-node 入口触发组件，用 spy 模拟请求响应和 Store 已确认状态，不宣称真实执行或正式 E2E。

尚未关闭：runId/文档身份分离、HTTP 接受后 SSE 尚未到达的状态查询、启动响应丢失幂等恢复、跨文档事件过滤和宿主启动取消。当前标记仅覆盖准备请求生命周期，不能替代后端幂等与资源互斥合同。
