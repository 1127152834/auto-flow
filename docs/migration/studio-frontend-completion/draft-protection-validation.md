# 草稿保存与新建入口（局部）

日期：2026-09-13；状态：implemented/verified；完整F5未完成。

不再用节点数量决定草稿是否可以保存或是否应保护；仅变量、仅名称及未完成画布均可手动保存。按钮、Alt+N和AI事件入口共用新建确认；取消不清空变量。beforeunload检查也按dirty。保存捕获同一文档快照和名称，文件存在查询失败不继续写入；HTTP错误不能视为成功；旧保存响应不确认新变量修改。

测试：先前四条失败，修复后六条通过；完整94文件/1,128用例及类型/lint/构建通过。实际浏览器UI+Mock：新建only_variable=保留我，点击新建出现确认，取消后变量仍在；零节点文档保存为“F2 零节点变量草稿.json”成功。

边界：当前新建确认仍为原版放弃/取消语义；统一保存/放弃/取消及运行、拾取、录制清理、打开/导入/宿主协调仍需F5完整实现。本记录不代表完整离开矩阵通过。
