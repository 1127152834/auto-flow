# B6 网络共享正式窗口验收

`result.json` 记录 macOS arm64 开发构建的正式 Electron UI 实测。通过真实点击、输入、画布连线和保存，创建并重开两条流程；真实 worker 启动文件夹、文件及屏幕共享。独立读取文件夹列表、两个原文下载和屏幕 JPEG 帧后，从第二条流程停止共享，核对三个端口关闭。工作区、文件及端口均为临时资源；未触及用户数据库。

前两次尝试使用瞬时保存提示作为测试判据，提示消失后产生误报。改为通过只读工作流列表核对持久化内容，保留真实 UI 保存操作及节点、连线、重开断言。未修改产品代码或放宽运行、下载、JPEG、日志及清理断言。Windows、macOS Intel、正式打包入口、屏幕录制权限拒绝和第二台局域网设备仍未实测。
