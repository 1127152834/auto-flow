# 项目自动化消费 Studio 五节点（2026-09-23）

这是 AutoFlow 必要适配：复用已迁入的 WebRPA 图执行器、五节点和 CloakBrowser，不增加替代执行器。项目目录与自动化验证现在准入打开网页、输入文本、点击、提取、截图；旧四节点链协议仍兼容。Studio 完整快照原样冻结，运行不反向保存文档。

## 已核验证据

- [开发入口真实 UI](../formal-electron-A3JoOx/result.json)：项目新建→Studio 五节点配置连线→保存→原生正常关窗重开→自动化绑定文档与主应用 Profile→批次执行→输出和 PNG 查看。实际长导航停止、失败截图、修正 URL 再次成功；节点执行数量分别为 1、1、5，清理后才确认结束，无受管浏览器残留。
- 节点截图使用共享产物存储，落盘后通过原事件事务确认。SQL 回执丢失不删已提交文件；未确认路径、同路径不同 ID、符号链接及取消写入有针对性保护。结果/失败截图按用途展示，哈希与 PNG 内容核验。
- worker 保留单 stdin 线程，单异步读者持续分派控制/事件回执。停止、EOF 可中断启动和长导航；失败截图有 5 秒上限并响应停止，保留原节点错误；迟到 ACK 不跨事件。
- 宿主打开已保存流程时 Toolbar 继承保存身份，避免重复 POST 的 409；新建草稿不会沿用旧身份。专项先红后绿，正式 UI 保存修订随后确认。
- [检查汇总](result.json) 和 logs 保存当前检查及失败记录。最新关联后端 163 项、前端保存/导出/宿主 65 项、项目证据组件 18 项通过；类型、lint、OpenAPI、构建通过。不同专项有重叠，不相加成总用例数量。

## 失败与边界

首次桥接准入 3 红 1 兼容通过，修复后全通过。正式 `formal-electron-ukkrvX` 揭示宿主保存身份缺陷；`formal-electron-B8doXV` 保存已成功，但新测试忘传等待时限而立即失败，修正 helper 默认时限，保留修订增长断言。第一次只有成功链的 `formal-electron-LltZch` 不代替停止和失败验收。

[本次正式包](../formal-electron-6dbukC/result.json)复验相同完整链路通过，记录 app.asar 和冻结后端哈希；PyInstaller 构建172秒，冻结启动通过，包内reference/Mock/Vite路径检查通过。macOS arm64本地unsigned包，不表示签名公证。仅 macOS arm64 实机，Intel/Windows 未验收。仍有其余项目节点族、权限/资源、业务数据桥、已登记元数据/快捷键/启动命令缺口。本块不改节点台账622/17、不恢复14个排除通知节点。
