# 项目任务临时环境终态清理（2026-09-24）

状态：本批环境终态清理、结果截图展示与控制管道关闭已通过专项、真实浏览器、开发入口及 macOS arm64 未签名目录包验收。历史偶发中断的唯一原因未证实，保留追踪，不宣称所有停止竞态均已排除。

## 范围与依据

- 依据 `docs/project-management/design/execution-and-environment.md` 第9节：无保留意图的工作副本关闭后清理；失败保存与关联不完整现场不得自动删除。
- 来源：Firecrawl 与元素变化项目正式 UI 证据出现终态仍“待清理”。根因是 ProjectBatchScheduler 没有调用 EnvironmentService 收尾，不能通过把终态投影成已清理掩盖。
- 分类：AutoFlow必要适配。复用实例锁、现有浏览器关闭器、EnvironmentStore、停写门禁；无新执行器、迁移、清理框架或浏览器后备方案。
- 项目默认资源＋显式覆盖继续生效；14通知排除不变。没有访问或修改真实用户数据库。

## 实际变化

- 从终态核心运行和实例持久记录识别一次性工作副本，已关闭批次在调度器恢复时也能处理；待核验运行、人工处理、维护实例和保存现场不自动清理。
- 保存、End、人工处理与自动清理复用实例锁；清理前再次核验。新保存命令登记实例身份和保留意图，旧命令归属未知时保守保留。
- 磁盘删除完成后才释放持久环境占用并记录 cleaned；失败保留占用和 cleanup_failed，后续轮询可重试，不覆盖源版本。
- 任务终态后继续刷新未完成/失败的清理；已清理现场不再显示有效的保留按钮。迟到人工请求拒绝重建已清理现场。
- 结果截图按实际产物 purpose 标题展示，不再称为失败截图；混合截图使用中性标题。

## 已运行验证

- `backend-regression.log`：142项相关合同、环境、调度、生命周期及恢复通过。后追加迟到人工保护后 `retention-final.log`：37项通过；集合重叠，不相加宣称覆盖数量。
- `real-browser.log`：真实CloakBrowser成功、元素等待超时、等待中停止3项通过。HTTP状态、真实工作副本目录、worker、运行占用均独立核对；日志/PNG读取仍通过。
- `frontend.log`：21文件164项通过，覆盖终态清理刷新、失败后恢复、清理后入口、结果截图标题与原关联修复。
- 原调度容量失败用例使用 browser=none，无法触发环境预留，专项复现后将该用例资源改为实际需要浏览器；保留全部回滚断言，无放宽。
- 正式开发入口 W7ZeTp 已通过成功与UI停止后的清理数据库、目录、进程、页面断言；uoNDah 已通过结果截图正确标题和已清理状态。随后发现并补齐已清理任务保留按钮保护，最终入口结果另记。

## 边界

- 仅独立临时工作区，macOS arm64。Windows/macOS Intel实机与签名发布包未验收。
- 旧保存命令缺实例身份时不猜测清理对象；需要人工核验，未修改历史数据库或伪造清理成功。
- 该批只关闭共享环境清理和截图展示缺口，不将剩余节点、模型、外部服务或硬件验收标为完成。

## 最后并发保护与未通过尝试

- 慢清理移出批次停止准入锁；核心仍有活跃运行时暂缓旧实例清理，保留停止调度响应。两项测试先失败，修复后与数据/参数调度67项通过，见 stop-admission-regression.log。
- 已保存但未关联结果只能修复关联，不能重复End；保留原修复链，新增禁用断言先失败后通过。
- iVkaGt 正式开发重跑：成功任务清理通过，停止任务返回 interrupted，不满足 cancelled 断言；失败现场保留，原因尚未从当次记录确定。不是通过证据，不修改断言；后续单独重跑并保存完整任务错误信息。
- 构建缓存只失效生成的 PYZ-00.toc，以重新编译最终模块；不删除源码、历史证据、数据库或其他任务构建。正式包使用前必须比对包内方法字节码与当前源码。

## 停止异常调查（继续中）

- Ie6Ngs 最终目录包再次失败：任务 interrupted / WORKFLOW_RESULT_UNKNOWN；进程及环境已清理，但这不满足 cancelled。`task-stop.json` 保留错误和节点尝试。不能计作正式通过。
- 源码及冻结 worker API 的立即停止、延迟0.75秒停止通过；三次真实UI诊断重跑也通过，尚未捕获原始异常。因此不将机器负载推断为已证实原因。
- 单独稳定复现一个共享协议缺陷：收到 finished 后，父进程未关闭控制管道就等待退出；等待 EOF 的子进程超时。`worker-eof-red.log` 为失败证据。已改为收到合法终态包后关闭 stdin，再保持原退出码检查与进程树清理；不把未知结果改成 cancelled，不延长超时。
- 相关回归暴露旧调度夹具缺少 chain/v1.nodes，模型绑定阶段即 KeyError、未进入被测调度逻辑。补齐真实计划字段与完整预期比较，保留原调度、取消、失败和清理断言。四文件91项通过（集合与前述回归重叠）。
- 临时UI诊断只记录异常类型/协议阶段，不改变业务结果；诊断模块已移除，不作为无插桩正式包完成证据。最终源码和目录包需使用修复后版本重新验收。

## 最终验收（2026-09-24）

- 源码：`real-browser-final.log` 3项真实CloakBrowser成功、超时、延迟停止通过，59.68秒；HTTP/SQLite、实际目录、worker和占用事实均核对。`worker-eof-final.log` 91项、`recovery-final.log` 9项通过；有重叠场景，不与历史计数相加。
- 开发正式UI：[cWgIdZ](../formal-project-element-change-electron-cWgIdZ/result.json)；最终目录包：[jYk8Qm](../formal-project-element-change-electron-jYk8Qm/result.json)。两入口均真实编排、保存、正常关窗、项目默认Profile启动、输出及日志、等待中UI停止；取消终态、无后继输出、实际工作副本删除、页面已清理且无保留按钮通过。
- 目录包结果截图：[m1k6Sk](../formal-project-firecrawl-electron-m1k6Sk/result.json)。三类真实抓取结果和PNG保留，任务已清理；截图人工复核为“节点输出截图”，不误称失败。
- `frozen-code-verification.txt` 对照最终包内5个方法和当前源码；`package-sha256.txt` 登记最终backend/app.asar。`freeze-final.log` 172.166秒；`package-final.log` 未签名macOS arm64目录包成功。TypeScript/前端lint/OpenAPI/结构和前端构建使用本批相同前端源码的已通过记录，未反复重建。`ruff-final.log` / `mypy-final.log` 最后后端检查通过。
- iVkaGt、Ie6Ngs两次旧版本中断仍是真实失败记录；修复后两个正式入口通过，EOF缺陷有确定性红绿证据，但原先两次没有底层异常栈，不能把因果关联写成完全确定。若复现，沿当前保留的协议/终态证据继续定位，不放宽cancelled门槛。
- 未测：Windows、macOS Intel、签名发布包。本批没有修改主应用用户数据库、没有恢复排除节点，不关闭其他节点/模型/外部依赖验收。
