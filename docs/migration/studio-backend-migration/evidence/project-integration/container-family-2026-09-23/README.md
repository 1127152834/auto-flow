# 项目任务基础列表／字典节点族（2026-09-23）

- 范围：`list_operation`、`list_get`、`list_length`、`dict_operation`、`dict_get`、`dict_keys`。六节点均在批准的 213 节点范围内，执行器已从冻结 WebRPA 迁入 Studio；本批只接入项目任务的运行目录，不重写算法。
- 真实 worker：一条六节点流程依次追加列表、取值、计算长度、设置字典键、取值和列出键；输出及六次成功节点事件均落库，没有申请浏览器资源，worker 清理完成。与既有八节点文本族共用参数化用例 `test_project_task_executes_pure_data_family_in_real_worker`，两族连续三轮通过。
- 冻结源码差分及关联回归：数据结构执行器差分和项目真实 worker／启动／图执行器共 194 项通过；修改文件 Ruff、生产目录 mypy、OpenAPI 一致性通过。项目任务可运行目录由 65 增至 71 个入口，Studio 批准范围仍为 213 个。
- 测试曾以人为缩短的 10 秒 worker 启动预算在两族均失败，异常为启动 `TimeoutError`，没有任何节点事件。恢复生产默认 90 秒预算后连续三轮通过；失败不计作节点语义问题，也未放宽运行终态、输出或清理断言。
- 边界：`list_export` 需要项目任务尚未接通的产物登记口，继续不开放。六节点项目正式 Electron UI 和最新冻结包尚未验收；本记录不将真实 worker 测试冒充正式窗口通过。其他节点族与全部 213 节点的项目任务接入状态不随本条关闭。
