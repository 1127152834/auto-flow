# 小圆角手动验收

本文件是待用户执行的步骤，不能视作用户已通过。工作区 `/Users/zhangtiancheng/Documents/projects/autoflow-table-system`，主目录未合并。

1. 在隔离版本进入“项目”，点击“新建项目”。比较按钮、名称输入、描述输入、背景卡片和弹窗转角：接近直角但可见4/6px弧度。焦点环随输入轮廓，无裁切。
2. 按Escape关闭，焦点回到“新建项目”；输入内容后关闭仍按既有未保存保护运行。
3. 浏览器配置、内核弹窗、代理/代理池、模型、设置逐页检查卡片、菜单和输入；文字标签呈小圆角，状态点依旧圆形。
4. 记录表的组合搜索框保持中间直边、外侧4px；表格容器6px；下拉展开不撑宽页面。
5. 打开Studio，检查配置面板、节点、AI消息、下拉菜单及对话框；端口仍为圆形，选中边框和连接不受影响。
6. 使用应用缩放200%，确认输入焦点环、弹窗内部滚动和底部按钮正常。

每项记录通过/失败/未执行，附截图、版本和复现步骤。Windows与打包测试未执行。

自动重现：根目录运行 `npm run build`，再运行 `node scripts/qa-radius-system.mjs`；它只创建带`.radius-qa.json`标记的临时工作区，完成后关闭本次进程并保留截图及目录。删除前确认报告的workspace路径和标记，只删除该次临时目录。
