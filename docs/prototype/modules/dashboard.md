# 总览原型

## 主流程
默认进入总览，展示浏览器配置、健康代理、已安装内核三个指标，以及最近活动和 sidecar 状态。指标点击后跳转对应模块并保留筛选上下文。

## 状态
- 加载：指标区域显示 Skeleton，活动区域保持标题和占位行。
- 空态：没有活动时显示“还没有活动记录”和进入配置的主按钮。
- 错误：sidecar 不可用时显示 ErrorState，按钮为“重连”和“查看设置”。
- 部分失败：单个指标显示“暂不可用”，不阻塞其他指标。

## 动效和提示
指标数值 180ms 淡入；刷新操作 300ms 旋转图标；健康状态更新后显示 Toast“状态已刷新”。
