# 设置原型

## 标签页
本地服务、数据与工作区、偏好设置使用 Tabs；切换标签不丢失未保存字段，离开设置页才触发未保存确认。

## 本地服务
显示运行状态、端口、版本、最近心跳；重启使用 AlertDialog，确认后展示“重启中”，恢复后显示“服务已恢复”。断线时顶栏状态变为警告并提供重连。

## 文件和诊断
路径只读展示规范化路径；打开目录调用主进程能力；导出诊断显示脱敏范围和生成进度。清理缓存需明确列出将删除的内容。
