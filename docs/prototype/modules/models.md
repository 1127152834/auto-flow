# 模型管理原型

## 主流程
供应商列表显示类型、Base URL、模型数量和连接状态；模型目录可启用/禁用并查看能力标签。连接测试显示延迟、状态码摘要和下一步建议。

## 密钥
API Key 只写不读，编辑页面永远显示“已配置”或“未配置”，替换时重新输入。错误响应不得回显密钥。

## 交互
新增/编辑供应商共用 Dialog；删除需要确认；测试按钮使用独立任务状态，不阻塞其他供应商编辑。默认模型变更显示 Toast 并更新总览。
