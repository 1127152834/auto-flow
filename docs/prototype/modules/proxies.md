# 代理与代理池原型

## 主流程
代理列表提供协议、标签、健康状态筛选；代理池通过侧栏入口进入成员编辑。批量导入弹窗粘贴文本，点击导入后展示逐行结果。

## 健康检查
测试按钮变为 loading，展示阶段、进度和取消；成功显示延迟，失败显示可复制的失败原因与重试按钮。批量测试显示任务进度，不阻塞页面其他操作。

## 敏感信息
密码和 Token 输入使用密码控件；列表和 Toast 只显示脱敏值；诊断导出过滤认证字段。

## 空/错误态
没有代理时显示“添加代理”和“批量导入”两个动作；代理池没有成员时显示成员选择器；导入部分失败使用黄色提示区，不覆盖成功行。
