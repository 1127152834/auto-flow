# 真实 Electron 桌面观察

2026-09-24，confirmed，原生CUA accessibility操作，独立workspace见real-bulk-cleanup.json。两台自建设备，真实Lima/ReDroid；未注入UI或运行时结果。

1. 选中两台停机设备，搜索“无匹配验收”看到0/2；清除搜索后两台仍被选中。
2. 提交启动批次，目标和动作冻结；终态succeeded，逐项均“已完成”，设备均“已就绪”。
3. 点击“开始新批次”：选择清空，动作可更改。改为停止，选择两台并提交。
4. 点击“取消未开始项”：第一项显示“已准入，执行中”，第二项“已取消”。终态partially_failed：第一项“已完成”，第二项“已取消”；设备计数运行中1/已停止1。
5. 再次点击“开始新批次”：默认动作启动，两项选择清空，停机实例可选、已运行实例禁选。
6. 结束自己的Electron进程，清理两台设备，经真实归属verify确认容器和卷均为空。已有基础镜像保留。

第一次启动批次已在发出取消前完成，因此该次没有取消证据；取消证据来自第二个停止批次。清理脚本首次对已删除容器调用runtime.inspect遇到no such object；改用现有所有权verify查询容器/卷，重跑完成。此修正只涉及QA脚本，不放宽产品检查。

当前构建为磁盘观察126ac28b构建；这些验收只证明原有批量入口，不证明尚未完成的创建磁盘确认UI。
