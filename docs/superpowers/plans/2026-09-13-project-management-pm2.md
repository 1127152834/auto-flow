# PM2 修订实施计划：完成本地数据表、业务状态与 Excel 闭环

> 执行技能：writing-plans 维护执行卡；subagent-driven-development 分工，先规格审查再工程审查；verification-before-completion 核验。

- 日期：2026-09-13
- 状态：confirmed / implementing；来源：用户本轮批准完整修订计划。
- 目标：复用已提交 PM2，实现真实数据编辑、耐久批量状态和受控 Excel 导入/替换/导出。
- 架构：React 领域组件、单 FastAPI、SQLite、Electron 受控文件适配；不新增工作流执行器或通用队列。
- 工作区：autoflow-project-management-implementation；起点 8749739。
- 历史执行记录：[原执行卡](2026-09-13-project-management-pm2-prior-execution.md)。其中继续 PM3+ 的历史指令不适用于本次任务；本次完成后停在 PM2 验收点。
- 基线：[实际文件快照](../../project-management/implementation/pm2-revision-baseline.json)。PM0/PM1 历史证据不改写。

## P0 基线、契约及责任

- [x] 核对独立工作区、HEAD、已有未提交文件；禁止覆盖或代为提交原有 WIP。
- [x] 同步公共 Excel 路由/映射/操作响应契约。
- [x] 迁移从 pm02_status_tombstones 顺序增加批量状态、文件持久化；不改历史迁移。主线 0006_workflow_runs 分叉留待主线集成，不在 PM2 引入 Studio M2。

| 责任 | 修改边界 | 当前状态 |
|---|---|---|
| 原编辑任务（用户确认仍在进行） | 既有 dirty DataTableDetailPage、editing Hook 与 smoke 脚本 | 保持原样，不作本轮交付证据 |
| status_batches 智能体 | 新批状态后端领域/应用/仓储/HTTP/测试，删除引用守卫 | 实施中 |
| file_bridge 智能体 | 文件桥；随后批量状态及 Excel 向导领域组件 | 文件桥已提交 7e46835；组件正在审查 |
| excel_services 智能体 | 新 Excel 应用/仓储/HTTP/文件授权及测试 | 实施中 |
| 主协调 | 迁移、共享 Operation schema、bootstrap、preload/main 装配、生成类型、文档、前端集成 | 实施中 |

## P1 页面编辑闭环

- [ ] 接入记录新增/编辑/删除、单条设置/清空状态、字段新增/编辑、状态新增/编辑/删除及表资料。
- [ ] 草稿/版本冲突/迟到响应隔离、离开保护、焦点和错误定位；只提交修改字段。
- [ ] 保存列表查询/列显示/位置，代次变化清除失效字段引用。正常 UI 不展示内部 ID/修订/代次，不显示未实现同步假数量。
- [ ] 真实 Electron 从界面完成写操作；HTTP 预置不得替代 UI 验收。

## P2 耐久批量业务状态

- [ ] 固定显式引用和状态版本，最多1000条；每块1–100默认100；重复身份不同版本拒绝。
- [ ] Operation/批/块原子接受；块内状态、修订、事件和结果全成或全冲突，其他块继续。
- [ ] 取消仅停止未开始块；原操作 failed/BATCH_STATUS_CANCELLED，结果展示已改/冲突/未执行。显式 null 始终推进版本。
- [ ] 重启查询持久事实，只恢复原操作未提交块；删除检查未完成批引用。
- [ ] 后台每块进入 QuiesceGate.mutation；未完成操作参与工作区 blocker。无 PM4 实体不宣称真实任务占用已验证。

## P3 受控文件与导入

- [ ] DesktopResult 成功 null 表示取消选择；Renderer 仅接 token/displayName/kind/expiresAt，公开接口不接受 path。
- [ ] hostToken 内部登记授权，绑定工作区/实例/窗口/项目/用途；首次 inspect 消费，提交引用 inspection 并复查指纹。
- [ ] 新建表 POST /table-imports/excel；替换 POST /tables/{tableId}/imports/excel，明确新代次、全状态 null、不继承旧关联。
- [ ] 显式字段映射、全量身份/类型检查；隐藏候选分段写入，短事务发布结构/代次/操作结果，无可见空壳或半表。
- [ ] 替换核对结构、代次和预览后的内容/状态等变更证据；故障保留旧表，恢复查询发布事实。
- [ ] 先导入向导组件再页面：选择→工作表→字段/身份→校验/影响→进度→结果。接受后关闭视图不等于取消。

## P4 一致性导出

- [ ] 整表/当前筛选、冻结排序和选列、可附状态；一致只读快照，不跨分页混入不同时间事实。
- [ ] 新建单表 XLSX，保持文本前导零、日期、缓存公式值并防公式注入；不改源文件、不覆盖目标。
- [ ] 发布前存授权/摘要证据，发布后丢响应核验原文件，不另存重发；结果不匹配明确冲突。

## P5 验收与提交

每包：失败测试→实现→定向测试→规格审查→工程审查→修复复核→独立提交。

- [ ] 后端 pytest / Ruff / mypy；前端 Vitest、typecheck、lint、build；openapi:check、test:scripts、test:structure、git diff --check。
- [ ] 临时数据库覆盖空库/PM1/当前 PM2 升级；临时文件覆盖指纹变化、越权、损坏、目标冲突及故障恢复。
- [ ] 批量竞争/取消/重启、候选发布中断、导出发布后结果恢复、工作区保护。
- [ ] 真实空白表编辑和批状态→Excel导入/编辑/替换→筛选导出/重启核验；同时既有模块回归。
- [ ] 键盘、200%缩放、长值、下拉宽度、滚动、大表实际性能；Windows及其他未运行平台如实标未执行。
- [ ] 更新覆盖、账本、结构和 .ai；只给已验证项登记证据；主项目和其他工作区不由本任务修改。

不在本阶段：Sheets、工作流读写节点、真实任务占用、环境关联操作、字段/整表删除、项目生命周期。

## 执行记录

2026-09-13：完成起点只读核对，已分派互不重叠的后台批状态、Electron 文件桥和 Excel 服务任务；当前没有业务完成结论。原有六处 WIP 不变。最终验收报告尚未生成。

2026-09-13：用户确认原有编辑任务仍在进行，继续等待其提交。初始后端719测试通过；新异步命令23项、批状态实际HTTP和三起点迁移4项定向通过，均不代表完整PM2验收。独立审查发现旧代次、CAS、删除guard、noop计数及coordinator异常恢复问题，已交实现者补反例修复。

2026-09-13：文件授权桥与异步命令恢复提交7e46835，55项定向验证及独立审查通过。用户再次明确原编辑任务仍在进行，页面/Hook/smoke仍等待其提交。批量状态后端29项定向通过，前端恢复审查发现同会话换键重发及迟到响应问题，正在补反例闭合。Excel检查已增加持久检查job与窗口授权；导出适配器新增发布前摘要证据及只读核验，34项适配器测试通过，均待完整服务/界面集成验收。

2026-09-13：批量状态及完整 Excel 后台已交付代码，759 项后端全量测试、Ruff、mypy（195源文件）、OpenAPI与前端类型检查通过。当前 head 为 pm02_excel_exports。审查闭合记录及范围见 [后台核验](../../project-management/implementation/pm2-backend-verification.json)。新建/替换导入分200条隐藏提交，最终发布与唯一变更事实同事务；公开来源仅文件名、工作表及时间；导出以原目标摘要恢复。P2–P4后台已验证，页面接入和真实流程仍待验收，不能勾选整包完成。原编辑任务仍在进行，六处原 WIP 不在本轮提交中。

2026-09-13：前端完整112文件/803测试通过，typecheck、OpenAPI、build、任务范围ESLint和diff检查通过；全量lint仍有原编辑任务测试文件第18行unused参数。原6文件哈希确认未变。新表Excel目录真实流程、全局5入口、200%目录/初始向导、Escape焦点、Select宽度与冷重启验证通过，证据run-BHSroO；原生文件面板仍为测试注入，非人工验收。旧run-IwnO1p曾清localStorage，已修正文档限制，不作为连续恢复证明。一万行后台/文件闭环实测及表头0行→普通新增记录反例通过。前端组件均经规格/工程审查与反例修复；详情页接入及其完整交互验收仍等待原任务独立提交，PM2保持inProgress。

本轮独立提交：`32e424e`后台，`a9c0b75`批量选择/状态组件，`747395b`Excel组件与目录真实导入。原任务六处文件未提交；SHA-256保护核对见docs/project-management/implementation/pm2-worktree-isolation.json。等待原编辑任务提交后，按pm2-page-integration-handoff.md继续详情页接入和完整验收。

2026-09-13（继续实施）：重新核对 HEAD 0f07fcd，原任务六个文件仍未提交且 SHA-256 均与保护快照一致；已询问是否完成交接，未收到明确答复前不接管。独立审查和失败测试确认并修复三处组件缺口：批状态部分失败缺少事实刷新通知；切换上下文时已加载旧终态可能误投新回调；替换导入重复影响检查沿用过期确认。新增 onSettled 只用于当前作用域刷新、显式 operation 来源归属和影响检查撤权。历史测试和实际应用证据不改写，本轮只登记组件回归，不认定详情页或完整 PM2 已验收。连续实施 PM2–PM9 的旧 .ai 授权已标 superseded，当前停在 PM2。

本轮增量核验：[组件恢复核验](../../project-management/implementation/pm2-component-recovery-verification.json)。38 个项目数据测试文件、349 项测试、类型检查、四个修改文件的 ESLint 和构建通过；原六个文件仍与保护快照一致。没有追加真实应用或跨平台通过记录。
