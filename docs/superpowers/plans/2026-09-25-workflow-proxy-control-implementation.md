# 工作流代理控制实施计划

日期：2026-09-25。状态：implemented；本地验证完成，实网/Windows/打包验收待执行。来源：用户明确要求实施本计划。
规格：../specs/2026-09-25-workflow-proxy-control-design.md
执行：当前任务逐项实现、定向测试、最终独立审查；不操作任意真实代理。
基线：0f9884522d052a427d130f5157ac5f0df99ca215。隔离分支 codex/workflow-proxy-control。

## 任务与证据

- [x] 1. Provider机器原因与每操作预算；测试冷却/限流/unknown/未达目标，保留管理页兼容。
- [x] 2. 当前代理绑定、占用登记、两个worker通道；测试归属/幂等/停止/查询。
- [x] 3. 共享配置组件与查询节点；测试目录真实查询/变量/默认值/保存。
- [x] 4. IP节点完整链；测试固定基准/有限重试/捕获/relay验证。
- [x] 5. 地点节点完整链；测试目标匹配/已满足/容量/错误。
- [x] 6. 回归、OpenAPI、文档与独立审查。

每个切片先增加行为测试并观察失败，再实现与回归。使用既有pytest/vitest，不增加测试框架。
验证：pytest定向，ruff check，mypy，npm test/typecheck/lint/build/openapi:check/test:structure。
最终记录实际命令、结果、未完成的平台/实网证据。只提交本功能文件。

## 执行记录

- 设计落盘：用户本轮已明确批准并要求实现，无需再次确认。
- 基线只读验收：上一轮39项通过；真实供应商写入未执行。
- Ruling: 确认读取失败或仍忙时保持 unknown，只有供应商明确允许再次操作时才能把未达目标终结为失败 — 不能把相同 IP 当作远端操作结束 — 代价是供应商证据不足时需要后续查询，无法强制继续切换。

- Ruling: 遗留 worker 缺少可靠代理身份时，清理未确认则阻断当前工作区全部代理 — 不能从地址或凭据猜测占用归属 — 代价是其他代理也需等遗留清理完成；未来持久化可信绑定后可缩小范围。
- 任务一：`f202e70c`；任务二：`0b7a6253`；任务三至五：`2fa69c36`。设计初始提交 `fdd992e7`。
- 最终独立审查发现 3 项 P1、1 项 P2：节点结束后晚到写入、轮间完成后重复 POST、旧浏览器节点漏掉互斥、第二次预检冷却被误判不可恢复。均先复现失败再修复；审查修复组 58 项通过。无延期 minor。
- 同一修复阶段补充探测异常回归：切换已发送后抛错也必须保留 operationId 和实际 requestsSent；修复前失败，修复后通过，不重复 POST。
- 回归先完成 273 项通过；最终补充组合 232 项通过、1 项既有失败，失败已在基线 `0f988452` 隔离源码上复现。前端 1271 项通过；mypy 496 个源文件通过；37 个改动 Python 文件 Ruff 通过；前端 typecheck/lint/build、OpenAPI 和结构检查通过。
- 详细命令、既有失败、警告及未验证范围见 `.ai/sessions/2026-09-25-workflow-proxy-control.md`。任务六的本地交付完成不代表真实供应商或 Windows 验收通过。
