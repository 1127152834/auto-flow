# 设置与总览实施计划

- 日期：2026-09-12；状态：completed（本次范围实现与验证完成）。
- 用户已授权：自主设计与实施，直接在 `codex/architecture-baseline` 开发，无需逐阶段再次确认。
- 规格：[设置与总览](../specs/2026-09-12-settings-dashboard-design.md)。

- [x] 固化设计、桌面共享类型、HTTP 返回模型；检查既有 dirty work，不修改其未授权内容。
- [x] 后端切片：聚合计数、runtime 信息、当前执行器占用、host-only quiesce/resume、请求互斥与契约测试；生成客户端类型。
- [x] 桌面切片：本机设置与备份、工作区目录校验和原生选择、生命周期切换/回滚、受控路径打开、偏好恢复、只读诊断快照和原生保存、IPC 及关键测试。
- [x] UI 切片：设置卡片、偏好行、工作区确认/进度/结果、诊断预览、总览统计/入口组件；随后组合页面并接真实桥接/API，验证异常与取消。
- [x] 应用壳接入：侧边栏不新增；总览/设置页可用，已实现领域入口可达；离线恢复不锁住设置，切换重建 client/cache。
- [x] 集成验证：后端和前端全套检查、OpenAPI、构建、独立临时数据目录 smoke、macOS UI；修复本次回归并记录外部任务未决项。
- [x] 更新原型/迁移状态与 `.ai`，仅提交本任务文件及必要接入 diff；不混入其他任务未提交改动。

并行边界沿用用户此前对功能迁移并行和 gpt-5.6-sol 的授权：后端模块、前端领域组件可独立处理；桌面生命周期与共享接入由主任务负责。共同文件按明确所有权合并，禁止 agents 自行 git commit。

验证记录与剩余边界见 [设置与总览实施状态](../../migration/settings-dashboard-status.md)。Windows 原生验证及浏览器 Task 11 页面组合属于后续/并行范围，不计作本任务已通过。
