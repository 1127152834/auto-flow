# 规格与工程自审

日期：2026-09-14。执行者：本侧对话助手。状态：候选版自审记录；**不是独立审查，不宣称 G4 已完整退出**。

## 规格核对

| 要求 | 事实 / 证据 | 结论 |
|---|---|---|
| 同一表格内新增 | `DataRecordsTable` 的原 TableBody 追加 `RecordDraftRows`，正常入口不打开表单 | 已实现，V01/V04 |
| 组件先于页面 | 独立草稿、粘贴、存储、Hook 和三类组件，再接 DataTableDetailPage | 已实现 |
| 原子多行保存 | create_many 不调用单条 commit；记录/Operation/DataChange 同事务；第二条证据失败整体回滚 | 后端测试通过 |
| 不混淆值与身份 | missing/null/空文本/0/false；带类型 RecordKey；clientRowId 只属于草稿 | 单元测试与真实粘贴通过；整数身份另有既有领域测试 |
| 未知结果不重跑 | 固定 key/payload；先查询；可信未接受后显式重发；映射完整才清草稿 | E09/E10 通过 |
| 不跨工作区/数据代次写入 | 持久信封校验；旧代次允许查已知操作但禁止重新创建到新代次 | 两工作区 E2E 与旧代次 Hook 测试通过；组合跨代 E2E 待补 |
| 原有功能 | 原单条 API、已保存记录编辑、Excel/批状态能力保留 | E16 与 PM2 独立 smoke 通过 |
| 文件责任 | 主项目/旧仓/父任务只读；选择已提交 a6a0e8e 后继续 | 本分支独立；其他任务 HEAD 推进不由本任务写入 |

## 修复与复核记录

| 问题 | 修正 | 验证 |
|---|---|---|
| 批量 Operation 初次序列化包含内部数据且不符合 wire schema | 复用现有 `_op`；真实 schema 生成客户端 | 2 个 HTTP 契约测试及 OpenAPI 检查 |
| 草稿列遗漏已有“最近修改”列 | 追加未保存单元格并修正 colspan | RecordDraftRows 测试，实际截图 |
| 保存期间禁用格将原输入显示成“不可填写” | 禁用只冻结编辑，仍显示原值 | RecordGridCellEditor 测试，V08 |
| Enter 新行丢失当前列；100 行边界焦点无法退出 | 新行传递目标 fieldId；达到上限转移到保存操作 | RecordDraftRows 测试 |
| Boolean 格按字符可能隐式写入 false | 打开编辑不写值，焦点转给现有 Select；闭合后 Escape 返回 | RecordGridCellEditor 测试 |
| 保存回调刷新异常可能被当成创建失败 | 成功事实与后续列表刷新分开 | Hook 与实际恢复流程 |
| 重连后只失效缓存未能刷新当前观察到的查询 | 新增保存和字段冲突显式 refetch 当前表/字段/记录 | E08 在服务重启后执行，E14 总数与筛选验证 |
| 旧代次 pending 因信封加载拒绝而无查询入口 | 受控读取旧代次信封，仅允许原操作查询；确认后清旧信封，不重发到新代次 | 旧代次两项 Hook 测试；组合 E2E 仍待补 |
| 200% 缩放时保存栏离开可视区域 | 固定视口底部并预留内容空间 | V11 + 实际按钮 bounds 断言 |
| 自动点击落在关闭动画遮罩上 | QA 先检查 elementFromPoint 命中目标 | E16 实际旧记录编辑复验 |
| QA 按旧“状态/记录”页签及常驻按钮定位 | 更新为当前页签、更多菜单和简单筛选定位；保留全部业务断言 | PM2 批状态/文件闭环/10000 行 smoke 通过 |
| 测试脚本假设创建字段返回 201，实际现有接口返回 200 | 依据真实既有 handler 修正测试预期，不修改业务接口 | E08 复验 |

## 视觉审阅

选中图与实图已在相同 1487×1058 内容基准下对照，见 comparison.html。新增区域的十项结构核对：原表体追加、没有新增表单、淡色草稿、当前格边框、真实身份占位、真实状态显示、逐格错误、尾部增行、统一计数/保存、局部横滚均有源码及截图依据。

明确存在的视觉差异：应用继续使用已有顶部导航、页头留白、真实查询入口、身份/最近修改/操作列；没有把原型示例的业务字段和记录数量硬编码。底部保存采用固定视口位置，在大表和 200% 下可见，因此不会在每一张截图中贴住表格的最后一行。日期、数值、布尔列由真实表结构决定。

**未完成独立逐图评分，也不宣称每张图达到 ≥85 分。** 这些截图是候选基线，保留给用户视觉验收；本记录没有通过修改评分范围或像素相似度冒充计划门槛。当前代码可以手测，但 G4 视觉/独立审查门槛仍未关闭。

## 剩余验收

1. E13：旧单条创建 pending 与重新导入后旧代次草稿的组合真实 E2E。实现和自动测试已有，不能用它们替代完整应用证据。
2. 原生中文输入法、完整辅助技术与所有键盘边界。composition 的防误提交仅组件测试，原生输入法没有标通过。
3. 用户逐图评分与手动用例。侧对话不能派发独立审查智能体；自审不能代替它。
4. Windows、其他架构、打包版本均未执行。

## 复用与复杂度检查

使用现有 Input/Textarea/Select/DropdownMenu、scalar/record 校验、Operation 与数据库会话。没有增加新依赖、状态机框架、Excel 引擎或数据库迁移。批量请求和草稿上限是明确边界；不把本功能扩成全量电子表格编辑器。`record-grid-*` 各自承担纯模型、TSV、存储，页面只负责上下文与组合。
