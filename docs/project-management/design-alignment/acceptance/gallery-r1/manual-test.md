# 原始 Gallery R1 手动验收

状态：R1本机自动验收通过，代码版本a6a0e8e；本文用户用例全部未执行。自动结果见machine-report.json与cases.json，原图并排见comparison.html，不以B0历史分数替代原图验收。

## 启动与隔离

在 `/Users/zhangtiancheng/Documents/projects/autoflow-project-management-implementation` 执行：

```bash
npm run build
node scripts/qa-project-alignment-r1.mjs --manual
```

工具创建带 `.r1-qa.json` 标记的临时父目录及A/B两个工作区，输出真实路径。禁止将普通业务目录作为目标。该版本使用真实Electron、FastAPI、SQLite；终端保持打开以执行下述测试命令。`shot`保存当前截图；`quit`只退出，保留资料；`clean`仅清理工具创建且验证标记的工作区，截图报告保留。

## 测试步骤与预期

| 编号 | 前置与逐步操作 | 预期结果 |
|---|---|---|
| G-R1-M01 | 顶部“项目”→“新建项目”；名称填“内容采集A”，说明填“保存文章资料”，创建。返回项目目录，按相同步骤创建B。点击底部“全部项目”，先打开A返回，再打开B返回，点击“返回最近”。 | 最近B排在A前；两列卡片显示真实说明与最近打开时间，原图库大标题/左新建右搜索/底部全部入口位置保持。 |
| G-R1-M02 | 在A卡片右上更多→编辑项目→修改说明→取消→继续编辑→取消并放弃；随后重新打开编辑并保存新说明。 | 菜单不打开项目，未保存保护有效；放弃不写入，成功后卡片显示新说明。 |
| G-R1-M03 | 工具终端执行`seed`一次；在全部项目搜索“R1分页”，下一页，打开某一项目后返回。修改排序再重复。 | 第一页50项，第二页2项；返回恢复搜索、排序、页码与同会话滚动。仅该工具生成资料用于规模验证，不代替M01界面创建。 |
| G-R1-M04 | 打开A→数据→新建数据表；名称“资料库”，描述“保存整理后的文章资料”；创建后返回数据目录，更多→编辑数据表，修改描述并保存。 | 卡片显示真实记录数、来源、本地维护事实；可见“打开数据表”与其朗读名称相符；菜单编辑不打开记录页。 |
| G-R1-M05 | 在资料库字段页新增文本“标题”、数字“数量”；在记录页新增“温室光照管理笔记”/0和“花园检查”/1。 | 记录身份稳定，0不变成空值；查看、编辑、更多为三个独立入口；最近修改为真实数据时间。 |
| G-R1-M06 | 点击筛选，修改字段/状态条件，取消；重复后Escape；重复后点击外部；再修改并应用。排序和显示列各重复一遍。 | 前三种关闭不改变结果；应用才生效；筛选/排序回第一页，选列不重查后端、不改变页码。复杂条件仍可编辑且不丢失。 |
| G-R1-M07 | 搜索字段选“标题”，输入“温室”；输入时观察列表，按Enter。再应用一个业务状态条件，清空快捷搜索。 | 输入期间不查询；Enter才应用；清空只移除快捷搜索，业务状态条件保留；当前筛选导出使用同份最终查询。 |
| G-R1-M08 | 回目录，终端`readerror`后点击刷新图标；观察旧内容，恢复后刷新。新建前执行`lost`，创建“响应找回项目”，按页面“核对保存结果”。 | 读取失败保留已有资料；首次失败不伪造0项。写入响应丢失明确提示结果未确认；核验原操作后只有一个项目。测试注入不得当成真实断网。 |
| G-R1-M09 | 编辑项目但不保存；终端`conflict`真实提交竞争修改，再点击保存。使用页面载入最新资料/重新编辑后保存。 | 409保留输入；明确采用最新事实后才能重新提交；不静默覆盖。该命令只使用隔离工作区真实HTTP，不直接改数据库。 |
| G-R1-M10 | 同工作区编辑草稿后执行`service`；取消离开并确认草稿保留。正常保存后`switch`切B，再切A；`restart`重启应用。 | 服务重连不覆盖草稿；连接恢复状态不误称只读可用；切换期间不能提交，A/B资料隔离，重启后持久事实仍在。 |
| G-R1-M11 | `zoom200`；逐一打开筛选、排序、选列，使用Tab/Enter/Escape；滚动长列表；`zoom100`还原。 | 全页不横向撑宽；只表格内部横向滚动；浮层内部滚动；嵌套Select先关闭，再关闭Popover，焦点归还原触发器。 |
| G-R1-M12 | 终端`entries`，检查浏览器、代理、模型、设置、总览和Studio入口；回资料库检查记录编辑、批量状态、Excel导入/导出入口。 | 真实入口可达，无假按钮；现有PM2行为保留。入口可达不代表全部模块功能完整回归。 |

对照原图：`00-projects/100-projects-prototype-5238b4.png`、`05-data/001-data-v1-approved-ca940d.png`、`002-records-c496e3.png`、`017-record-filter-672930.png`、`016-create-table-73d061.png`。只允许全局侧菜单变顶部、实际业务数据及已登记未实现能力的差异。

## 记录格式

每条填写：用例编号、通过/失败/未执行、测试提交、操作步骤、实际结果、截图路径。反馈示例：`G-R1-M06 / 失败 / <commit> / 修改筛选后Escape / 列表意外变化 / <shot.png>`。所有用户手测初始为“未执行”。Windows、其他架构、打包验收未执行时单独保留该状态。
