# Gallery R1 审查与问题闭合

状态：R1工程、本机端到端及独立视觉核验通过；用户手动验收未执行。只以原始图库 PNG 为基准，B0 不参与评分。

## 规格与工程审查

| 包 | 规格审查 | 工程审查 | 修正与证据 |
|---|---|---|---|
| 项目目录/数据卡片 | 通过 | 通过 | 最近与全部独立，未访问排除；真实数量、来源、只读与暂停分开；保留编辑与返回上下文 |
| 共享 Frame/项目头 | 通过 | 通过 | 项目 h1 → 数据目录 h2 → 卡片 h3；长名称换行；保留原图完整项目头，返回收进面包屑 |
| 查询内容 | 通过 | 通过 | 组合错误路径、多行字符串原值不丢；简单与高级无损切换；未应用标记同行；空值运算符不显示多余输入 |
| 行操作 | 通过 | 通过 | 查看/编辑/更多分别传 typed identity；菜单已展开后 disabled/loading 仍禁删除 |
| 新建表弹窗 | 通过 | 通过 | 原016居中结构；修正无效标题选择器；名称120/说明1000 Unicode码点合同保留 |
| 200%浮层 | 通过 | 通过 | 搜索 flex 最小宽度；Radix视口约束及触发器resize可见性；关闭后监听清理 |
| 通知 | 保留既有R1实现 | 定向及全量复验 | operationId 去重、有界队列、旧请求隔离；不以Toast代表业务持久成功 |

独立审查由 continuous_r1_spec 和 gallery_g0_quality 执行，查询组件由 gallery_r1_query 实现。主协调检查实际 diff，并运行集成测试；不把智能体口头报告当作执行结果。

## 实际失败及处理

| 发现 | 归因 | 修正/复验 |
|---|---|---|
| 查询面板打开后Select在pointerup消失 | QA未先发送mouseMoved，截图之后指针坐标未同步 | CDP动作先移动再按下/释放；保留失败run及事件诊断；嵌套Escape重新通过 |
| 200%工具条收缩时重叠，浮层底部越界 | 页面布局与离屏触发器 | min-width/内部高度/sticky避让，并在resize时保证触发器可见；实际200%复验 |
| 一个页面出现两个h1 | 恢复完整项目头后原数据目录仍h1 | 不改变可见结构，改数据目录h2/卡片h3；App断言仍要求唯一h1 |
| 016实图80分 | 标题CSS指向不存在data-slot、取消按钮与尺寸错误 | 真实header h2选择器、控件字号/尺寸/描边取消、去多余分隔；run-XieVdX复评88 |
| 旧PM2 smoke找不到“状态” | 页签已经按原图变为“业务状态”，定位未更新 | 仅改真实页签定位，不删除状态/批次/Excel业务断言 |
| 组合导出证据JSON无法序列化日期 | QA报告用json.dumps未处理openpyxl datetime | 证据显示使用default=str，原XLSX不变；重新执行同用例 |

## 视觉逐图审查

评分为人工审图判断，不是像素相似率，也不是用户签收。每对原图/实图在同一次工具调用中查看。门槛为每张85及全部适用强制结构通过，禁止平均。

| 原图 | 实际画面 | 布局40 | 信息25 | 样式20 | 操作15 | 总分/结果 |
|---|---|---:|---:|---:|---:|---|
| 100-projects-prototype | run-jUPFWL/VR-A01-recent.png | 37 | 24 | 16 | 14 | 91/通过 |
| 001-data-v1-approved | run-XieVdX/r1-b-two-local-tables.png | 38 | 22 | 16 | 14 | 90/通过 |
| 016-create-table | run-XieVdX/r1-b-create-table.png | 35 | 23 | 16 | 14 | 88/通过 |
| 002-records | run-XieVdX/r1-c-original-visible-columns.png | 35 | 22 | 16 | 14 | 87/通过 |
| 017-record-filter | run-XieVdX/r1-c-filter-populated.png | 37 | 23 | 14 | 12 | 86/通过 |

原016上轮 run-lbdsCZ 为80（失败），保留该历史；不能用后续分数覆盖。全局侧菜单改顶部是唯一整体版式调整。真实本地/Excel来源、系统生成身份、指定文本字段搜索/复杂查询/批状态等能力差异详见page-map，未编造Sheets或外部SQLite事实。

仍存在可见细节差异：弹窗留白稍多，控件边框/圆角沿用现有令牌，记录标题列按动态字段扩展，目录分页控件体量略小。它们已纳入扣分；没有把当前结果描述为像素一致。200%仅验证响应式可用性，不给虚构原图分。

最后一次小补增加搜索提示、隐藏isNull输入、查询取消按钮描边/底栏尺寸；须结合最终run再确认，不把旧截图当新构建截图。

## 最终视觉复验（a6a0e8e）

原图仍逐对同次调用查看。最终目录run-fzpgVA维持91（37/24/16/14）；最终run-fLAsGt：001目录90（38/22/16/14）、016弹窗88（35/23/16/14）、002记录89（35/23/17/14）、017筛选89（37/23/15/14）。适用GF结构全部通过。原图和实图文件见page-map与comparison.html。

200%新建表正文内部滚动，不能宣称全部字段同时可见；标题、关闭及底部按钮完整可达，真实提交通过。筛选在200%上翻，完整底栏可见且无重叠。最后补的搜索占位、取消描边/底栏字号在新截图中确认。其余差异为非阻断细节，不循环修改已过门槛页面。

额外QA诊断：Electron临时缩放按文件页地址缓存，原页面设200%，在另一个hash改100%后返回会恢复原页200%。隔离诊断记录native因子及DOM视口；QA在回到原地址后再次还原目标缩放并核对真实因子/视口，没有改生产缩放或跳过200%提交。

最终997项Vitest、32项后端契约、29项脚本、3项结构与构建/类型/lint/OpenAPI通过。full run与directory run通过；PM2目录14流程及Excel/批状态7流程通过。详见machine-report、cases及logs。用户手动用例仍全部未执行。R1规格复核确认无剩余阶段阻断，允许进入R2。
