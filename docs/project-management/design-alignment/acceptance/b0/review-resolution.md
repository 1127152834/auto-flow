# B0 审查与修正记录

- 日期：2026-09-13；范围：五组实际PNG及配套资料，不是业务代码审查。
- 规格审查：独立 `b0_spec_review`（gpt-5.6-sol），逐张使用view_image original核图；修正后再次核对五图，结论为关键阻断和重要问题已闭合、可交用户验收。
- 工程质量审查：独立 `b0_quality_review`，在规格复核通过后开始；结论在下文登记。
- 用户验收：尚未执行。

## 问题闭合

| 编号 | 发现 | 实际处理与复核 |
|---|---|---|
| B0-S01 | 初版R2新增/编辑可选择业务状态，详情像可编辑表单 | R2-A字段改只读；保留显式状态下拉/保存/清空；B/C状态为静态未设置，不随内容提交 |
| B0-S02 | 首稿核验中与同一保存成功并列，来源/操作人字段无依据 | 移除虚构来源与操作人；R2-G只保留核验和离开视图，成功须查询后出现；R3-H独立状态卡编号1–3 |
| B0-S03 | R3多余数据表卡片、字段页统一保存栏缺失、超限建议分批 | 移除资源卡片；补完整字段表/统一保存栏；改超限整个候选不保存和重新检查，不自动拆分 |
| B0-S04 | 自动化引用误写成未处理/未绑定，暗示已有能力 | R3-C改为清晰大字“自动化引用：尚未接入”，不报告数量；其余实际引用精确口径见交互说明 |
| B0-S05 | 首稿漏掉批状态，被导入步骤占满 | R3-E合并向导步骤总览；保留I批状态确认/进度、J200修改/100冲突/50未执行；不称全部成功 |
| B0-S06 | 查询工具未显示排序展开、批状态按钮 | R1-C补三个独立展开面板及主栏显示列/批量状态；取消/应用边界明确 |
| B0-S07 | 缺正式manifest、brief和账本 | 已新增来源ID/hash/准确像素尺寸/画板映射及18项未来任务；静态核验通过 |
| B0-S08 | 协作者只检查R1的旧文档状态未更新 | 全部5图主协调与独立规格复核已登记；用户测试表仍全部未执行；manifest视觉checked与用户notExecuted分开 |
| B0-S09 | 示例字段键date与图中published_at不一致 | 文档统一published_at，遵循原字段稿；不把展示名当fieldId；只作为合成fixture |
| B0-S10 | Excel映射缺地址、重导入发布事实不清 | 图E增加地址→文章链接；F补确认后新一批数据，交互11节定义新datasetGeneration、发布前旧数据不变 |
| B0-S11 | 冲突后重新编辑基线不清 | R2-F改保留我的输入/采用最新内容；交互11节明确复制机会、本地基线更新、重新校验和不直接写入 |
| B0-S12 | 字段影响缺具体前后值 | R3-A显示资料标题；C列标题→资料标题以及category新增回填；字段页仅2项草稿变化 |

## 保留的轻微限制

五图为生成的多画板总览，小号辅助汉字存在栅格误差，精确产品文案由交互说明绑定；主要按钮、身份、状态、限额和部分结果已核查。没有把生成图称为像素精确的1440×1024应用截图。R2实际1983×793，真实宽度、缩放与键盘由R阶段执行。

原coverage/verification及PM0–PM2报告保留历史时点。本包只读检查重新验证源图hash，并使用自己的报告，不把缺图完成状态回填进旧报告。主线存在其他任务的持续提交，记录实际观察而不宣称整个主目录全程不变。

## 工程质量复核

通过，可交用户验收，置信度高，无阻断。独立复核重新执行资产脚本和git diff --check，独立计算五图hash/尺寸，检查最终生成pass与文件字节，使用原始分辨率查看R2/R3关键画面，确认主要按钮、恢复、限额与部分结果可读。实际Git状态确认无apps/packages业务源码修改或新增，历史QA目录保留。

质量建议已处理：脚本增加未跟踪路径边界检查；把futureTasksUntouched改为futureTaskRecordsNotStarted，准确描述其验证的是账本声明，另外独立检查业务路径未变化。报告持久化使用先捕获完整标准输出再写文件，避免输出重定向先截断报告导致自检查失败。

生成记录含机器绝对原始路径，当前机器已核对；仓内交付PNG及SHA-256可跨机验证，原始生成缓存/主目录图库的完全复验仍需要对应来源目录，此限制保留。用户验收尚未执行。
