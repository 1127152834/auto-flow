# G0 共用页面结构核验

状态：G0共享结构装配、自动检查及独立规格/工程复核通过，可以进入R1。此报告不声明 R1/R2/R3 整页视觉通过。

## 已修正审查问题

1. 表级页签与标题顶部对齐：87381e5。
2. 新增/编辑缺少当前表名：面包屑读取与详情相同的工作区/实例/项目/表查询，完整显示真实表名；测试先失败后通过。
3. Frame 的 overflow-hidden 截断 sticky 参照：b77a3cc 改为 overflow-clip。
4. 200% 时标题被页签挤成竖排：ae1e5a2 为标题建立20rem换行基准；真实几何断言验证不重叠。
5. unconfigured 来源错误标为本地：显式来源分支与反例测试。

规格审查由 gallery_g0_spec 完成，工程审查由 gallery_g0_quality 完成；源码问题已闭合。测试、截图与复核都不代替用户手动验收。

## 真实验证范围

[run-AatYpG](../../../../migration/project-data-directory-qa/run-AatYpG/result.json) 使用真实 Electron、FastAPI、隔离 SQLite，14项流程通过。核心项目、表、字段、状态、记录由界面创建；竞争修改/事实查询为真实 HTTP，丢响应为明确测试注入，工作区及服务桥调用单独登记。

同次工具中查看了原001/003/004/005与实际empty/record-detail/record-create/record-conflict；检查共享头、五页签在卡片内、004/005无侧列、面包屑真实表名。200%新增页额外断言标题宽度、页签无重叠、滚动后保存栏在视口内。截图包含源码/构建/脚本hash、字体、viewport/DPR/zoom元数据。

## 仍待后续实现

- R1：目录卡片/标题比例、页头尺寸、记录表及查询操作位置。
- R2：字段通栏行及分隔、校验/底栏文案、详情状态与时间合并、编辑/更多操作及确认框。
- R3：字段抽屉草稿和聚合事务、状态真实引用、来源与内联设置。

当前截图使用单字段合成数据且部分含Toast，仅作共享结构与功能证据。不能将其作为完整正常态原型评分；正式阶段需对应原图的多字段资料、通知退出后截图及逐页门槛。

Windows、其他架构、打包验收和用户手测：未执行。

合并定向检查最终6文件64项通过。首次合并运行中新增结构测试在catalog尚未就绪时同步查询form，发生等待竞态；改为等待同一卡片内form真实出现，保留全部结构断言，失败日志仍保留于tests-before-wait-fix.log。
