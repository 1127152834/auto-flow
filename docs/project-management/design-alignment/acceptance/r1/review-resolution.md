# R1 审查与修正记录

日期：2026-09-13。状态：开发与本机自动核验完成，等待用户验收。执行 Superpowers 的分包、规格审查、工程审查与交付核验；独立审查者 `r1_popover_review`，目录/手测校对 `r1_directory_components`，查询组件交叉复核 `r1_query_components`。

| 包 | 反例与发现 | 修正和复核 |
| --- | --- | --- |
| R1-03 | 缺模块 red；Radix 默认外部点击不满足本计划焦点恢复；缺真实 Tab 路径 | 复用 OverlayHost，尊重 preventDefault 并恢复触发焦点；Modal → Popover → Select 的 Tab/两次 Escape 测试通过。共享控件 3 文件 11 项定向通过，最终全量通过。 |
| R1-01/02 | 组件初次 5 项 red，二次边界 3 项 red；孤立 row 角色、长文本、重复空态按钮、缓存错误文案不准确 | 使用 article 与独立操作；调整换行/空态/真实缓存提示；增加独立 recent 请求、跨工作区偏好和实际路由往返恢复测试。规格、工程复核通过。原“数据目录无 h1”判断经源码复核撤回。 |
| R1-04 | 查询取消/应用目标行为先 red；搜索失败后错误残留、trim 未同步、清空身份与异步字段恢复 | 共享 SearchInput，错误定位、首文本字段恢复、成功同步、原条件无损；20 项组件定向通过，页面定向及最终全量通过。 |
| R1-04 装配 | 提前清理字段可能短暂放宽查询；无效最终条件仍可导出；搜索与操作在 200% 重叠 | 最终查询按真实字段/状态统一严格校验，无效时禁查/禁导出；失效引用修复后重新查询。搜索区在窄视口独占行；本机截图复查和无重叠断言通过。 |
| R1-05 | 新通知反例 2 项 red；旧计时器可能删除更新通知，Strict Mode 初始化需要保持 pending | 上限三条、操作身份复用数值 ID、清理重启计时器；5 项通知测试通过。消费者使用工作区身份与原请求 scope/ticket 校验。 |
| 验收工具 | 原所有权判定需抵抗相对路径/链接；一次读失败实际会自动重试；不能将写入前抛错叫响应丢失 | realpath 与专用标记校验；readfail/readerror 分开；lost 在实际提交后丢弃响应并阻断一次查询。安全测试通过，真实恢复只有一个项目事实。 |
| 手测文档 | 第二页只有两张卡无法测滚动；表冲突和返回目录步骤缺失；把非模态 Popover 称遮罩 | 第一页测滚动、第二页测分页恢复；写全继续编辑/重新编辑/保存/返回步骤；修正控件名和点击浮层外。自动与用户结果分开。 |

完整最终检查见 [verification.json](verification.json)。本机 Electron 截图和真实 HTTP/SQLite/XLSX 验证有独立报告，未以组件测试冒充应用通过。R2/R3 未执行。

早期 harness 失败原始报告汇集 [diagnostic-history.json](diagnostic-history.json)，包括定位/等待表达式修正；200% 搜索重叠是实际布局问题，已修复并在 run-mb5jSP 修复复验，再以最终已提交工具 run-tpXDVQ 复验。run-X578wD 的手动工具批命令中，service 因读请求仍进行而被 QuiesceGate 拒绝；不把该次会话整体标通过，也不绕过保护。最终空闲条件下生命周期命令另记 run-litxco。手动命令错误现在持久记录 commandErrors。

最终可追溯性审查：原报告为提交前工作树结果，审查要求绑定版本。已提交 QA 工具 a13e649，新增 gitHead、scriptSha256、desktopBuildSha256，再完整执行 run-tpXDVQ，通过且 commandErrors 为空；旧运行不作为最终版本证明。

独立最终复核（r1_popover_review）：实际 git show 中脚本与运行 SHA256 一致；按相同算法重算桌面构建 SHA256 一致；最终报告/画板唯一指向 run-tpXDVQ。可追溯性阻塞闭合，无剩余阻塞项。
