# 项目管理与行内新增：主项目合并核验

日期：2026-09-14。范围：将 `361d4fd` 接入主项目；保留 `dcb1831` 的 R2/R3 交付以及主项目最新 Studio。此次不推进 PM3，不声称原分支 G4 全部验收完成。

## 整合方式与冲突处理

- 在独立整合分支完成开发与测试，主目录通过快进接入；保留原 `codex/record-grid-entry` 与项目管理实施分支。
- 主线初始基线 `cda081d`，整合期间跟进 `2f51399` 的 Studio 浏览器脚本服务合同。保留当前 Studio 代码、独立窗口和已退役执行器的数据库历史，不恢复旧执行服务。
- 正常新增记录在现有表格尾部连续录入，一次有界原子保存；已有记录仍走 R2 详情/编辑页，字段保持 R3 草稿、预检及统一保存。
- 追加 `0009_merge_project_data` 合并迁移，父版本为 `0008_workflow_debug` 与 `pm02_schema_drafts`。测试覆盖空库、旧资源库、Studio 分支、PM1 和 PM2 分支，检查资源与历史 Studio 数据保留。降级到 PM1 时必须同时断言保留 Studio head，不能依赖版本表第一行的顺序。
- 项目路由统一注册于 `bootstrap/project_http_routes.py`，真实应用和无副作用 schema 导出共用。生成类型保留最新 Studio 合同以及全部真实项目接口。
- 发现并修复服务重连后查询 observer 与新 QueryClient 分离的问题：同一工作区保持 QueryClient，实例身份隔离远端数据，重连刷新活动查询，真实工作区切换仍由 provider key 隔离。反例测试先因保存后界面不更新失败，再验证保存可见、草稿保留、旧响应隔离，以及模型认证恢复和总览刷新。内核事件测试继续断言旧实例迟到事件不进入缓存。
- 更新行内录入 QA 的字段准备步骤，按 R3 “应用到草稿 → 保存字段 → 确认保存字段”执行；已有记录使用面包屑返回箭头。业务断言保留。测试生成的无关 Studio 清单恢复主线版本，不混入项目管理变更。

## 自动验证

| 范围 | 实际结果 |
|---|---|
| 后端全量 | 967 passed；2 条依赖弃用警告 |
| 前端全量 | 243 个文件、2698 passed |
| 最新 Studio 增量 | 后端合同与 schema 导出 19 passed；前端 4 个文件、59 passed |
| 工程 | TypeScript、ESLint、Ruff、mypy、OpenAPI、构建通过 |
| 脚本与结构 | 44 / 3 passed |
| Studio 真应用回归 | 主窗口与 sidecar、工作台开启/复用/重新打开、关闭主窗口后工作台保留、退出清理通过 |

全量结果对应整合后的项目与缓存修复；随后接入最新 Studio 提交，增量测试、全局类型生成与构建另行复验。日志分别保留，不把增量结果称为另一次全量结果。最初长 Unicode 输入用例在高并发时超时，后续完整复验全部通过；未删除或放宽该断言。

## 真实应用与限制

最终行内录入脚本、截图、运行路径和哈希见 `verification.json` 引用的 run。核心项目、表、字段和记录由 UI 创建；规模/竞争资料、原生文件选择结果注入、受控工作区切换 IPC 和直接路由恢复在脚本报告中明确区分，不冒称全鼠标操作。源码和构建哈希随截图保存。

Windows、其他架构、打包、原生中文输入法、用户手动验收及独立视觉评分未执行。原 `361d4fd` 报告中的旧 pending/重新导入组合 E13、G4 门槛仍保留，不因合并自动改为通过。

[手动测试方案](../record-grid-entry/manual-test.md)。用户启动主项目后，重点检查：新增行连续输入/粘贴/保存；服务重连后编辑已存记录立即刷新；字段多项统一保存；工作区切换不串草稿。
