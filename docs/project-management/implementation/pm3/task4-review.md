# Task 4 执行器审查记录

日期：2026-09-15。状态：规格及工程审查闭合。范围为独立 PM3 分支的 worker、资源、dispatcher 与生命周期；不是 PM3 页面或完整里程碑验收。

执行采用 `subagent-driven-development`，每个子包先规格审查、后工程审查。以下是实际复现和修复记录，不以测试数量替代规格审查。

| 子包 | 主要问题与处理 | 当前审查 |
|---|---|---|
| 子进程 | started ACK 后重新检查停止；Playwright 默认超时设为 0，由节点的秒预算统一控制；非清空输入确定性追加；relay 和 context 任一清理失败均不报清理成功；错误 ACK 与 relay 退出同时失败单独覆盖；非零 timeout 保留秒及小数，不再错误除以 1000 | 规格、工程通过；14 项单测 |
| 父进程 | 完整 JSONL 才接收；拒绝布尔/浮点身份；提交回调成功才 ACK；启动取消等待真实句柄；目录已移除可确认，但其他清理错误保留容量；启动失败和目录失败组合允许恢复；同任务重试不自取消/等待自身 | 规格、工程通过；15 项进程集成测试 |
| 资源 | proxy 按模式严格白名单，拒绝额外凭据/路径和空对象；冻结配置与租约列表不共享；无效快照规范为资源错误；配置、内核及正式版 License 持有对应锁 | 规格、工程通过；7 项单测 |
| 原生恢复与装配 | 原生归属不可读不等同无进程；严格扫描失败保留目录；异常 shutdown 仍收齐所有模块和 executor；原 profile 锁和内核锁未释放时保持待核验 | 规格、工程通过，profile guard 增量已闭合 |
| dispatcher | 修正在途 stopping 事件被错误撤权、状态事件缺失、变量丢弃、异常原文落库、超时改写为失败；进一步闭合恢复未确认准入、orphan 强停、资源申请取消清理及并发核验、非 queued 重放和响应序号 | 规格、工程通过；shutdown 隐藏资源占用失败保留、恢复成功消除旧清理错误已复核 |

## 实际证据边界

- 真实 CloakBrowser 原生执行已运行：四种基础节点、停止前无导航、节点超时后无后续动作、进程与临时目录清理。
- 真实 SQLite、真实 Profile、复制到测试工作区的已安装 CloakBrowser、持久事件和冻结变量链已运行。测试重新创建应用服务并查询同一数据库，不能称为完整 Electron 或 sidecar 进程重启验收。
- 合成 Python 子进程用于协议/故障注入；不冒充真实浏览器动作。原生孤儿回收测试明确使用合成进程。
- Windows 活跃 worker 使用现有 Job/保留进程句柄；重启遗留目录尚无原生归属核验，实现明确保留 reconciling。Windows、其他架构和打包均未执行。
- 用户于 2026-09-15 取消 Studio demo 联合验收。管理页面 Electron E2E、截图比对及用户手测尚未执行；真实后端测试不替代它们。

## 工程核验环境

工作区缺少被 Git 忽略的 WebRPA 元数据参考文件，曾使 `npm run test:scripts` 失败。恢复的是清单明确要求的单一只读参考输入；其 SHA-256 与已提交 `required-field-source-coverage.json` 一致：`fdc519967c3e8405fb226f9c6831c057547d684bb7d187b51ee86b9a5628318e`。未更改脚本断言、生成元数据或主目录源文件。

最终检查次数、源码哈希与未执行项以 `task4-verification.json` 为准；该报告须在审查全部闭合后生成。
