# Final run UUID E2E

Passed against the immutable isolated snapshot recorded in `uuid-final-snapshot.json`, with source HEAD `a5e7d48` plus the recorded PM3 working changes. The 13 screenshots cover batch and task directories, task logs, input/output, successful and naturally failed evidence, normal stop, force-stop confirmation, and the loaded screenshot preview.

The batch refresh error is a declared synthetic CDP response fault: one user refresh receives three HTTP 500 responses for the same URL to cover the initial query and `retry=2`. The force-stop setup uses a declared synthetic OS process pause. It walks only the current test Electron PID's descendant tree, requires exactly one command matching the workflow worker, records that PID's process start time, and sends `SIGSTOP` only to that process. The UI then performs the real normal-stop request, waits through the 30-second grace, and submits the real force-stop request. Before continuing, the run verifies that the same PID and start time no longer exist and records `processExitConfirmed: true`. If the scenario exits early, `finally` sends `SIGCONT` only when both PID and start time still match, avoiding PID reuse. The missing-selector task is a natural local browser failure. `result.json` is unchanged from the run and retains its temporary evidence paths and hashes. `run.log` contains no runtime token.
