# PM3 运行管理视觉门槛复核

日期：2026-09-15  
状态：当前构建通过 03-runs/004–007 的有限视觉门槛复核  
置信度：高（结构与当前可见状态）；中（未由本轮夹具填充的状态）

## 基准与证据

本次只检查以下四张原型，不扩展到 Studio 或其他模块：

| 页面 | 原型 | 当前应用证据 |
|---|---|---|
| 批次详情 | `latest/03-runs/004-batch-detail-approved-459f25.png` | `qa-runs/run-mpYP5E/04-batch-detail.png` |
| 任务日志 | `latest/03-runs/005-task-log-510347.png` | `qa-runs/run-mpYP5E/05-task-logs.png` |
| 输入与输出 | `latest/03-runs/006-task-input-output-approved-7af0aa.png` | `qa-runs/run-mpYP5E/06-task-input-output.png` |
| 异常与证据 | `latest/03-runs/007-task-exception-evidence-approved-559ddf.png` | `qa-runs/run-UL3zki/07-task-evidence.png`、`08-failure-preview.png` |

证据来自 1440 × 1024、DPR 1 的真实 Electron + FastAPI + SQLite + CloakBrowser 流程。成功场景 `run-mpYP5E` 的 `sourceSha256` 为 `a2ad7844070dd1adee89c1e1246642ca7c0bb9be4d636576c2394c18dbc7351e`；失败场景 `run-UL3zki` 的 `sourceSha256` 为 `0f3a8fd2a379f84cee6cb57627819b90bcf886aa29e4d5874e6cc19465153a0a`。两者记录的 HEAD 均为 `636931ec7508375d54b8d78ff779644cfef74bf4`。

本轮使用 15:05 重新生成的 `main-B1pE5qHo.js`。已从 bundle 核对到“最终业务输出”“节点中间输出”“定位对应日志”等本轮实现文本，因此未沿用 14:28 旧 bundle 生成的截图。

## 判定方式

每页独立按 100 分评估：强制主体结构 45 分、信息层级 25 分、统一视觉系统 15 分、当前状态的业务可操作性 15 分。每页必须达到 85 分；不使用平均分掩盖单页失败。

唯一允许的结构例外是全局导航保持顶部。暖灰背景、黏土棕强调色、细网格表格和小圆角作为已批准的全局视觉调整保留。

## 逐页结果

### 004 批次详情：88 / 100，通过

强制结构：通过。

- 保留项目上下文、六个项目页签、返回批次列表、批次名称与状态、开始/结束/耗时/结束原因、任务统计、只读配置快照、任务查询、状态筛选、任务表及分页。
- 批内任务表已经恢复原型要求的“输入标识、任务状态、结束节点、结束时间、操作”，没有继续显示重复的“所属批次”列。
- 停止批次位于批次摘要的主操作区，运行中状态的事实和操作关系清楚。
- 细网格表格、小圆角、低阴影和暖灰层级保持一致，长内容没有撑宽页面。

剩余偏差：

- 原型把批次摘要、任务表与配置快照放在一个连续主面板中；当前用两个相邻卡片表达，视觉连续性稍弱，但阅读顺序与业务层级没有改变。
- 当前截图是运行中状态，因此没有覆盖原型中的“部分任务失败”提示条及结束原因填充态。结构评分通过不等于该状态已完成视觉验收。
- 截图右上角仍有两个创建成功 Toast；它们属于真实操作反馈，但不适合作为稳定视觉基线。

### 005 任务日志：87 / 100，通过

强制结构：通过。

- 保留任务摘要、任务状态、自动化名称、所属批次开始时间、输入标识、开始/结束/耗时以及日志、输入与输出、异常与证据三个页签。
- 主体采用原型一致的左右结构：左侧节点时间线，右侧历史日志、关键词搜索、节点筛选、级别筛选和细网格日志表。
- 节点使用冻结后的业务名称，任务使用用户可理解的顺序编号；当前画面没有技术 UUID 或节点内部标识泄漏。
- 表格密度、边框、字号和小圆角符合已批准的统一表格系统。

剩余偏差：

- 当前证据是全成功任务，不能验证原型中失败节点的危险色高亮、同节点多次尝试层级及从错误日志跳转异常证据的视觉状态。
- 截图保留了进入详情后的页面滚动位置，项目标题区未完整出现在视口中；可见主体符合原型，但这张图不应作为“页面初始顶部状态”的回归基线。
- 搜索框和筛选控件比原型更紧凑，信息密度符合全局表格决定，但右侧工具区的呼吸空间略少。

### 006 输入与输出：87 / 100，通过

强制结构：通过。

- 保留任务摘要和任务页签，主体继续使用“运行时输入快照 / 输出与产物”双栏布局。
- 左栏明确区分批次固定参数和本任务数据输入；`0`、`false`、`null` 等值保持真实语义。
- 右栏固定呈现“最终业务输出、节点中间输出、证据附件”三个区域，不再把全部结果混成一张无语义表格。
- 本轮参数任务没有最终业务输出、项目数据输入或证据附件，界面均显示明确空态；节点中间输出使用细网格事实表展示。

剩余偏差：

- 本轮夹具只填充节点中间输出，没有覆盖最终业务输出、项目记录输入和证据附件的有数据状态；这些区域的存在已验证，填充后的视觉仍需后续回归证据。
- 原型的失败任务使用更强的“无最终业务输出”结果面板，并提供当前业务记录入口；当前成功参数任务的数据事实不同，因此不能进行逐文本比较。
- 两栏内容较少时页面下半部留白较大。该留白来自真实数据量，不应通过伪造信息填满。

### 007 异常与证据：86 / 100，通过

强制结构：通过。

- 保留任务摘要、异常摘要、错误类型、发生节点、尝试次数、发生时间、问题说明和“定位对应日志”入口。
- 下方符合原型的左右结构：左侧直接内联真实失败截图，右侧显示错误记录与历史尝试。
- 点击内联截图仍可打开受控放大弹窗；`08-failure-preview.png` 证明预览入口和遮罩层真实可用。
- 发生节点和历史尝试使用业务名称，未把任务、运行、节点或证据 UUID 显示给用户。

剩余偏差：

- 当前真实点击失败只映射为“运行失败”，具体性仍低于原型的“读取页面超时”。发生节点“点击元素”、尝试次数和日志定位可以支持排查，但后续应为当前实际错误码补充安全业务映射。
- 本轮失败夹具页面内容很少，真实截图的大部分区域为空白；内联与放大布局已经验证，但截图可读性不能代表内容丰富页面的效果。
- 错误记录采用两列事实表，原型展示更多错误文件与说明字段。当前 PM3 后端没有相应文本附件事实，界面没有虚构文件入口。

## 结论与限制

四个页面均达到独立 85 分门槛，顶部导航、细网格表格和小圆角未造成主体结构偏离。与上一轮相比，阻断性偏差已经闭合：批内任务列恢复、输入输出三区固定、失败截图内联、日志定位入口可用。

本结论只覆盖上述四页和当前真实成功/失败场景。以下内容未被本轮截图证明，不能登记为已通过：

- 批次部分成功、部分失败的提示状态。
- 失败节点多次重试时的时间线层级。
- 最终业务输出、项目记录输入和证据附件的填充态。
- Windows、其他 CPU 架构、打包应用以及用户手动测试。

建议后续视觉回归保留当前四张截图，并补三组状态型证据：混合结果批次、重试后失败任务、包含项目记录与证据附件的输入输出页。它们用于补齐状态覆盖，不改变当前 PM3 的页面结构。
