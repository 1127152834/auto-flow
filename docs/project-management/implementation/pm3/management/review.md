# 管理配置有界审查记录

日期：2026-09-15。原型/业务不重新设计；实现与审查按文件归属执行。

| 问题 | 修正 | 核验与状态 |
|---|---|---|
| sourceDefault 混淆跟随浏览器与继承项目代理 | 先选显式覆盖或项目默认，再解析来源策略 | 两种相反真实资源事实反例；独立复审闭合 |
| 显式模型错误误指项目默认 | 使用“选用的模型提供方”并保留真实路径/身份 | 独立复审闭合 |
| 参数说明/模型字段未覆盖图稿 | DTO、规范化、原操作结果、生成类型与管理控件一起补齐 | 说明省略及模型三态通过 HTTP/组件验证；OpenAPI 不将省略说明生成必填 |
| 参数原始数字与跨页签草稿 | 保留独立编辑草稿、统一 dirty/valid、重置边界与错误定位 | 已有有界审查通过，不重复改写数字解析 |
| 内层筛选草稿绕过统一保存/离开保护 | InputPlanEditor 上报 draft state；外层阻止未应用提交，重置 key 传到内层 | 组件反例 + run-nQhPnq 的真实输入管理链；独立复核闭合 |
| 可选排序字段与服务端合同不一致 | 业务字段仅提供已映射字段；无绑定仍允许业务状态与系统排序 | 保留原表达式，绑定后真实 HTTP 保存成功；独立复核闭合 |
| 输入计划 422 无字段定位、参数说明错误无可访问描述 | 按稳定 inputId 映射错误，可聚焦行提示；说明绑定 aria-describedby | 独立复核闭合 |
| 创建后 replace URL 清空离开保护 | 仅项目内创建成功保留 guard；强制工作区替换继续清除旧 guard，两者均撤销旧导航 ticket | navigation RED→GREEN；真实创建后修改→返回→Escape 保留 URL/输入；独立复核闭合 |
| 部分后台检查不可用时屏蔽全部资源错误 | 管理校验保留已查到的资源问题，capability 未接入仍 unavailable/runnable=false | 真实应用 contract 验证 |

前端审查发现缺陷时没有降低标准：首次输入计划审查 FAIL，修复后复查四项 PASS；真实 E2E 随后发现导航缺陷，再修复并跑通。历史失败运行目录保留，不能以最终通过改写它们。

## 实际流程证据

`../management-runs/run-eSwQYK/result.json` 为最新完整管理流程结果（最终资源布局修复后）。测试直接运行 Electron、真实 FastAPI 与隔离 SQLite；工作流资料由真实文档服务准备，竞争编辑由真实 PUT 产生；响应丢失明确为 CDP 测试注入。重启后持久性是 GET 事实核对，不是用户打开页面。

图稿人工检查单独记录在 `visual-review.md`，不以测试用例数或自动截图完成代替视觉通过。

资源页首次视觉审查82分失败：删去重复标题、减小间距、按控件列对齐四行检查并去除该页额外摘要；同尺寸重新执行完整管理QA，独立定向复核90分通过。

目录搜索在最终规格核对发现逐字请求偏差；补充“输入两字只应有初次目录请求”反例实际失败，改独立查询草稿+Enter/搜索提交，组件及真实请求次数检查通过。首轮CDP Enter缺少字符事件，补充真实键盘 text=CR 后通过，未修改产品逻辑适配测试。历史失败 run-Jez8RB 保留。

全量前端初次超时：未改原表单实现/断言/超时；该文件单独复验和限2 worker全量通过。后端旧“任何工作流接口都不存在”断言与已批准的只读管理目录冲突，改精确两个GET白名单及认证/405/UUID校验；独立合同复审通过。原全量失败日志保留，测试专用修正后仅重跑受影响合同。
