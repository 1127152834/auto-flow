# PM8 截图对照与逐画面视觉审查

- 日期：2026-09-20
- 工作区：`/Users/zhangtiancheng/Documents/projects/autoflow-project-management-pm8`，分支 `codex/project-management-pm8`
- 权威运行：`docs/project-management/implementation/pm8/qa-runs/20260919200955/report.json`（`status: passed`）
- 实际截图：`qa-runs/20260919200955/*.png`，11 张，viewport **1440×1024**、dpr 1、`document.documentElement.scrollWidth ≤ 1440`（每张都在 `report.json` 里逐条记录，无横向撑宽）
- 原型目录（只读）：`/Users/zhangtiancheng/Documents/projects/autoflow/docs/references/project-management-prototypes-2026-09-13/latest/`
- **审查者：主协调（自审）。** 原计划的独立审查智能体三次投递均只收到模式提示、收不到任务正文（工具投递故障），因此本次逐图审查由交付者本人完成，并在 `verification.json` 的 `independentReview` 里如实登记为未执行。这不是"已通过独立审查"。

## 0. 强制结构要求（每张画面都必须满足，任一不过即该画面不通过）

| 编号 | 要求 | 判定方式 |
|---|---|---|
| S1 | 保留**顶部导航**，不换成侧边导航 | 截图中 `AutoFlow` 页头与横向主导航可见且高亮正确 |
| S2 | 二级页面只有一个页面主标题，项目上下文保持紧凑 | 二级页不重复"项目"大标题，只保留紧凑项目头 + 页签 |
| S3 | 统一**细网格表格**（1px 分隔、紧凑行高、左侧对齐、状态列带文字） | 表头与行线为细实线，无粗描边/斑马纹，状态列同时给图标与文字 |
| S4 | **小圆角**：控件 4px、卡片/弹窗 6px（`styles/radii.css`） | 弹窗与卡片视觉半径明显小于旧版 12–16px，仍可见圆角 |
| S5 | 页面级主操作只出现一次（页头/工具栏），不在多处重复 | 同一动作在截图中只出现一个入口 |
| S6 | 1440×1024 下无横向溢出 | `report.json` 每张的 `scrollWidth ≤ 1440` |
| S7 | 状态不靠颜色单独表达 | 状态文案与图标同时存在 |
| S8 | 弹窗为受控对话框，可 Escape / 取消退出并恢复焦点 | 组件测试 + 运行中实际关闭（见 `verification.json`） |

**已知且已批准的全局偏离（不计入扣分）：** 原型画板是**侧边导航**，本项目按用户明确指令保留**顶部导航**；表格密度按用户确认的"统一细网格表格"方案收紧，比原型更紧凑。除这两项外，页面主体布局、信息层级与操作位置按原型。

## 1. 逐画面评分

评分口径：100 分制，≥85 视为通过；**任一强制结构要求不过则该画面不通过**，不用平均分掩盖。

| # | 截图 | 对照画板 | 分数 | 强制结构 | 结论 |
|---|---|---|---|---|---|
| 1 | `01-runs-batch-running.png` | `03-runs/004-batch-detail-approved-459f25.png` | **88** | S1–S8 全过 | 通过 |
| 2 | `02-settings-blockers.png` | 无对应画板（PM8-C1 新面） | **88** | S1–S8 全过 | 通过 |
| 3 | `03-archive-blocked.png` | `05-data/015-delete-table-blocked-2b8a64.png` | **85** | S1–S8 全过 | 通过（见偏离 D1） |
| 4 | `04-archive-confirm.png` | `02-automation/004-delete-confirm-54c904.png` | **86** | S1–S8 全过 | 通过（见偏离 D2） |
| 5 | `05-archived-readonly.png` | `02-automation/010-archived-readonly-46a6a4.png` | **88** | S1–S8 全过 | 通过（见偏离 D3） |
| 6 | `06-export-dialog.png` | 无对应画板（PM2 导出，PM8 只读复核） | **86** | S1–S8 全过 | 通过 |
| 7 | `07-resource-referenced.png` | `05-data/015-delete-table-blocked-2b8a64.png` | **87** | S1–S8 全过 | 通过 |
| 8 | `08-delete-stale-impact.png` | `05-data/015-delete-table-blocked-2b8a64.png` | **89** | S1–S8 全过 | 通过 |
| 9 | `09-delete-name-guard.png` | `02-automation/004-delete-confirm-54c904.png` | **88** | S1–S8 全过 | 通过 |
| 10 | `10-deleted-project-kept-neighbour.png` | `00-projects/100-projects-prototype-5238b4.png` | **87** | S1–S8 全过 | 通过 |
| 11 | `11-archive-response-loss.png` | 无对应画板（PM8-B1 响应丢失恢复） | **86** | S1–S8 全过 | 通过 |

最低分 85（画面 3），无画面低于门槛。

## 2. 关键画面对照说明

**画面 1（批次详情，vs `03-runs/004`）** —— 面包屑「项目 / 运行记录 / 批次详情」、紧凑项目头、六页签（运行记录高亮）、「返回批次列表」、批次卡（运行中徽标 + 开始/结束/耗时/结束原因）、「本批次 1 个任务 成功 0 失败或异常 0 进行中 1」、任务工具栏（搜索 + 自动化 + 状态 + 时间）与六列表格、分页、底部「本次运行的配置快照」折叠条，与画板逐块对应。
偏离：画板把批次元信息放在标题同一行（6 列一行），实际拆为标题下方一行 4 列；画板的失败汇总警示条只在有失败时出现（本画面无失败，故不显示，PM7 已单独覆盖该状态）。

**画面 3（归档被真实阻断，vs `05-data/015`）** —— 弹窗列出「归档影响」（删除 3 张数据表及其记录、删除 1 个自动化）与「过程阻断」（批次尚未结束、任务尚未结束，各带真实 `state`）。
偏离 D1：画板中被阻断的主操作是**禁用**的；本项目归档的阻断是**过程阻断而非拒绝**（`design/pm8-lifecycle-recovery.md` 第 57 行明确："归档预检出的 blockers 是过程阻断（列出真实对象与处理入口），不是拒绝；删除预检出的 blockers 是拒绝"），所以「归档项目」保持可点、由后端在事务内拒绝并给出 409 `LIFECYCLE_CONFLICT`。这是规格要求的行为差异，不是视觉缺陷。删除路径的禁用语义见画面 8/9。

**画面 4（归档确认，vs `02-automation/004`）** —— 标题 + 说明、影响清单、「过程阻断 / 没有阻断项，将立即归档。」、取消 + 归档项目。
偏离 D2：`02-automation/004` 是**删除**确认画板，含琥珀警示条与「将删除/将保留」分组面板；归档是**可逆**操作，因此实际只保留纯文本说明与影响清单，未套用删除特有的警示与分组。删除路径在画面 8/9 按画板结构实现。

**画面 5（归档只读，vs `02-automation/010`）** —— 顶部信息条「项目已归档，仅可查看与导出」、项目名右侧「已归档」徽标、页签保留、数据表卡标「本地数据 | 只读」、记录行操作退化为「查看」。
偏离 D3：画板把归档提示写在项目头内部，实际放成项目头上方的整宽信息条。信息等价、层级更醒目，判定为可接受偏离。

**画面 6（只读导出）** —— 「导出 Excel」对话框：导出范围（整张表）、数据列勾选（姓名）、「包含业务状态（当前记录将导出为空状态）」与主操作「选择保存位置」。归档项目仍可走真实导出并产出 4859 字节的 `人员.xlsx`（`PK` 魔数）。

**画面 7（全局资源引用保护）** —— 从顶部导航进入「浏览器配置」，对仍被项目引用的配置执行真实删除：「资源仍被项目或自动化引用，请先处理引用」+「仍被以下对象引用，删除会破坏它们下次运行：PM8 生命周期验收 · 自动化「PM8 生命周期自动化」（environmentPolicy.profileId）」+「请先在对应项目或自动化里改用别的资源，再回来删除。」引用对象由服务端返回，界面不提供强制删除。
（本轮修正：此前该截图取在纯 API 调用之后，画面停在项目目录，看不到引用清单；已在 QA 脚本里改为真实界面路径后重跑。）

**画面 8（删除影响过期）** —— 同上的删除对话框在被注入"影响过期"后：「影响范围可能已变化，请重新核对后再确认。」「重新核对后需再次输入名称。」+「重新核对影响」+「删除影响待重新读取。」，主操作禁用。与画板"先核对再执行"的阻断结构一致。

**画面 9（确认名不符）** —— 「永久删除项目」+「此操作无法撤销，请核对影响范围。」+「将删除」清单 +「将保留：外部 Excel 原文件、远端 Sheets 与全局资源、其它项目的对象。」+「必须先处置 / 没有阻断项。」+「输入项目名称以确认」，输入错误名称时主操作禁用；直连接口同样返回 422。

**画面 10（删除后邻居保留）** —— 全部项目目录切到「全部状态」，只剩「PM8 保留项目」，被删除的 A 不再出现。同一画面同时可核对"A 已消失、B 仍保留"。
（本轮修正：此前该截图停在"已归档"筛选的 0 条空视图，看不到保留项目。）

**画面 11（响应丢失后找回）** —— 归档对话框在命令结果未确认时显示「上次保存结果尚未确认，请核对保存结果」并保留重试入口；按原幂等身份核对后只存在一条归档事实。

## 3. 未覆盖的画面与原因

| 画板 | 状态 | 原因 |
|---|---|---|
| `03-runs/018`（检查失败 → 重新检查） | 未执行 | 依赖真实生产执行核心；PM8 不扩围 |
| `03-runs/021`（证据缺失） | 未执行 | 同上，PM5 已登记为缺口 |
| `06-environments/*`、`04-statistics/*` | 未执行 | 属 PM5/PM7 范围，PM8 只做回归未逐图复评 |
| 200% 缩放、键盘/焦点、长文本的逐图证据 | 部分执行 | 组件测试覆盖 Escape/焦点/Escape 嵌套；本机未逐画面出 200% 截图，按"未执行"登记 |

## 4. 结论

- 11 张真实截图全部满足 S1–S8，逐画面最低 85 分，无画面低于门槛。
- 三处与画板的结构性差异（D1 归档阻断非拒绝、D2 归档用可逆确认样式、D3 归档提示为整宽信息条）都有规格或语义依据，按可接受偏离登记，不隐藏。
- **本审查由交付者自审，独立审查因工具投递故障未执行**；不构成独立评审通过。
