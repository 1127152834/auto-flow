# 后续里程碑恢复盘点

日期：2026-09-14。状态：confirmed（代码与提交盘点），后续实施顺序为proposed。用户已接受表格和小圆角方向，并要求检查尚未完成的里程碑。

核对基线：主项目codex/architecture-baseline@6f2fe2a；UI交付codex/global-table-system@f814173。主目录存在Studio文档、WebDAV等其他任务未提交内容，本次未修改。git祖先检查确认f814173未进入主线。

| 范围 | 当前真实状态 | 下一步 |
|---|---|---|
| PM0 | 契约/基线文档已交付，但部分执行器前提已过时 | PM3开工同步合同，不重做PM0 |
| PM1 | 项目目录/编辑/打开/概览基础已实现并合入 | 集成回归 |
| PM2 | 本地表、字段、状态、记录、Excel和批状态已实现并合入 | 保留已有业务证据；完成组合验收尾项 |
| R1–R3 | 页面层级、记录详情/编辑、字段聚合草稿与原子保存已有实现 | 不重做页面；正常新增遵循已合入的同表录入 |
| 全局精细网格与小圆角 | d3afba6/f814173已交付并获用户方向确认 | 在隔离集成区汇合最新主线，避免覆盖Studio工作 |
| PM3 | 尚未实现项目自动化与真实参数批次 | 下一正式业务里程碑 |
| PM4 | 尚未实现多表原子领取、占用和工作流显式写表 | PM3与核心变量/控制流就绪后推进 |
| PM5 | 尚未实现业务环境持久化、End完整保留和人工继续 | 依赖PM4及核心检查点 |
| PM6 | 尚未实现Google Sheets连接与同步闭环 | 依赖PM2/PM4，可与PM5并行 |
| PM7 | 尚未实现完整运行查询、统计下钻和失败后续批次 | 依赖真实运行/业务事实 |
| PM8 | 尚未实现完整归档/恢复/删除与跨资源清理 | 依赖运行/环境/同步的完整资源事实 |
| PM9 | 尚未完成全链、规模、Windows/macOS安装包验收 | 在完整功能后联合验证 |

## PM3开工前必须修正的事实

`apps/backend/src/autoflow/application/projects/service.py`当前仅data=available，automations/runs/environments/statistics/sync仍为notImplemented。`bootstrap/project_http_routes.py`没有注册项目自动化或运行服务。`adapters/http/workflow_studio_openapi.py`明确仅发布前端类型、不安装工作流业务路由。Studio当前状态见主项目.ai/memory/studio-status.md；已有编辑器与Mock协议不等于正式文档/执行服务。旧0005–0008迁移存在不证明执行器仍可用；迁移已由0009_merge_project_data汇合，不重复规划同一合并。

原PM3需C01/C02真实文档保存以及C05/C06/C07 Run/日志/取消/浏览器生命周期；这些能力要按当前Studio前后端边界重新确认负责人和交付门槛，不在项目模块创建另一套执行器。主线Studio工作仍进行中，不依据过时F0勾选或静态Schema推断其全部前端已验收。

## 建议的直接执行顺序

1. 在隔离集成工作区汇合已确认表格/圆角与主线正式提交，保留其他任务未提交文件。
2. 编写修订PM3详细规格/执行卡：对原图库逐页映射，定义自动化管理、四页签聚合保存、参数/资源策略、Studio文档身份、启动/停止/查询恢复；同步旧合同前提。
3. PM3-A先完成真实管理配置与文档关联的垂直切片；前置真实文档能力必须由核心提供。
4. 核心门槛满足后交付PM3-B/C：一次参数运行→真实浏览器动作→结果/日志→停止与响应丢失恢复。各包包含端到端脚本、截图和手动验收。

既有组合验收尾项包括行内新增旧草稿/重新导入兼容、原生输入法、Windows和打包；main-integration/verification.json明确originalG4Complete=false。后续针对当前版本补验证，不据此宣称所有已实现代码未完成，也不把旧partial直接改成passed。

本次只完成状态核对与恢复顺序，没有改业务代码、启动PM3执行器或合并主目录。旧execution-ledger首页PM2 inProgress与原始总计划勾选不是当前实现证据，本快照按最新代码和后续交付记录作勘误。
