# Android into current baseline integration
2026-09-24; user explicitly requested code merge to baseline. Baseline initially8e5564e0 but another user-owned task concurrently fast-forwarded it to PM9 runtime90d77acd and metadata c6e02427. Our isolated candidate codex/android-baseline-integration started c6e02427; active no-commit merge of Android746c9c5b. Merge base 31bfbb514bb2dc56cf9a4213448c06bc0ef74400 (verified with git merge-base; a92f0688 is a later shared feature reference, not this merge base). Source Android full backend4124/frontend5689 passed; latest PM9 local source results independent, native CI has known failures that must not be hidden. Need preserve both feature sets, current safety/runtime and incremental database history; no broad ours/theirs selection, no retired workflow revival.

TaskB backend: resolve Python and spec conflicts preserving both PM9 changes and Android management/control/backup/runtime fencing. Add incremental Alembic merge revision if migration graph has twoheads; don't rewrite applied revisions. Update expected heads and add real SQLite from-each-existing-head upgrade regression if needed. TDD observed failure before smallest resolution; report actual covering tests/logs. Own backend files only, excluding generated OpenAPI frontend.
TaskF frontend/scripts: resolve workflow frontend test/audit and smoke script/capabilities conflicts preserving PM9 and Android requirements. Own conflicted frontend files excluding shared/api/generated.ts, smoke JS conflicts, capabilities.json. No product feature redesign.
TaskR parent: preserve .ai context from both sides; prepare dependencies; generate OpenAPI only afterbackend compiles; run final integrated tests/gates, independent scoped merge review, commit candidate, then update baseline only when concurrent writer is finished and clean/currentexpected head. No push. Preserve original Android worktree's 3 inherited Studio docs, source branches, all other worktrees/main modifications.
Dependency check: B creates contracts and merged migrationhead; R generates types consumed byF (F leaves generated.ts alone). F smoke scripts depend on B session/token/runtime semantics and must preserve current wrappers. .ai knowledge rows preserve both histories. No concurrent writes to same files. All report artifacts here; no agent commits while merge inprogress.
