[project]
name = "autoflow-backend"
version = "0.1.0"
description = "AutoFlow backend service"
requires-python = ">=3.11,<3.12"
dependencies = [
  "fastapi",
  "pydantic",
  "uvicorn",
]

[dependency-groups]
dev = [
  "httpx",
  "pytest",
  "pytest-asyncio",
  "ruff",
  "mypy",
]
