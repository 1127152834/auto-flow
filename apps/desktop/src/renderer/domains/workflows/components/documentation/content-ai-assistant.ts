// Source: WebRPA@5ccb900e, components/workflow/documentation/content-ai-assistant.ts; see SOURCE.md for license and adaptation boundaries.
export const aiAssistantGuideContent = `# AI 小助手指南

> 当前 Studio 教学范围为保留节点及前端配置交互。Mock 用于交互演示，不执行真实网页、模型或系统操作；文中的执行行为需正式后端支持。

AI 小助手面板用于输入需求、查看回复和工具记录，并将支持的画布操作应用到当前工作流。当前 Mock 提供交互演示，不调用真实模型，也不代表外部工具已执行。

## 打开与配置

使用工具栏的小助手入口打开面板，在全局配置中设置 AI 助手的服务地址、模型及工具权限。查看实际连接错误后再调整配置。

## 画布操作

当前客户端支持读取画布、添加和删除保留节点、修改参数、连接节点、管理变量、整理布局以及撤销和重做。

例如输入“列出当前画布节点”或“添加一个打开网页节点”。收到修改结果后检查节点及参数；需要回退时使用撤销，或使用提供的快照回退入口。

## 权限与取消

需要批准的操作会显示请求。批准或拒绝以当前请求为准；取消回答后，不应把尚未完成的操作当作成功。操作记录用于检查具体变更。

## 会话与历史

面板提供新建会话、历史记录和消息展示。工具回复中的失败与取消状态应保留，不能只凭自然语言回复认定保存、运行或外部请求成功。

## 节点配置辅助

脚本编辑器中的 AI 编码助手可以提交需求并预览服务返回的代码。确认后再应用到编辑器，检查引用的变量、节点类型及参数。生成结果仍需人工检查和运行验证。
`
