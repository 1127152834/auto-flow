// Source: WebRPA@5ccb900e, components/workflow/documentation/content-advanced.ts; see SOURCE.md for license and adaptation boundaries.
export const advancedFeaturesContent = `# 高级功能

> 当前 Studio 教学范围为保留节点及前端配置交互。Mock 用于交互演示，不执行真实网页、模型或系统操作；文中的执行行为需正式后端支持。

本章介绍流程控制、AI集成、API请求等高级功能。

---

## 流程控制

### 子流程（模块复用）

将常用的模块组合封装成可复用的子流程，实现模块化和代码复用。

**创建子流程（3步）**：

1. **拖入分组**：从左侧「流程控制」分类拖入「分组」模块到画布
2. **设为子流程**：选中分组，在右侧配置面板开启「设为子流程」开关
3. **命名子流程**：输入子流程名称（如"登录流程"）

分组会变成**绿色边框**，显示「子流程定义」标识。

**在子流程内放置模块**：

1. 拖动分组边缘调整大小
2. 将需要复用的模块拖入分组内部
3. 在分组内部连接这些模块

**调用子流程**：

1. 从左侧「流程控制」分类拖入「调用子流程」模块
2. 在右侧配置面板的下拉列表中选择要调用的子流程
3. 将「调用子流程」模块连接到主流程中

**执行逻辑**：
- 子流程内的模块**不会被主流程直接执行**
- 只有执行到「调用子流程」模块时，才会执行对应分组内的所有模块
- 子流程可以**访问和修改主流程的变量**
- 同一个子流程可以在主流程中**多次调用**

**错误处理**：
- 子流程内的模块出错时，会先执行该模块的橙色错误分支（如果有）
- 然后触发「调用子流程」模块的橙色错误分支
- 子流程不会因为单个模块出错而强制结束

**使用场景**：
- 登录流程复用：多个任务都需要先登录
- 数据采集模板：相同的采集逻辑复用
- 通用操作封装：如翻页、滚动加载等

**示例**：
\`\`\`
画布布局：

┌─────────────────────────────────────┐
│  [子流程分组: 登录流程]              │
│    输入用户名 -> 输入密码 -> 点击登录   │
└─────────────────────────────────────┘

主流程：
  打开网页 -> 调用子流程(登录) -> 采集数据 -> 导出
\`\`\`

**发布与分享**：
- 导出/发布时子流程自动包含
- 其他用户下载后可直接使用和修改子流程

---

### 定时执行

在指定时间或延迟后执行后续流程，用于创建定时任务。

**定时类型**：

#### 1. 指定时间执行

在设定的日期和时间执行。

| 参数 | 说明 | 示例 |
|------|------|------|
| 执行日期 | 目标日期 | 2024-12-31 |
| 执行时间 | 目标时间 | 09:00 |

**示例**：每天早上9点执行签到
\`\`\`
定时执行：
  类型：指定时间
  日期：2024-12-26
  时间：09:00
  
-> 等待到指定时间后继续执行后续模块
\`\`\`

#### 2. 延迟执行

等待指定时长后执行。

| 参数 | 说明 | 示例 |
|------|------|------|
| 延迟小时 | 等待的小时数 | 1 |
| 延迟分钟 | 等待的分钟数 | 30 |
| 延迟秒数 | 等待的秒数 | 0 |

**示例**：延迟5分钟后执行
\`\`\`
定时执行：
  类型：延迟执行
  延迟分钟：5
  
-> 等待5分钟后继续执行后续模块
\`\`\`

**使用场景**：
- 定时签到、打卡
- 延迟发送消息
- 等待特定时间执行任务
- 避开高峰期执行

**注意事项**：
- 定时执行模块不受超时限制
- 如果目标时间已过，会立即执行
- 工作流停止时会取消等待中的定时任务

---

### 条件判断

根据条件执行不同的分支，实现"如果...则...否则..."的逻辑。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 左值 | 条件左边的值 | \`{价格}\`、\`{状态}\` |
| 运算符 | 比较方式 | ==、!=、>、< 等 |
| 右值 | 条件右边的值 | \`100\`、\`"成功"\` |

**支持的运算符**：

| 运算符 | 说明 | 示例 | 结果 |
|--------|------|------|------|
| == | 等于 | \`{count} == 10\` | count等于10时为真 |
| != | 不等于 | \`{status} != "error"\` | status不是error时为真 |
| > | 大于 | \`{price} > 100\` | price大于100时为真 |
| < | 小于 | \`{age} < 18\` | age小于18时为真 |
| >= | 大于等于 | \`{score} >= 60\` | score大于等于60时为真 |
| <= | 小于等于 | \`{num} <= 0\` | num小于等于0时为真 |
| contains | 包含 | \`{text} contains "关键词"\` | text包含"关键词"时为真 |
| startsWith | 开头是 | \`{url} startsWith "https"\` | url以https开头时为真 |
| endsWith | 结尾是 | \`{file} endsWith ".jpg"\` | file以.jpg结尾时为真 |

**分支连接**：

条件判断模块有两个输出：
- **[√] 真**：条件成立时执行
- **[×] 假**：条件不成立时执行

**示例**：检查登录状态
\`\`\`
条件判断：{登录状态} == "已登录"
  ├─ 真 -> 执行签到操作
  └─ 假 -> 执行登录操作
\`\`\`

---

### 循环执行

重复执行一段流程。

**循环类型**：

#### 1. 计数循环

执行固定次数。

| 参数 | 说明 |
|------|------|
| 循环次数 | 要执行的次数 |
| 索引变量 | 循环索引的变量名（默认：loop_index） |

**自动创建的变量**：
- \`{loop_index}\`：当前是第几次（从0开始，可自定义名称）

**示例**：翻页采集10页数据
\`\`\`
循环执行 10 次
  ├─ 采集当前页数据
  ├─ 点击下一页
  └─ 等待页面加载

可以用 {loop_index} 知道当前是第几页（0-9）
\`\`\`

#### 2. 条件循环

满足条件时持续执行。

| 参数 | 说明 |
|------|------|
| 循环条件 | 继续循环的条件 |

**示例**：循环直到没有下一页
\`\`\`
条件循环：{有下一页} == true
  ├─ 采集当前页数据
  ├─ 检查是否有下一页
  ├─ 点击下一页
  └─ 等待页面加载
\`\`\`

---

### 遍历列表

对列表中的每个元素执行操作。

**配置项**：

| 参数 | 说明 |
|------|------|
| 数据源变量 | 要遍历的列表变量名（直接填变量名，不加花括号） |
| 循环变量 | 当前元素的变量名（默认：item） |
| 索引变量 | 当前索引的变量名（默认：index） |

**自动创建的变量**：
- \`{item}\`：当前正在处理的元素（可自定义名称）
- \`{index}\`：当前元素的索引（从0开始，可自定义名称）

**示例**：遍历URL列表采集数据
\`\`\`
设置变量：网址列表 = ["url1", "url2", "url3"]

遍历列表：网址列表（注意：直接填变量名，不加花括号）
  ├─ 打开网页：{item}
  ├─ 获取数据
  └─ 打印日志：正在处理第 {index} 个网址
\`\`\`

**提示**：循环变量 \`item\`、\`index\` 会自动出现在变量智能提示中。

---

### 跳出循环 / 跳过当前循环

控制循环的执行流程。

**跳出循环（Break）**：
- 立即结束整个循环
- 跳到循环后面的模块继续执行

**跳过当前循环（Continue）**：
- 跳过本次循环的剩余部分
- 直接进入下一次循环

**示例**：找到目标后停止
\`\`\`
遍历列表：商品列表（直接填变量名）
  ├─ 条件判断：{item[price]} < 100
  │   ├─ 真 -> 设置变量：找到的商品 = {item}
  │   │       跳出循环
  │   └─ 假 -> 跳过当前循环
\`\`\`

---

## AI大脑

调用AI大模型处理文本，让工作流更智能。

### 支持的模型

| 提供商 | 模型 | API地址 |
|--------|------|---------|
| OpenAI | gpt-3.5-turbo、gpt-4 | https://api.openai.com/v1 |
| 智谱AI | glm-4、glm-3-turbo | https://open.bigmodel.cn/api/paas/v4 |
| Deepseek | deepseek-chat | https://api.deepseek.com/v1 |
| 月之暗面 | moonshot-v1-8k | https://api.moonshot.cn/v1 |
| 其他 | 兼容OpenAI接口的模型 | 自定义 |

### 配置说明

| 参数 | 说明 | 示例 |
|------|------|------|
| API地址 | 模型接口地址 | https://api.openai.com/v1 |
| API密钥 | 你的API Key | sk-xxx... |
| 模型名称 | 要使用的模型 | gpt-3.5-turbo |
| 系统提示词 | 设定AI的角色和行为 | 你是一个数据分析专家 |
| 用户消息 | 发送给AI的内容 | 请分析以下数据：{数据} |
| 保存到变量 | 存储AI回复的变量名 | AI回复 |
| 温度 | 创造性程度（0-1） | 0.7 |

### 系统提示词技巧

**角色设定**：
\`\`\`
你是一个专业的商品评论分析师，擅长从评论中提取关键信息。
\`\`\`

**输出格式**：
\`\`\`
请以JSON格式输出，包含以下字段：
- sentiment: 情感（正面/负面/中性）
- keywords: 关键词列表
- summary: 一句话总结
\`\`\`

**限制条件**：
\`\`\`
请只输出JSON，不要有其他解释文字。
回答要简洁，不超过100字。
\`\`\`

### 实用场景

#### 1. 文章摘要
\`\`\`
系统提示词：你是一个文章摘要专家，请用3句话总结文章要点。
用户消息：{文章内容}
\`\`\`

#### 2. 情感分析
\`\`\`
系统提示词：分析以下评论的情感倾向，只回答：正面、负面或中性
用户消息：{评论内容}
\`\`\`

#### 3. 数据提取
\`\`\`
系统提示词：从文本中提取联系方式，以JSON格式输出：{"phone": "", "email": ""}
用户消息：{网页文本}
\`\`\`

#### 4. 内容生成
\`\`\`
系统提示词：你是一个营销文案专家
用户消息：请为以下商品写一段50字的推广文案：{商品信息}
\`\`\`

---

## AI视觉

调用视觉理解模型分析图片内容。

### 支持的模型

| 提供商 | 模型 | 说明 |
|--------|------|------|
| 智谱AI | glm-4v-flash | 免费，推荐 |
| 智谱AI | glm-4v | 付费，更强 |
| OpenAI | gpt-4-vision-preview | 付费 |

### 图片来源

| 来源 | 说明 | 使用场景 |
|------|------|----------|
| 元素截图 | 截取指定元素的图片 | 验证码识别 |
| 页面截图 | 截取整个页面 | 页面分析 |
| 图片URL | 网络图片地址 | 分析在线图片 |
| 变量 | Base64编码或本地路径 | 使用之前保存的图片 |

### 配置说明

| 参数 | 说明 |
|------|------|
| API地址 | 视觉模型接口地址 |
| API密钥 | API Key |
| 模型名称 | 如 glm-4v-flash |
| 图片来源 | 选择图片获取方式 |
| 提示词 | 告诉AI要做什么 |
| 保存到变量 | 存储识别结果 |

### 实用场景

#### 1. 验证码识别
\`\`\`
图片来源：元素截图
选择器：#captcha-img
提示词：请识别图片中的验证码，只输出验证码字符，不要其他内容
\`\`\`

#### 2. 商品图片分析
\`\`\`
图片来源：图片URL
URL：{商品图片链接}
提示词：描述这个商品的外观特征，包括颜色、材质、风格
\`\`\`

#### 3. 图表数据提取
\`\`\`
图片来源：元素截图
选择器：.chart-container
提示词：提取图表中的数据，以JSON格式输出
\`\`\`

---

## AI智能爬虫（实验性功能）

> **实验性功能提示**：AI智能爬虫和AI元素选择器是实验性功能，依赖第三方AI服务，可能存在不稳定性，不推荐在生产环境使用。

AI智能爬虫利用大语言模型的理解能力，通过自然语言描述即可从网页中提取数据，无需编写复杂的选择器。

### AI智能爬虫

使用自然语言描述要提取的数据，AI会自动分析网页并提取相应内容。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| API密钥 | OpenAI兼容的API Key | sk-xxx... |
| 基础URL | API接口地址 | https://api.openai.com/v1 |
| 模型名称 | 使用的模型 | gpt-4o-mini |
| 提取指令 | 用自然语言描述要提取的内容 | 提取商品名称、价格和评分 |
| 输出格式 | JSON或Markdown | JSON |
| 保存到变量 | 存储提取结果的变量名 | 提取结果 |

**使用示例**：
\`\`\`
打开网页：https://example.com/product/123

AI智能爬虫：
  提取指令：提取商品名称、价格、库存数量、商品描述
  输出格式：JSON
  保存到变量：商品信息
  
打印日志：{商品信息}
\`\`\`

**输出结果示例**：
\`\`\`json
{
  "商品名称": "无线蓝牙耳机",
  "价格": "299元",
  "库存数量": "1000+",
  "商品描述": "高品质音质，长续航..."
}
\`\`\`

### AI元素选择器

使用自然语言描述要定位的元素，AI会自动生成CSS选择器。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| API密钥 | OpenAI兼容的API Key | sk-xxx... |
| 基础URL | API接口地址 | https://api.openai.com/v1 |
| 模型名称 | 使用的模型 | gpt-4o-mini |
| 页面URL | 要分析的网页地址 | https://example.com |
| 页面加载等待 | 等待页面加载的时间（秒） | 3 |
| 元素描述 | 用自然语言描述要定位的元素 | 搜索按钮 |
| 保存到变量 | 存储生成的选择器 | 搜索按钮选择器 |

**使用示例**：
\`\`\`
AI元素选择器：
  页面URL：https://www.baidu.com
  元素描述：搜索输入框
  保存到变量：输入框选择器
  
输入文本：
  选择器：{输入框选择器}
  内容：AutoFlow
\`\`\`

**注意事项**：
- 这两个功能需要消耗AI API额度
- 提取准确性取决于网页结构和AI模型能力
- 复杂页面可能需要多次尝试和调整提示词
- 建议先在测试环境验证效果
- 对于稳定的生产环境，推荐使用传统的CSS选择器方式

---

## API请求

发送HTTP请求，与外部服务交互。

### 请求方法

| 方法 | 用途 |
|------|------|
| GET | 获取数据 |
| POST | 提交数据 |
| PUT | 更新数据 |
| DELETE | 删除数据 |
| PATCH | 部分更新 |

### 配置说明

| 参数 | 说明 | 示例 |
|------|------|------|
| URL | 请求地址 | https://api.example.com/data |
| 方法 | HTTP方法 | GET、POST等 |
| 请求头 | HTTP头部 | {"Authorization": "Bearer xxx"} |
| 请求体 | POST/PUT的数据 | {"name": "test"} |
| 保存到变量 | 存储响应结果 | 响应数据 |

### 请求头设置

常用请求头：
\`\`\`json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer {token}",
  "User-Agent": "Mozilla/5.0"
}
\`\`\`

### 请求体格式

JSON格式：
\`\`\`json
{
  "username": "{用户名}",
  "password": "{密码}",
  "data": {数据}
}
\`\`\`

### 使用响应数据

API响应通常是JSON格式，保存到变量后可以这样访问：

\`\`\`
假设响应：{"code": 200, "data": {"name": "test", "items": [1,2,3]}}

{响应[code]}           -> 200
{响应[data][name]}     -> test
{响应[data][items][0]} -> 1
\`\`\`

### 实用示例

#### 1. 获取天气
\`\`\`
URL: https://api.weather.com/v1/current?city={城市}
方法: GET
保存到: 天气数据

然后使用：{天气数据[temperature]}°C
\`\`\`

#### 2. 发送通知
\`\`\`
URL: https://api.pushplus.plus/send
方法: POST
请求体: {
  "token": "{推送token}",
  "title": "价格提醒",
  "content": "{商品名} 降价到 {价格} 元了！"
}
\`\`\`

#### 3. 提交表单数据
\`\`\`
URL: https://api.example.com/submit
方法: POST
请求头: {"Content-Type": "application/json"}
请求体: {
  "name": "{姓名}",
  "email": "{邮箱}",
  "message": "{留言内容}"
}
\`\`\`

---

## 发送邮件

自动发送邮件通知。

### 配置说明

| 参数 | 说明 | 示例 |
|------|------|------|
| SMTP服务器 | 邮件服务器地址 | smtp.qq.com |
| 端口 | 服务器端口 | 465（SSL）或 587（TLS） |
| 发件人邮箱 | 你的邮箱地址 | xxx@qq.com |
| 授权码 | SMTP授权码 | 不是登录密码！ |
| 收件人 | 目标邮箱 | 多个用逗号分隔 |
| 主题 | 邮件标题 | 支持变量 |
| 内容 | 邮件正文 | 支持变量和HTML |

### 获取QQ邮箱授权码

1. 登录 [QQ邮箱网页版](https://mail.qq.com)
2. 点击 **设置** -> **账户**
3. 找到 **POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV服务**
4. 开启 **POP3/SMTP服务**
5. 按提示发送短信验证
6. 获得16位授权码

### 常用SMTP服务器

| 邮箱 | 服务器 | SSL端口 | TLS端口 |
|------|--------|---------|---------|
| QQ邮箱 | smtp.qq.com | 465 | 587 |
| 163邮箱 | smtp.163.com | 465 | 25 |
| Gmail | smtp.gmail.com | 465 | 587 |
| Outlook | smtp.office365.com | - | 587 |

### 邮件内容示例

\`\`\`
主题：【价格监控】{商品名} 降价提醒

内容：
您关注的商品价格有变动：

商品名称：{商品名}
原价：{原价} 元
现价：{现价} 元
降价幅度：{降幅}%

点击查看：{商品链接}

---
此邮件由AutoFlow自动发送
\`\`\`

---

## 下载文件

从网页下载文件到本地。

### 下载模式

| 模式 | 说明 | 使用场景 |
|------|------|----------|
| 点击下载 | 点击下载按钮触发下载 | 需要登录或有权限验证的下载 |
| URL下载 | 直接通过URL下载 | 已知文件直链地址 |

### 点击下载模式

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 选择器 | 下载按钮的选择器 | \`#download-btn\` |
| 保存目录 | 文件保存的文件夹 | \`C:/Downloads\` |
| 自定义文件名 | 可选，指定文件名 | \`report.pdf\` |

### URL下载模式

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 文件URL | 文件的下载地址 | \`https://example.com/file.pdf\` |
| 保存目录 | 文件保存的文件夹 | \`C:/Downloads\` |
| 自定义文件名 | 可选，指定文件名 | \`{日期}_report.pdf\` |

**路径选择**：点击保存目录输入框右侧的 按钮可以通过资源管理器选择文件夹

---

## 剪贴板操作

### 设置剪贴板

将文本或图片设置到系统剪贴板。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 内容类型 | 文本或图片 | text / image |
| 文本内容 | 要复制的文本 | \`{数据}\`，支持变量 |
| 图片路径 | 图片文件路径 | \`C:/images/pic.png\` |

**使用场景**：
- 复制数据到剪贴板供其他程序使用
- 配合键盘操作实现粘贴图片到输入框
- 跨应用数据传递

**示例**：粘贴图片到聊天输入框
\`\`\`
1. 设置剪贴板：图片路径 = C:/images/screenshot.png
2. 点击聊天输入框
3. 键盘操作：Ctrl+V
\`\`\`

---

## 执行JavaScript

在页面中执行自定义JavaScript代码。

### 基础用法

\`\`\`javascript
// 返回值会保存到指定变量
return document.title;
\`\`\`

### 常用示例

**获取页面信息**：
\`\`\`javascript
return {
  title: document.title,
  url: window.location.href,
  scrollHeight: document.body.scrollHeight
};
\`\`\`

**获取所有链接**：
\`\`\`javascript
return Array.from(document.querySelectorAll('a'))
  .map(a => ({text: a.textContent, href: a.href}));
\`\`\`

**修改页面元素**：
\`\`\`javascript
document.querySelector('#element').style.display = 'block';
\`\`\`

**滚动操作**：
\`\`\`javascript
window.scrollTo({top: 1000, behavior: 'smooth'});
\`\`\`

**等待元素**：
\`\`\`javascript
await new Promise(resolve => {
  const check = () => {
    if (document.querySelector('#target')) resolve();
    else setTimeout(check, 100);
  };
  check();
});
return true;
\`\`\`

---

## 用户输入

在工作流执行过程中弹出对话框，让用户输入数据。

### 配置说明

| 参数 | 说明 |
|------|------|
| 提示文字 | 显示给用户的提示 |
| 默认值 | 输入框的默认内容 |
| 保存到变量 | 存储用户输入的变量名 |

### 使用场景

- 输入验证码
- 输入动态密码
- 确认操作
- 输入搜索关键词

---

## 暂停执行

暂停工作流执行，等待用户手动继续。

### 使用场景

- 需要人工检查
- 等待外部操作完成
- 调试断点

---

## 系统操作模块

当前目录保留执行命令、关机/重启和锁定屏幕的配置入口；本节不包含桌面键鼠节点。

### 执行命令

执行系统命令（CMD或PowerShell）。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 命令 | 要执行的命令 | \`dir C:\\\\Users\` |
| 执行方式 | CMD/PowerShell | cmd |
| 超时时间 | 命令执行超时（秒） | 30 |
| 存储变量名 | 保存命令输出的变量 | output |

**安全警告**：请谨慎执行系统命令，避免执行未知来源的命令。

---

### 关机/重启

执行系统电源操作。

**操作类型**：

| 类型 | 说明 |
|------|------|
| 关机 | 关闭计算机 |
| 重启 | 重新启动计算机 |
| 注销 | 注销当前用户 |
| 休眠 | 进入休眠模式 |
| 睡眠 | 进入睡眠模式 |

**配置项**：
- 延迟时间：执行前等待的秒数
- 强制执行：是否强制关闭程序

---

### 锁定屏幕

锁定Windows屏幕，相当于按下 Win+L。

---

## 异常处理

每个模块都有一个**橙色的异常处理连接点**（位于模块右侧），当模块执行失败时，会自动触发连接到该点的流程。

### 工作原理

\`\`\`
                    ┌─-> 异常处理流程
模块 ──┬─-> 正常流程  │
       └─-> (error) ─┘
\`\`\`

- **正常流程**：模块执行成功时，从底部连接点继续
- **异常处理**：模块执行失败时，从右侧橙色连接点触发

### 使用方法

1. 从模块右侧的**橙色连接点**拖出连线
2. 连接到异常处理模块（如打印日志、发送通知等）
3. 当该模块执行失败时，会自动执行异常处理流程

### 示例：网络请求异常处理

\`\`\`
API请求 ──┬─-> 处理响应数据 -> 继续流程
          └─-> (error) -> 打印日志："请求失败" -> 发送通知
\`\`\`

### 示例：元素点击异常处理

\`\`\`
点击元素 ──┬─-> 继续操作
           └─-> (error) -> 截图保存 -> 重试逻辑
\`\`\`

### 特性

| 特性 | 说明 |
|------|------|
| **自动触发** | 模块失败时自动执行异常处理分支 |
| **不影响主流程** | 有异常处理时，失败不会中断整个工作流 |
| **所有模块支持** | 每个模块都有异常处理连接点 |

### 最佳实践

**[√] 推荐的异常处理**：
- 记录错误日志
- 发送失败通知
- 保存错误截图
- 执行重试逻辑
- 设置默认值继续执行

**示例：完整的异常处理流程**
\`\`\`
打开网页 ──┬─-> 正常流程...
           └─-> (error) -> 打印日志 -> 等待5秒 -> 重新打开网页
\`\`\`

---

## AI识别

AutoFlow 提供了基于AI的图像识别功能，包括人脸识别和图片OCR文字识别。

---

### 人脸识别

比较两张图片中的人脸是否匹配，可用于身份验证、人脸比对等场景。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 识别图片路径 | 待识别的图片（可包含多个人脸） | \`C:/images/photo.jpg\` |
| 目标人脸图片 | 用于比对的目标人脸图片 | \`C:/images/target.jpg\` |
| 匹配容差 | 0-1，越小越严格 | 0.6（默认） |
| 结果变量名 | 存储识别结果的变量 | face_match_result |

**分支输出**：

人脸识别模块有两个输出分支（类似条件判断）：
- **[√] 匹配**：检测到匹配的人脸时执行
- **[×] 不匹配**：未检测到匹配的人脸时执行

**结果变量内容**：

\`\`\`json
{
  "matched": true,
  "confidence": 85.5,
  "source_faces": 3,
  "target_faces": 1,
  "best_distance": 0.145
}
\`\`\`

| 字段 | 说明 |
|------|------|
| matched | 是否匹配 |
| confidence | 匹配置信度（百分比） |
| source_faces | 识别图片中检测到的人脸数量 |
| target_faces | 目标图片中检测到的人脸数量 |
| best_distance | 最佳匹配距离（越小越相似） |

**容差值参考**：

| 容差 | 说明 | 适用场景 |
|------|------|----------|
| 0.4 | 非常严格 | 高安全性验证 |
| 0.5 | 较严格 | 一般身份验证 |
| 0.6 | 默认值 | 推荐使用 |
| 0.7 | 较宽松 | 相似人脸查找 |

**示例**：员工打卡验证
\`\`\`
人脸识别：
  识别图片：{摄像头截图}
  目标人脸：C:/employees/{工号}.jpg
  容差：0.5
  
  ├─ 匹配 -> 记录打卡成功
  └─ 不匹配 -> 提示验证失败
\`\`\`

**注意事项**：
- 首次使用需要安装 face_recognition 库（依赖 dlib）
- 图片中应包含清晰的正面人脸
- 光线、角度会影响识别准确度
- 建议使用高质量的目标人脸图片

---

### 图片OCR

识别图片中的文字内容，支持中英文混合识别。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 图片路径 | 要识别的图片文件 | \`C:/images/text.png\` |
| 结果变量名 | 存储识别文本的变量 | ocr_text |

**使用场景**：
- 识别图片中的文字信息
- 提取截图中的数据
- 识别扫描文档内容
- 读取图片验证码

**示例**：提取截图中的价格
\`\`\`
截图保存 -> 图片OCR -> 正则提取价格 -> 保存数据
\`\`\`

**示例**：批量识别图片文字
\`\`\`
遍历图片列表：
  ├─ 图片OCR：{item}
  ├─ 打印日志：识别结果: {ocr_text}
  └─ 保存到列表
\`\`\`

**注意事项**：
- 使用 ddddocr 库进行识别（项目已内置）
- 图片清晰度会影响识别准确度
- 支持 jpg、png、bmp 等常见图片格式
- 复杂排版的文字可能识别不完整

---

## AI 数据处理模块

AutoFlow 在侧边栏「AI数据处理」分类下提供了 8 个开箱即用的 AI 工作流模块，把大语言模型的理解能力直接嵌入工作流，让数据采集后的清洗、归类、判断都能交给 AI。

这些模块**默认复用全局 AI 配置**（在设置中填好一次 API 地址/密钥/模型即可），也可以在模块内单独覆盖。每个模块都把结果写入指定变量，供后续模块引用。

### 模块总览

| 模块 | 模块类型 | 用途 |
|------|----------|------|
| AI信息抽取 | ai_extract | 从非结构化文本中按字段抽取为 JSON 对象 |
| AI文本分类 | ai_classify | 把文本归入给定类别中最贴切的一个 |
| AI文本摘要 | ai_summarize | 把长文本压缩成指定字数/风格的摘要 |
| AI翻译 | ai_translate | 把文本翻译成目标语言，保留格式 |
| AI情感分析 | ai_sentiment | 判断文本情感倾向（正面/负面/中性）并打分 |
| AI数据规整 | ai_normalize | 把日期/金额/电话/数值/地址统一成标准格式 |
| AI语义去重 | ai_dedup_semantic | 对列表做语义层面去重（合并含义相同的项） |
| AI智能路由 | ai_route | 按内容智能选择后续分支，给工作流"判断力" |

---

### AI信息抽取（ai_extract）

把网页、邮件、订单、聊天记录等非结构化文本，按你指定的字段抽成结构化 JSON。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 待抽取文本 | 原始文本，支持变量 | \`{网页文本}\` |
| 抽取字段 | 逗号分隔的字段名，或"字段:说明"的 JSON | \`姓名,电话,地址,下单时间\` |
| 保存到变量 | 存储抽取结果（JSON对象） | 抽取结果 |

**结果使用**：
\`\`\`
{抽取结果[姓名]}     -> 张三
{抽取结果[电话]}     -> 13800138000
\`\`\`

> 找不到的字段会返回空字符串；若模型未返回合法 JSON，会兜底返回原始文本。

---

### AI文本分类（ai_classify）

把文本（工单、评论、邮件等）自动归入预设类别之一，并给出置信度和理由。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 待分类文本 | 要归类的文本，支持变量 | \`{工单内容}\` |
| 候选类别 | 至少两个，逗号分隔 | \`投诉,咨询,好评,退款\` |
| 保存到变量 | 存储命中的类别名 | 工单类别 |

> 模型只会从给定类别中选择，不会新造类别；保存的变量即类别名，可直接用于条件判断分支。

---

### AI文本摘要（ai_summarize）

把长文压缩成摘要，可指定最大字数和风格。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 待摘要文本 | 长文本，支持变量 | \`{文章正文}\` |
| 最大字数 | 摘要字数上限 | 200 |
| 风格 | 可选，如"正式/口语/要点列表" | 要点列表 |
| 保存到变量 | 存储摘要正文 | 文章摘要 |

---

### AI翻译（ai_translate）

把文本准确、地道地翻译成目标语言，保留原文换行格式。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 待翻译文本 | 原文，支持变量 | \`{评论内容}\` |
| 目标语言 | 翻译成的语言 | 英文 / 日文 / 法文 |
| 保存到变量 | 存储译文 | 译文 |

---

### AI情感分析（ai_sentiment）

判断文本的情感倾向，输出正面/负面/中性，并给出 -1~1 的情感分值。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 待分析文本 | 评论/反馈文本，支持变量 | \`{用户评论}\` |
| 保存到变量 | 存储分析结果 | 情感结果 |

**结果使用**：
\`\`\`
{情感结果[sentiment]}   -> 正面 / 负面 / 中性
{情感结果[score]}       -> 0.82（越接近1越正面）
{情感结果[confidence]}  -> 0.9
\`\`\`

---

### AI数据规整（ai_normalize）

把杂乱写法的日期、金额、电话、数值、人名、地址统一成标准格式，特别适合采集后的数据清洗。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 待规整内容 | 原始值，支持变量 | \`{原始日期}\` |
| 规整类型 | date/money/phone/number/name/address | date |
| 目标格式 | 可选，自定义目标格式 | YYYY-MM-DD |
| 保存到变量 | 存储规整后的结果 | 标准日期 |

**规整示例**：

| 类型 | 输入 | 输出 |
|------|------|------|
| date | 2024年3月5日 | 2024-03-05 |
| money | ￥1,299.00 | 1299 |
| money | 2.5万 | 25000 |
| phone | +86 138 0013 8000 | 13800138000 |

> 若类型不在预设列表中，可只填"目标格式"自定义规整规则。

---

### AI语义去重（ai_dedup_semantic）

对列表做**语义层面**的去重，合并"表达不同但含义相同"的项（普通去重只能去掉完全相同的字符串）。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 待去重列表 | 列表变量，或逗号/JSON 文本 | \`{商品名列表}\` |
| 保存到变量 | 存储去重后的列表 | 去重结果 |

**示例**：
\`\`\`
输入：["苹果手机", "iPhone", "华为Mate", "Mate系列"]
输出：["苹果手机", "华为Mate"]  （iPhone 与苹果手机语义重复被合并）
\`\`\`

> 为保证质量，单次建议 ≤300 项，超出请先分批；去重只保留每组重复中第一个出现的项，不改写原内容。

---

### AI智能路由（ai_route）

根据输入内容，从你给定的若干分支中智能选出最合适的一个，输出分支名。配合条件判断使用，让工作流具备"判断力"。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 待判断内容 | 用于决策的文本，支持变量 | \`{客户消息}\` |
| 分支定义 | 至少两个，"名称:说明"用分号分隔 | \`退款:要求退钱; 咨询:询问信息; 投诉:表达不满\` |
| 保存到变量 | 存储选中的分支名 | 路由结果 |

**注意**：分支说明里常含逗号，因此分支之间请用**分号 ; 或换行**分隔，不要用逗号。

**典型组合**：
\`\`\`
采集客户消息 -> AI智能路由（退款/咨询/投诉）
  ├─ 条件判断：{路由结果} == "退款"  -> 走退款流程
  ├─ 条件判断：{路由结果} == "咨询"  -> 自动回复常见问题
  └─ 条件判断：{路由结果} == "投诉"  -> 转人工 + 建工单
\`\`\`

---

### AI 数据处理实战流水线

把多个 AI 模块串起来，可以搭出"会思考"的自动化流程：

**电商评论舆情监控**：
\`\`\`
采集评论列表 -> 遍历列表
  ├─ AI情感分析：{item}
  ├─ AI智能路由：负面->建工单 / 好评->归档
  └─ AI信息抽取：抽出涉及的商品、问题点
\`\`\`

**杂乱数据清洗入库**：
\`\`\`
准备列表变量 -> 遍历列表
  ├─ AI数据规整：日期 -> 标准日期
  ├─ AI数据规整：金额 -> 纯数字
  └─ 保存处理结果
\`\`\`

**多语言客服分流**：
\`\`\`
接收消息 -> AI翻译（统一成中文）-> AI文本分类 -> AI智能路由 -> 对应处理分支
\`\`\`

`
