// Source: WebRPA@5ccb900e, components/workflow/documentation/content-browser.ts; see SOURCE.md for license and adaptation boundaries.
export const browserGuideContent = `# 自动化浏览器详解

> 当前 Studio 教学范围为保留节点及前端配置交互。Mock 用于交互演示，不执行真实网页、模型或系统操作；文中的执行行为需正式后端支持。

本章详细介绍自动化浏览器的工作原理、配置方法和使用技巧。

---

## 当前连接方式

Studio 通过服务接口请求打开浏览器、导航、拾取和录制。当前 Mock 用于前端交互验证，不会启动真实浏览器，也不保存真实登录状态。

浏览器内核与配置由 AutoFlow 的浏览器服务管理。本页不要求安装旧项目运行时或操作旧项目数据目录。

## 打开自动化浏览器

### 方式一：工具栏按钮

点击顶部工具栏的 **自动化浏览器** 按钮，可以：
- 打开/关闭浏览器
- 导航到指定网址
- 启动元素选择器

### 方式二：运行工作流

“打开网页”节点保存导航参数；实际启动与导航由运行服务处理。

---

## 元素选择器

元素选择器是 AutoFlow 的核心功能，让你无需编写 CSS 选择器就能定位网页元素。

### 启动方式

1. **工具栏启动**：点击"自动化浏览器" -> "启动选择器"
2. **配置面板启动**：点击选择器输入框旁的 按钮

### 选择模式

| 操作 | 功能 | 使用场景 |
|------|------|----------|
| **Ctrl + 点击** | 选择单个元素 | 选择按钮、输入框等 |
| **Alt + 点击** | 选择相似元素 | 选择列表项、表格行等 |
| **Esc** | 取消选择 | 重新选择 |

### 单元素选择

1. 启动选择器
2. 按住 **Ctrl** 键
3. 点击目标元素
4. 选择器自动填入配置

### 相似元素选择

用于选择多个结构相似的元素（如商品列表）：

1. 启动选择器
2. 按住 **Alt** 键
3. 点击第一个元素
4. 点击第二个相似元素
5. 系统自动识别所有相似元素

**生成的选择器示例**：
\`\`\`css
.product-list > div:nth-child({index})
\`\`\`

其中 \`{index}\` 是变量，配合循环使用可遍历所有元素。

---

## 页面导航

### 打开网页

\`\`\`
模块：打开网页
网址：https://www.example.com
等待条件：load（页面完全加载）
\`\`\`

### 等待条件详解

| 条件 | 说明 | 适用场景 |
|------|------|----------|
| load | 等待所有资源加载完成 | 普通网页 |
| domcontentloaded | DOM 加载完成即可 | 快速响应 |
| networkidle | 网络空闲 | Ajax 动态页面 |

### 页面跳转

| 模块 | 功能 |
|------|------|
| 刷新页面 | 重新加载当前页面 |
| 返回上一页 | 浏览器后退 |
| 前进下一页 | 浏览器前进 |
| 关闭页面 | 关闭当前标签页 |

---

## 处理弹窗

### 浏览器原生弹窗

处理 \`alert\`、\`confirm\`、\`prompt\` 弹窗：

| 弹窗类型 | 处理方式 |
|----------|----------|
| alert | 点击确定 |
| confirm | 点击确定或取消 |
| prompt | 输入文本后确定 |

**配置示例**：
\`\`\`
模块：处理弹窗
操作：确认（点击确定）
输入文本：验证码123（仅 prompt 需要）
\`\`\`

### 网页自定义弹窗

网页自定义的弹窗（非浏览器原生）需要用普通的点击操作处理：
\`\`\`
1. 等待元素：.modal-dialog 出现
2. 点击元素：.modal-confirm-btn
\`\`\`

---

## 性能优化

### 减少等待时间

\`\`\`
[×] 不推荐：固定等待 5000 毫秒
[√] 推荐：等待元素 #content 出现
\`\`\`

### 合理设置超时

| 场景 | 建议超时 |
|------|----------|
| 普通页面加载 | 30秒 |
| 复杂页面/慢网络 | 60秒 |
| 元素等待 | 10秒 |
| 文件下载 | 根据文件大小 |

### 避免频繁操作

\`\`\`
[×] 不推荐：
点击 -> 立即获取数据

[√] 推荐：
点击 -> 等待元素出现 -> 获取数据
\`\`\`

---

## 反检测说明

AutoFlow 使用真实浏览器，但某些网站可能检测自动化行为。

### 降低检测风险

1. **添加随机延迟**：操作之间加入随机等待
2. **模拟人类行为**：不要操作过快
4. **合理的请求频率**：避免短时间大量请求

### 示例：添加随机延迟

\`\`\`
循环执行
  ├─ 执行操作
  ├─ 随机数：1000 到 3000
  └─ 等待：{随机数} 毫秒
\`\`\`

---

## 网页智能录制（录制即生成节点）

不想手动连节点？直接在浏览器里操作，AutoFlow 自动录成工作流节点。

### 使用

\`\`\`
1. 先打开自动化浏览器
2. 顶栏「录制」下拉 ->「网页智能录制」
3. 点「开始录制」，然后在浏览器里正常操作
4. 点「停止录制」-> 按需编辑 ->「生成节点」追加到画布
\`\`\`

### 能录到什么

\`\`\`
- 点击、输入、下拉选择、勾选
- 滚动（自动合并方向与距离）
- 功能键 / 组合键（回车、Tab、方向键、Ctrl+C 等）
- 页面导航（打开/跳转网址）
\`\`\`

### 跨页面 / 新标签页不中断

录制步骤由服务事件更新。停止后检查收到的步骤及导航关系；当前 Mock 不能用于验证真实页面采集或跨页可靠性。

### 录制后可编辑

\`\`\`
停止录制后，每一步支持：
- 删除该步
- 上移 / 下移调整顺序
勾选「自动等待」后，会按操作间隔自动插入等待节点（应对加载慢的页面）。
\`\`\`

> 录制生成的是标准节点，可以像手搭的工作流一样继续修改、加条件/循环。

---

## 常见问题

### 元素找不到

**可能原因**：
- 选择器不正确
- 元素在 iframe 中
- 元素动态加载

**解决方法**：
1. 使用元素选择器重新选择
2. 添加等待元素模块
3. 检查是否需要切换 iframe

## 最佳实践

### 工作流开始

\`\`\`
1. 打开网页
2. 等待关键元素出现
3. 检查登录状态
4. 开始执行任务
\`\`\`

### 工作流结束

\`\`\`
1. 保存数据
2. 打印完成日志
3. （可选）关闭页面
4. （可选）播放提示音
\`\`\`

### 错误处理

\`\`\`
条件判断：{元素存在} == true
  ├─ 真 -> 继续执行
  └─ 假 -> 打印错误日志 -> 跳出循环
\`\`\`
`
