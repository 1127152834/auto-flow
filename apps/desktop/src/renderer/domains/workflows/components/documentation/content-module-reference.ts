// Source: WebRPA@5ccb900e, components/workflow/documentation/content-module-reference.ts; see SOURCE.md for license and adaptation boundaries.
export const moduleReferenceContent = `# 模块速查表

> 当前 Studio 教学范围为保留节点及前端配置交互。Mock 用于交互演示，不执行真实网页、模型或系统操作；文中的执行行为需正式后端支持。

本页按侧边栏分类逐个列出模块，用于「我知道要做什么，但不知道该用哪个模块」时反查。
每类先说用途与典型组合，再列模块清单；需要完整配置说明和实战案例时，跳转到对应的专题文档。

> 查模块的三种方式：① 本页按分类找；② 左侧模块搜索框直接搜中文、拼音或首字母（输入 \`dkwy\` 就能搜到「打开网页」）；③ 问 AI 小助手「我要做 XX，用哪个模块」。

---

## 网页自动化补充模块

### 网页导航

配合「打开网页」使用的页面级操作。iframe 是最容易踩坑的一类：页面里嵌套的 iframe 属于独立文档，不先切进去，里面的元素怎么找都找不到。

| 模块 | 用途 |
|------|------|
| 操作已打开的网页 | 配置复用页面的目标，实际页面连接由浏览器服务提供 |
| 关闭网页 | 关掉当前标签页，一般放在工作流末尾释放资源 |
| 切换iframe | 进入指定 iframe，之后的元素操作都作用在这个 iframe 内 |
| 切换回主页面 | 从 iframe 退回主文档，忘了退出会导致后续找不到主页面元素 |
| 网页是否加载完成 | 把加载状态写入变量，供条件判断用；不阻塞流程 |

### 等待页面加载完成（wait_page_load）

在页面导航后等待指定加载状态。等待条件 waitUntil 可选 load（默认）、domcontentloaded、networkidle；超时时间 timeout 使用秒，默认 60，界面最小值为 1。它等待当前页面状态，不代替“等待元素”对具体选择器的等待。

### 网页元素交互与查询

「元素存在判断」「元素可见判断」返回布尔值到变量，是写健壮流程的基础：先判断再操作，避免元素没出现就点击而报错。

| 模块 | 用途 |
|------|------|
| 勾选框 | 勾选 / 取消勾选复选框，按目标状态设置而非盲目点击 |
| 元素存在判断 | 元素是否存在于 DOM，结果存入变量 |
| 元素可见判断 | 元素是否可见（存在但被隐藏时为 false） |
| 表格数据提取 | 把 HTML 表格整体读成列表，比逐个单元格取值快得多 |

### 网页数据采集：网络监听

抓不到页面上渲染的数据时（数据是 JS 动态填的、或分页走的是接口），改成监听接口响应更稳。三个模块要按顺序成套使用。

| 模块 | 用途 |
|------|------|
| 开始网络监听 | 打开监听，之后页面发出的请求都会被记录 |
| 等待API请求 | 阻塞等待匹配 URL 的请求完成，把响应体写入变量 |
| 停止网络监听 | 关闭监听并释放资源 |

典型顺序：开始网络监听 → 点击元素（触发请求）→ 等待API请求 → 停止网络监听。

## 系统剪贴板

### 写入剪贴板（set_clipboard）

内容类型 contentType 默认 text，填写 textContent，可通过变量输入框引用工作流变量。选择 image 时填写 imagePath，可使用共享图像路径工具。该节点配置系统剪贴板写入，不属于已排除的图像编辑节点。

### 读取剪贴板（get_clipboard）

读取系统剪贴板文本，存入 variableName 指定的变量，默认变量名为 clipboard_content。可接“打印日志”检查文本，或让后续节点通过变量引用使用结果。

## 流程控制与触发器补充模块

### 流程控制

| 模块 | 用途 |
|------|------|
| 无限循环 | 一直循环直到被「跳出循环」终止。索引变量默认 \`loop_index\`（注意「循环」模块默认是 \`index\`），务必在循环体内写好退出条件 |
| 断言/检查点 | 校验变量 / 元素 / 表达式是否符合预期，失败时按配置中断或仅记录。做自动化测试时用它产出可读的结论 |
| 强制停止工作流执行 | 立即结束整条工作流，可填停止原因写进日志 |
| 运行其它工作流 | 把另一个工作流文件当子流程整体执行，用于复用与拆分大流程 |

### 触发器

| 模块 | 用途 |
|------|------|
| 手势触发器 | 用摄像头识别手势来触发后续流程，可设置置信度阈值与超时；识别结果写入变量 |

---

## 数据处理补充模块

### 列表操作

列表类模块统一约定：输入是「列表变量名」，输出写进「结果变量」。它们不修改原列表，方便串联。

| 模块 | 用途 |
|------|------|
| 列表导出 | 把列表写成文件（CSV / JSON 等） |
| 列表求和 | 数值列表求和 |
| 列表求平均值 | 数值列表求平均 |
| 列表求最大值 | 取最大值 |
| 列表求最小值 | 取最小值 |
| 列表排序 | 升序 / 降序排序，支持按字段排序 |
| 列表去重 | 去掉重复元素 |
| 列表截取 | 按起止下标取子列表 |
| 列表反转 | 顺序反转 |
| 列表查找 | 查找元素并返回下标或元素本身 |
| 列表计数 | 统计元素出现次数 |
| 列表过滤 | 按条件筛选出符合的元素 |
| 列表映射 | 对每个元素做同一种转换 |
| 列表扁平化 | 把嵌套列表拉平成一层 |
| 列表分组 | 按固定长度切成多段，常用于分批处理 |
| 列表去空 | 去掉空字符串 / null 元素 |
| 列表交集 | 两个列表共有的元素 |
| 列表并集 | 两个列表合并去重 |
| 列表差集 | 只在第一个列表里的元素 |
| 列表笛卡尔积 | 两个列表的所有组合 |
| 列表随机打乱 | 打乱顺序 |
| 列表采样 | 随机取 N 个元素 |
| 列表转字符串（高级） | 按分隔符拼接，可指定前后缀与字段 |

### 字典操作

| 模块 | 用途 |
|------|------|
| 字典键列表 | 取出所有键成列表 |
| 字典合并 | 合并两个字典，后者覆盖前者同名键 |
| 字典过滤 | 按条件保留部分键值对 |
| 字典映射值 | 对每个值做同一种转换 |
| 字典反转 | 键值互换 |
| 字典排序 | 按键或值排序 |
| 字典深拷贝 | 复制一份互不影响的字典 |
| 字典路径取值 | 用 \`a.b.c\` 这类路径取嵌套值，比逐层取值省事 |
| 字典扁平化 | 把嵌套字典拉平成单层，键用路径表示 |

> 处理接口返回的 JSON 时，「字典路径取值」+「字典扁平化」这对组合最常用：前者精准取一个字段，后者一次看清整个结构。

### 数学与统计

| 模块 | 用途 |
|------|------|
| 求余 | 取余数 |
| 开次方 | 开任意次方（默认平方根） |
| 求次方 | 求任意次幂 |
| 对数运算 | 支持自定义底数 |
| 指数运算 | 自然指数 |
| 排列组合 | 计算排列数 / 组合数 |
| 数字范围限制 | 把数值夹到 [最小, 最大] 区间内，防越界 |
| 随机数生成（高级） | 支持整数 / 浮点、指定分布与精度 |
| 数据归一化 | 把一组数缩放到 0~1 |
| 数据标准化 | 按均值和标准差做 Z-score 变换 |

---

## 数据表格行操作

### 删除行（table_delete_row）

使用 rowIndex 指定数据表格中的行，从 0 开始，-1 表示最后一行；此字段支持变量引用。它操作工作台数据预览中的表格，不读写 Excel 文件。

### 清空表格（table_clear）

清空数据预览表格中的全部数据，无额外节点参数。若需保留结果，应先导出数据；删除的数据不能通过编辑画布的撤销来恢复。

## AI 能力补充模块

### AI 视觉操作

| 模块 | 用途 |
|------|------|
| AI视觉操作 | 给一句自然语言指令（如「点击登录按钮」），由 AI 看屏幕截图判断该点哪里并执行。元素定位与图像识别都失效时的兜底方案，准确率依赖所用模型 |

### AI 生成

| 模块 | 用途 |
|------|------|
| AI生成图片 | 按提示词生成图片，结果路径写入变量 |
| AI生成视频 | 按提示词生成视频（耗时较长，注意把超时设足） |

### AI 爬虫

| 模块 | 用途 |
|------|------|
| AI智能爬虫 (实验性) | 给网址和一句需求描述，由 AI 决定怎么抓、抓什么 |
| AI元素选择器 (实验性) | 给一句描述让 AI 生成选择器，用于选择器难写的页面 |
| AI单页数据抓取 | 抓单个页面并结构化输出 |
| AI网站链接抓取 | 抓出站点内的链接清单 |
| AI全站数据抓取 | 按规则遍历整站抓取 |

> 标了「实验性」的两个模块结果不稳定，适合探索阶段用；正式流程建议用它们先摸清页面结构，再换成固定选择器的常规模块。

---

## 网络与集成补充模块

### 网络请求

| 模块 | 用途 |
|------|------|
| Webhook请求 | 发送 HTTP 请求（可自定义方法、请求头、请求体），响应体 / 状态码 / 响应头分别写入变量 |

### 消息推送

十几个渠道的配置模式一致：填服务端地址或令牌 + 消息内容。选哪个取决于团队在用什么。

| 模块 | 用途 |
|------|------|
| Discord通知 | 发到 Discord 频道 |
| Telegram通知 | 发到 Telegram（需机器人令牌与会话 ID） |
| Bark通知 | 推到 iOS 的 Bark 客户端 |
| Slack通知 | 发到 Slack 频道 |
| Teams通知 | 发到 Microsoft Teams |
| Pushover通知 | 通过 Pushover 推送 |
| PushBullet通知 | 通过 PushBullet 推送 |
| Gotify通知 | 推到自建 Gotify 服务 |
| Server酱通知 | 通过 Server 酱推到微信 |
| PushPlus通知 | 通过 PushPlus 推到微信 |
| Webhook通知 | 推到任意自定义 Webhook 地址 |
| Ntfy通知 | 推到 ntfy 主题（可自建） |
| Matrix通知 | 发到 Matrix 房间 |
| RocketChat通知 | 发到 Rocket.Chat 频道 |

### 通知与日志

| 模块 | 用途 |
|------|------|
| 系统消息 | 弹出 Windows 系统通知（标题 + 内容），用于提醒本机用户 |

### SSH 远程

| 模块 | 用途 |
|------|------|
| SSH上传文件 | 本地文件传到远程主机 |
| SSH下载文件 | 远程文件拉到本地 |
| SSH断开连接 | 关闭 SSH 会话 |

### 局域网共享

| 模块 | 用途 |
|------|------|
| 文件夹网络共享 | 把本机文件夹共享到局域网 |
| 文件网络共享 | 共享单个文件 |
| 停止网络共享 | 取消共享 |
| 开始屏幕共享 | 把本机屏幕共享到局域网供他人查看 |

## 实用工具补充模块

### 加密与编码

| 模块 | 用途 |
|------|------|
| MD5加密 | 计算 MD5 摘要 |
| SHA加密 | 计算 SHA 系列摘要 |
| URL编解码 | URL 转义 / 反转义 |
| UUID生成器 | 生成 UUID，默认变量名是 \`uuid\` |

### 颜色与时间转换

| 模块 | 用途 |
|------|------|
| RGB转HSV | 颜色空间转换 |
| RGB转CMYK | 转印刷色值 |
| HEX转CMYK | 十六进制色值转印刷色值 |

---

## 找不到想要的模块时

1. 先在左侧搜索框用**功能词**搜（如「压缩」「去重」「截图」），模块名之外的关键词也参与匹配。
2. 再问 AI 小助手：直接描述目标（「我要把网页采集到的列表去重后保存」），它会给出模块组合建议。
3. 确认确实没有对应模块时，用「自定义模块」把一段 Python / JS 逻辑封装成模块复用，或用「执行Python脚本」直接写逻辑。
`
