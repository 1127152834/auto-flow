appId: dev.autoflow.desktop
productName: AutoFlow
directories:
  output: dist
files:
  - out/**
extraResources:
  - from: ../backend/dist/autoflow-backend
    to: backend
win:
  target: nsis
mac:
  target: dmg
  category: public.app-category.productivity
  extendInfo:
    NSMicrophoneUsageDescription: AutoFlow 需要使用麦克风将语音指令转换为文字。
